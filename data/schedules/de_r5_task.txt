你是 QuantLab 项目的量化策略验收员。你上次（R4 审查）发现 P20 全天候策略存在"收益不复投 + NAV 永不滚动"的 P0 工程 bug，TRAE 已修复并重建产物。请验收修复是否正确、是否引入新问题，并给出通过/不通过结论。

【背景】
- 修复对象：D:\QuantLab\projects\Project_20_全天候底仓\strategy\strategy_all_weather_src.py（修复后源码）
- 重建产物：D:\QuantLab\projects\Project_20_全天候底仓\build\strategy_all_weather_v1.py（BUILD_TAG=20260913-173053，GBK 单文件）
- 构建脚本：D:\QuantLab\projects\Project_20_全天候底仓\build.py
- 你上次发现的问题（R4 结论，见 D:\QuantLab\data\schedules\de_optimize_ab_20260913.md）：_target_shares 恒用 CAPITAL_BASE=10万 计算目标份额；_settle 的 nav 只从 NAV_FILE 读旧值写回，全代码无收益更新路径 → 浮盈闲置丢复利 + 模拟盘验证指标失效。
- 你上次的三条修复建议：①目标份额改用当前 NAV（收益复投）②漂移带改权重口径 ③限价 ±1% 收窄 ±0.3%。

【验收点（逐项 PASS/FAIL + 证据）】
A. NAV 滚动实现：
  A1. _estimate_nav 函数逻辑正确性——持仓市值满仓近似是否合理（四腿 ETF 权益组合、月频再平衡后基本满仓的前提是否成立）；空仓时返回 CAPITAL_BASE 是否合理；部分持仓时市值估算是否正确。
  A2. handlebar 中目标份额计算是否改用滚动 NAV（不再用 CAPITAL_BASE）。
  A3. _settle 是否写回滚动 NAV（而非只读旧值）。
  A4. init 中 nav 初始化为 CAPITAL_BASE 是否合理。
  A5. 边界：_estimate_nav 在 prices 缺腿、positions 空、单腿停牌时的行为是否安全（有无除零/NaN）。
B. 漂移带权重口径：
  B1. cur_w = cur*price/nav 与目标权重 w 比较、|cur_w-w|<DRIFT_BAND 跳过——实现是否正确（尤其 nav>0 防护）。
  B2. 是否还有残留的金额口径（CAPITAL_BASE*DRIFT_BAND）旧逻辑。
C. 限价收窄：PRICE_OFFSET=0.003 是否生效，买卖价格计算（±0.3%）是否正确。
D. 产物校验：
  D1. 产物 GBK 编码、# coding=gbk 文件头、BUILD_TAG=20260913-173053 存在。
  D2. 产物 Py3.6 语法兼容（ast feature_version 3.6）、无 MOCK 残留、无 TAG_PLACEHOLDER。
  D3. 产物与源码一致（修复逻辑都进了产物）。
E. 回归风险：
  E1. 修复是否引入新问题（如 _settle 在 positions 为空时 nav 估算、目标计算循环依赖）。
  E2. 四腿权重合计 1.0 不变、DRIFT_BAND/TRIGGER_HHMM/LOT 等常量未被误改。
F. 次要（时间允许）：P19 D:\QuantLab\projects\Project_19_可转债双低\research\cb_double_low.py 新增的 --min-list-days 与 --cost 参数实现是否安全（不影响默认 baseline 结果）；P20 D:\QuantLab\projects\Project_20_全天候底仓\research\aw_512890_compare.py 是否有明显 bug。

【方法】
- 只读审查 + 可运行只读 python 脚本做逻辑验证（脚本与中间产物只允许写 D:\QuantLab\data\schedules\de_r5_\ 目录）；
- 禁止修改 D:\QuantLab 任何文件、禁止 git commit、禁止运行任何会改变数据/账本/配置的命令；
- 数字必须来自真实代码/数据，禁止编造；每个验收点给出 PASS/FAIL + 简短证据（代码行/运行结果）。

【输出】
- 唯一输出文件：D:\QuantLab\data\schedules\de_acceptance_ab_20260913.md；
- 结构：一、总评（通过/有条件通过/不通过，一句话结论）；二、验收点逐项表（A1-F2，PASS/FAIL/证据）；三、发现的问题清单（若有，含严重度 P0/P1/P2 与修复建议）；四、结论与建议（是否可上模拟盘验证单）。
- 完成后直接写文件并退出，不要询问下一步、不要等待确认。
