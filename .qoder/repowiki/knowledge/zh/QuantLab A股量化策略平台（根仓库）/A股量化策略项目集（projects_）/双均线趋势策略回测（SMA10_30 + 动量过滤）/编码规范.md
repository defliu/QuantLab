- 策略逻辑与回测执行分离：`strategy/dual_ma.py` 暴露 `precompute_ma/get_candidates/should_sell` 纯函数，由 `run_backtest.py` 负责仓位与资金循环。
- 通过模块级全局 `_ma_cache` 字典缓存面板级均线与信号矩阵，避免每日重复计算。
- 股票筛选以流通市值排序取 Top N（当前 4000），并剔除 ST 与停牌（suspend_type 在 S/R/R&S）后再进入回测。
- 交易日志通过追加写入 `reports/` 目录下的 equity.csv / trades.csv / sl_events.csv 三表结构记录完整过程。