- 本地验证：`python src/_strategy_atr_local.py`（走 miniQMT venv，≈10秒出选股/排序结果）。
- 构建后校验：`python validate_build.py`，强制检查 GBK 可解码、首行 #coding=gbk、py3.6 语法、无 f-string/:= 、无 MOCK 残留、必要中文字段、BUILD_TAG 格式及 Fix4a/4b/4c 三态回退逻辑存在。
- QMT 构建入口来自仓库 `broker/qmt_builder.py`，产物落至 `build/strategy_atr_lowvol_equalweight.py`（当前实盘部署文件）。
- 实盘账号固定为 `70180771`（新国金模拟端），禁止与其他策略混用。