BOM 月末效应：`python research/bom_turn_of_month.py` 直接产出 `results/BOM月末效应复现报告_*.md`、`bom_monthly_window_returns.csv`；菜场大妈 A4：`python research/run_a4_engine.py` 拉取全量 astock parquet 构建 aux 宽表（约需数秒～十秒级），写入 `results/taskb_A4_kpi.json` 并通过 engine.report 落盘 `results/a4_engine_out/<timestamp>/report.md`；风险栈变体通过 `run_riskstack.py` 驱动同一策略模块的不同 strategy_params 跑一组 R*_id 实验。