Python 3.11；pandas / numpy 做向量化解法；duckdb 直读 index_daily 表；parquet（E:/astock/daily/stock_daily.parquet、finance/dividend/income/basic 等）作为主数据源；PyYAML 加载策略配置；目标运行环境引用根目录 `backtest.engine` 与 `data.astock_reader` 统一回测框架。