Python 3.6.8 兼容（QMT 构建期强制校验），无第三方依赖（QMT 环境不保证 pyyaml，自建 YAML parser）；数据层复用 Project_12 的黄氏529公式库 (`huangshi_formula_scan`)；本地验证经 `broker/local_context.LocalContext` 对接 miniQMT 行情；信号产物为 UTF-8 CSV 部署于 `D:/QMT_POOL/`。