运行前需确保 `E:/astock/daily/stock_daily.parquet` 与 `E:/astock/finance/fina_indicator.parquet` 存在，并通过修改 `DAILY_PATH`、`FINANCE_PATH`、`START_DATE`、`END_DATE`、`INITIAL_CAPITAL`、`MAX_STOCKS`、`HOLDING_DAYS`、`STOP_LOSS`、`OUTPUT_DIR` 等顶层常量调整参数；执行入口为 `python projects/Project_08_指数增强/run_index.py`。