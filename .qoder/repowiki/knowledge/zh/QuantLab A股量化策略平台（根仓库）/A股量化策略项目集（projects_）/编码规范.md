- 每个 Strategy 项目采用 `run_backtest.py`/`local_validate.py` 入口 + `strategy/` 模块化 + `results/` 输出 + `configs/*.yaml` 配置的目录布局。
- 回测结果统一以 CSV/JSON 落盘到项目内 `results/` 或 `data/` 目录，便于外部看板汇总。
- 策略演进遵循 “research 试错 → build/strategy_vX 稳定版 → specs/审计报告” 的版本化路径。
- 涉及实盘的项目将交易执行抽到 `trade_executor.py`/`qmt_trader.py`，策略仅输出买卖信号，与QMT解耦。