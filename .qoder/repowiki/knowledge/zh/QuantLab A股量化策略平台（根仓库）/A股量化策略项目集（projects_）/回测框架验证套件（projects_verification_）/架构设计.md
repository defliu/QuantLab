测试分两组，均带 `if __name__ == "__main__"` 可直接运行，报告落 `results/`：
- `engine_tests.py`（B 组，引擎正确性）：B-1 已知结果复现、B-2 交易成本、B-3 涨跌停与停牌状态机、B-4 资金约束、B-5 结果一致性（可复现性）、B-6 先卖后买、B-7 止损、B-8 其它引擎契约；输出 `results/engine_test_report.md`。
- `robustness_tests.py`（D 组，鲁棒性）：D-1 子样本、D-2 牛熊分段、D-3 压力情景、D-4 成本压力、D-5 容量、D-6 未来函数检查、D-7 幸存者偏差；输出 `results/robustness_report.md`。
- 另有 `execution_tests.py`（撮合层）、`portfolio_tests.py`（组合记账）、`feasibility_tests.py`（可行性）作为补充。
- 输入为各策略的 equity 曲线（`Project_05/06/08/results/*equity.csv`）与 `D:/astock/daily/stock_daily.parquet`。
- 依赖 `backtest.engine` 与 `data.astock_reader`，因此验证的是「真实引擎 + 真实数据」而非桩。
