- 每个 Phase/Variant 写为自包含脚本，顶部注释声明对应任务书版本与冻结判据，禁止在脚本内改参，仅实现判据逻辑。
- 情绪五指标一律用滚动 250 日窗口做 z-score 标准化，再以等权合成 `sentiment_score` → 滚动 min-max 归一得 `pct_rank` → pd.cut 切五阶段。
- 小盘池口径统一为 circ_mv < 500000（万元）、is_st != 1、listed_days >= 60，用于所有未来收益基准对比。
- 判定结论以结构化 verdict list / dict 打印并通过 f-string 拼接成 Markdown 表格，写入 `results/Phase*_*.md` 归档。
- 回测收益计算统一使用前向偏移 shift(-FWD) 构造 fwd5/fwd20，避免任何 T+0 取价的前视偏差。