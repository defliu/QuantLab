运行前需在 `config/strategy.yaml` 中确认 astock parquet 路径；研究入口为 `python research/build_sentiment.py` 生成情绪序列后，再依次运行 `phase0_robust_check.py` / `phase0v2_a_active.py` / `phase0v2_b_overlay.py` / `phase0v3_limitup_structure.py` 逐项检验冻结判据。