目录下仅 `run_backtest.py`（LightGBM 多因子回测优化版 v2）与空的 `reports/`。关键常量：`DAILY_PATH="D:/astock/daily/stock_daily.parquet"`、`INITIAL_CAPITAL=100000`、`MAX_POSITIONS=30`、`HOLDING_DAYS=5`、`TRAIN_WINDOW=504`、`FORWARD_DAYS=5`。流程为直接读主源 parquet（过滤 ST / 停牌）→ 滚动训练窗口 504 日 → 预测 5 日前瞻收益 → 等权持 30 只持有 5 日，区间 2020-01-01~2026-06-30，产物写 `reports/`。**该脚本未使用统一 `backtest.engine`，也未引用 `models/` 目录**，与 Project_16 体系完全隔离，因此其口径不可与 Project_16 结果直接对比。
