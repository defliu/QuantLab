运行流程分两步：先执行 `python research/gen_trae_uptrend_signal_table.py` 生成 `signal_table_trae_top16.json` 与 `market_ma200.json`，再执行 `python research/run_trae_uptrend.py trae_uptrend_base` 触发回测；硬编码路径指向 `D:/QuantLab`、`E:/astock/daily/stock_daily.parquet` 与 `F:/backtest_workspace/data/duckdb/benchmark_index.duckdb`，需在 Windows QuantLab 环境下使用。