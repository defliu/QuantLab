- 选股信号提前离线计算并持久化为 JSON 表供回测查表使用，避免在 `run_backtest` 期间计算带来性能损耗与时序风险。
- 财务因子（ROE）通过 `vectorized_roe_panel` 以 PIT 安全方式对齐到指标索引（`ed <= date`），禁止直接使用原始财务日期。
- 策略配置统一外置为 YAML，由 `run_one` 按 name key 解析后拆分 `backtest/data/execution/strategy_params` 四段传入引擎。
- 全局常量（SIGNAL_TABLE、MARKET_MA200、REPORT_ROOT、PROJECT_ROOT）在模块顶部集中定义并通过 `sys.path.insert` 追加项目根目录以便 import 公共库。