Python 3.x（开发端）+ Python 3.6.8 兼容（QMT端，禁止 walrus/type hint/match/case/f-string）；pandas/numpy 面板计算；PyYAML 配置；QMT xtdata 行情与 passorder 交易；本地 miniQMT 适配器 broker/local_context.py 用于快速联调；DuckDB/pandas parquet 作为回测数据源（E:/astock）。