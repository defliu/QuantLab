定时任务通过 PowerShell 统一入口 `powershell -NoProfile -ExecutionPolicy Bypass -File run_scheduled.ps1 -Mode {daily|monitor|retrain|factor}` 调度；每日增量需先有 `E:/astock/Updatedata` 周子目录含 `stock_daily.parquet`，由 `data_config.read_main_daily` 在主仓基础上增量合并。服务器部署参考 `SERVER_DEPLOY.md`，模型晋升用 `promote_model.py`，回测切换参考 `backtest_dual.py`。

**G2（2026-09 起）**：

```bash
python rebalance_g2.py            # 默认 dry-run 出换仓计划；加 --live 才写桥
python reconcile_g2.py            # 日终对账（fills / pending / 持仓差额）
python qmt_bridge_client.py write-cfg --from-fills     # 写当日成本锚 positions_cfg_<date>.json
python scripts/wudao_client.py <tool> '<json_args>'    # 悟道直连（失败打 [WUDAO-ERR] 且 exit!=0）
python scripts/refresh_tushare_moneyflow.py            # F2 权威主源刷新（单位：万元，禁止 ÷1e4）
python scripts/nightly_check.py --fix                  # 01:00 夜间检修，报告 data/cache/nightly_check_<date>.md
```

隔离硬约束：G2 只认 `g2_config.py` 与 `D:/QMT_POOL/g2_bridge/` 下的账本/资金池，**不 import V1.3 的 `qmt_config`、不读 `D_model_top10`、不调 `qmt_trader`**；账号必须 `70180771`。部署后核对 QMT 主日志是否出现 `CTradeClient::order`。