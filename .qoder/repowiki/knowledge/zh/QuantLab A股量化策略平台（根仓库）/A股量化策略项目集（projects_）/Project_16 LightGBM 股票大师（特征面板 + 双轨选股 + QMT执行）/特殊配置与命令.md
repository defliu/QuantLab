定时任务通过 PowerShell 统一入口 `powershell -NoProfile -ExecutionPolicy Bypass -File run_scheduled.ps1 -Mode {daily|monitor|retrain|factor}` 调度；每日增量需先有 `E:/astock/Updatedata` 周子目录含 `stock_daily.parquet`，由 `data_config.read_main_daily` 在主仓基础上增量合并。服务器部署参考 `SERVER_DEPLOY.md`，模型晋升用 `promote_model.py`，回测切换参考 `backtest_dual.py`。

**G2（2026-09 起）**：

```bash
python rebalance_g2.py            # 默认 dry-run 出换仓计划；加 --live 才写桥
python reconcile_g2.py            # 日终对账（fills / pending / 持仓差额）
python scripts/wudao_client.py <tool> '<json_args>'    # 悟道直连（失败打 [WUDAO-ERR] 且 exit!=0）
python scripts/refresh_tushare_moneyflow.py            # F2 权威主源刷新（单位：万元，禁止 ÷1e4）
python scripts/nightly_check.py --fix                  # 01:00 夜间检修，报告 data/cache/nightly_check_<date>.md
# 2026-09-13 升级（T-20260912-003，DE 体检）：A1/B1 外部日历锚（断流必报警）、E2 三桥心跳+内容校验、
# C1 任务白名单自动核对、F4 语义匹配（不再误报）、--push-alert 飞书告警、B5 enh/B6 merged/B7 ENS 候选、
# D2 真熔断 600s 窗口；fail-loud=FAIL 即 exit 1。手动验证：python scripts/nightly_check.py --push-alert
```

**成本锚盘前生成链（2026-09-10 起为唯一权威机制，T-20260908-002 双层修复；09-11 增融合第三腿）**：

```bash
# 定时任务 Quant_P16_CfgPremarket_0900（工作日 09:00，StartWhenAvailable+电池放开）跑：
powershell -NoProfile -ExecutionPolicy Bypass -File gen_positions_cfg_premarket.ps1
#   ├─ gen_positions_cfg_g2.py  -> D:/QMT_POOL/g2_bridge/cmd/positions_cfg_<date>.json
#   │    （当日持仓快照缺失时自动回退最近快照 _read_latest_positions()）
#   ├─ gen_positions_cfg_v13.py -> D:/QMT_POOL/p16_v13_risk/cmd/positions_cfg_v13_<date>.json
#   │    （只读 qmt_trade_log.csv FIFO 含费成本，不依赖 QMT 客户端存活）
#   └─ gen_positions_cfg_ens.py -> D:/QMT_POOL/p16_ensemble_bridge/cmd/positions_cfg_<date>.json
#        （融合桥，与 G2 同源逻辑：hold_dates ∩ 账户持仓快照，独立目录）
# 日志 data/schedules/gen_positions_cfg_premarket.log，任一失败 exit 1
python qmt_bridge_client.py write-cfg --from-fills     # [备选] 盘后手动补写当日成本锚
```

**桥心跳超时告警（2026-09-12 补注册 `Quant_P16_Heartbeat_Alarm`，DE 复查 T-20260912-002 回提——此前 T-20260903-022/023 声称已部署实际未注册）**：

```bash
# 定时任务 Quant_P16_Heartbeat_Alarm（每 30 分钟、09:00 起；脚本自过滤交易日 + 09:30-15:10 盘中窗口）跑：
powershell -NoProfile -ExecutionPolicy Bypass -File heartbeat_alarm.ps1
#   └─ check_bridge_heartbeat.py：巡检 G2/ENS/V13 三桥最新 heart_<date>.json，>600s 未刷新
#      → notify_feishu 飞书告警（lark-cli 子进程，LARK_CLI/FEISHU_OPEN_ID 在 g2_config.py）+ exit 2
# 日志 data/schedules/heartbeat_alarm_<ts>.log
# 注意：heartbeat_alarm.ps1 的 Python 选择优先 TRAE 内置 FALLBACK_PY 并做可执行测试，
#       规避 WindowsApps\python.exe 商店 stub（exit 9009）；含中文 .ps1 必须带 UTF-8 BOM。
# 盘中真实触发核对：09-14 周一开盘首验（超 600s 应出飞书告警 + exit 2）。
```

策略端兜底（BUILD 20260910-184724 起，G2/V1.3 双策略）：当日成本表缺失 → 自动回退最近历史表；account_id 不符 → 拒绝加载；peak.json 恢复「只升不降」（`pv > current` 才采纳，防 0.0 污染覆盖成本底）。

**融合(ensemble) 独立管道命令（2026-09-11 上线，账号 70180771 虚拟子账户，与 G2 同客户端双桥并跑）**：

```bash
python deploy_predict_g2.py --ensemble --ens-live   # 融合选股落盘 selections/g2_ens/<date>_g2_top10.csv（16:45 管道已并入，一次产出 G2+融合）
python rebalance_ens.py --date <YYYYMMDD> --live    # 融合换仓（dry-run 默认；跳过 G2 账本已持有的票=重叠规避）
python reconcile_ens.py --date <YYYYMMDD>           # 融合日终对账（独立目录 data/reconcile_g2_ens/）
python gen_positions_cfg_ens.py                     # 融合成本锚（盘前 ps1 第三腿）
python build_p16_ens.py                             # 融合桥构建 -> build/strategy_p16_ens_bridge.py（GBK+BUILD_TAG）
python push_fill_card.py --date <d> --source ens    # 融合成交回报卡（2026-09-10 新增 ens 分支）
```
调度：融合换仓 **09:52**（TW 任务 051bc2d9，后移 2 分钟避 G2 09:50 同刻下单）+ 融合日终对账 15:45（18c0c8d2）；资金池独立 `D:/QMT_POOL/p16_ensemble_bridge/ens_strategy_capital.json`（10万，已入资金分配表）。

**QMT 部署方式（重要真相，2026-09-10 实证）**：QMT 客户端对策略**加密存储**——直接复制文件到 `<QMT>\python\` 无效；必须经客户端策略编辑器**粘贴**（流程：build.py 构建+校验 → 记事本打开产物 → 人工粘贴 → 查日志 BUILD_TAG 核对版本）。QMT 为自绘 UI（无 UIA 控件、无子 HWND、窗口最小化时 rect=-32000），**粘贴不可自动化**（deploy_paste_qmt.ps1 留档失败方案）。09-11 融合桥即经此流程加载（日志 `[P16ENS][BUILD] BUILD_TAG=20260910-233536` 核对通过）。

**纸面前向三臂（A/B 裁判，2026-09-08 起）**：每日 20:05 `g2_candidates_night.ps1 --ab --ensemble` 记信号（G2=N15、v3_enh/融合=N10 三臂），16:45 `paper_forward_daily.ps1` 回填收益（唯一合法数据源 `merged_daily_full.parquet`）。**⚠️ 2026-09-10 用户拍板覆盖该裁判**：G2 直接升级 N15-live_trail、融合 0 笔到期即上线；纸面三臂继续累积作为旁证与复盘对照。

隔离硬约束：G2 只认 `g2_config.py` 与 `D:/QMT_POOL/g2_bridge/` 下的账本/资金池，**不 import V1.3 的 `qmt_config`、不读 `D_model_top10`、不调 `qmt_trader`**；融合只认 `g2_ens_config.py` 与 `D:/QMT_POOL/p16_ensemble_bridge/`（G2 侧不加反向重叠规避，属已知残余风险）；账号必须 `70180771`。部署后核对 QMT 主日志是否出现 `CTradeClient::order`。