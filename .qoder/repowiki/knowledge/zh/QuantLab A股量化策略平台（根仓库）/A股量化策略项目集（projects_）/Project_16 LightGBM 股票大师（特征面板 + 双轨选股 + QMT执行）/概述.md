基于 LightGBM 与 F1-F6 评分卡的双轨 A 股选股工程，覆盖从行情+财务特征面板构建、OPTUNA 寻优训练、部署预测到 QMT mini-自动交易及盯盘监控的完整链路。


## 最新进展（2026-08-29 更新）

- **V1.2 已上线（2026-08-28）**：`feature_panel_v3.parquet` 重建至 8/27（4,776,117 行 / 4306 只，新增 `refresh_panel_v3.py` 并固化到周一 retrain）+ 重训模型 promote（2837 树），消除「正式模型训练于 8/14 旧面板、生产喂 8/27 新面板」的分布不一致。模型-面板同步检查 `verify_model_panel_sync.py` 已接入调度（不一致 exit 1，fail-loud）。
- **g2 实时化（T-20260825-002）**：新增 `g2_realtime.py`，10 个新因子中 **9 个实现当日实时化**（F2 新浪资金流 / F5 同花顺 881 板块自算 / 龙虎榜东财 datacenter / 研报东财 reportapi）；**北向因 2024-08 起改为季频披露，客观上当日无法实时化**。修量纲 bug：评分卡 F2 阈值单位为元，原代码把快照 `mf_main_net`（万元）直接传入导致 F2 恒 2-6 分。
- **资金池硬校验**：下单前逐笔校验单笔 ≤ 资金池×95%÷N、累计 ≤ 资金池×95%，超限拒绝并 `POOL_BLOCK` 报警（fail-loud），杜绝 8/24 超买 939 万事故重演。
- **独立审计（T-20260828-006）**：11 项声称全部属实；发现 2×P1 已于 T-20260828-008 修复——①共享数据层 `data/astock_reader.py` 被未登记 diff 静默改造（已回退干净版 `f8b2d4b`）；②P16 账本无 `account_id` 戳（已按 T-20260823-004 红线补齐，逻辑集中在 `qmt_config.py`）。
- **daily 链双 Bug 修复（T-20260828-007）**：①`run_scheduled.ps1` 注入 `$env:PYTHONIOENCODING="utf-8"` 根治 GBK 编码崩溃假警报；②`deploy_predict.py` 新增 `_prefer_snapshot()`，增量快照交易日 > 主面板时改用快照（带股票域过滤 5826→4306，保持 training-serving 一致）。
- ⚠️ **核心观察**：策略真实口径（剔除超买标的 300684）已实现仅 **599 元**，而全账户 467,996 元的 **99.87% 来自 8/24 超买单票运气**——策略真实 alpha 仍无证据。**前向验证 N>30 是唯一裁判**（当前 9 个交易日，约需 2-3 个月）。
- **评分卡寻优（T-20260828-004）**：权重寻优与 F5/F6 阈值寻优 OOS 稳健有效（10/10），但 **F2 资金阶梯阈值寻优为负优化（OOS 0/10）→ F2 保持生产默认**。推荐组合**未落盘**，待前向验证后人工确认。

## 2026-09 更新：G2 大 QMT 文件桥上线

- **双轨并存**：V1.3 仍在 miniQMT（`67014907`）运行；**G2 走大 QMT 内置 Python 3.6.8（账号 `70180771`）**，两者配置、资金池、候选、账本全部隔离，G2 绝不纳管/卖出他人持仓。G2 已于 2026-09-02 正式上线（09:50 换仓 + 15:45 对账），旧链路保留 ≥1 月做灰度对照。
- **撤单链路打通（9/1 实证）**：官方撤单是 `cancel(orderId, acct, 'stock', C)` + `can_cancel_order` 预检，`orderId = m_strOrderSysID`；**`passorder` 的 opType 没有撤单值**（24 是股票卖出，此前一直拿它假装撤单，被当卖出拒）。订单号提取 `m_nOrderID` 优先、sysid 兜底；真实成交量字段是 `m_nVolumeTraded`（不是 `m_nDealVolume`）；状态码 **55（部成）是活跃态不是终态**。
- **两个 P0 修复**：①`_to_qmt_code` 把 `600522.SH` 翻转成 `SH.600522` → QMT 报 `orderCode 不合法`，passorder 返回 0 但委托从未进通道（验证方法：主日志出现 `CTradeClient::order`）；②心跳 `os.replace` 撞外部监控文件锁抛 PermissionError 导致策略崩溃、下午 handlebar 全停 → 改原子写 + 重试 5 次。
- **数据侧**：Tushare `moneyflow` 升为 F2 模型特征层权威主源（注意**单位是万元**），评分卡 9:45 保持当日口径；新增熔断路由与 01:00 夜间检修（`scripts/nightly_check.py`，A–G 全模块自检，面板落后自动重刷）。
- ⚠️ **判定未变**：策略真实 alpha 仍无证据，前向验证 N>30（按独立观测计）是唯一裁判，当前样本量不足，需持续观察 2–3 个月。

## 2026-09-11 更新：G2 升级 N15-live_trail + 融合(ensemble) 上线

- **G2 升级（用户拍板）**：`HOLD_DAYS` 10→15，对齐网格最优 G2-live_trail/N15/红线60/TOP2（+0.213%）；桥出场语义与回测 live_trail 一致（审计确认），桥无需重建。
- **融合(ensemble) 上线（2026-09-11 00:10 交易模式）**：v3_enh + G2 rank 融合 w=0.5 / live_trail / N10 / 红线60 / TOP2（+0.211%），独立管道全套（选股/换仓/对账/成本锚/桥/资金池 10万），与 G2 **同账号 70180771 同大QMT 客户端双桥并跑**，靠「纳管全账户但只评估自家成本锚」+「融合跳过 G2 账本重叠票」隔离。
- **调度**：融合换仓 09:52（避 G2 09:50）+ 融合日终对账 15:45；盘前成本锚 ps1 增第三腿；16:45 管道一次产出双选股。
- ⚠️ **拍板覆盖防过拟合红线**：融合纸面前向 0 笔即上线（原要求 30 笔裁判）；观察期 ≥5 交易日，日终对账盯 orphan/差额/同票双账本。
