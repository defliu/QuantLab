模块以「阶段化流水线」组织：`data_config.py` 提供集中式数据/模型路径和主仓+Updatedata增量合并的 `read_main_daily`，作为所有脚本的唯一入口；阶段0~2由 `build_features[_v2].py`、`train_baseline.py`、`train_optuna.py` 产出 `feature_panel_*.parquet` + `features_v*.json` 与 `versions/models/lgb_model_v*.txt`；阶段3通过 `deploy_predict.py` 加载候选模型做 LightGBM 概率 × 评分卡(权重 `SC_WEIGHTS`) 双轨综合，输出 `data/selections/` 候选清单；阶段3b 用 `review_full.py` 拉取 TDX 资金流/新闻/板块做实时复核。盘后调度由 PowerShell 脚本 `run_scheduled.ps1` 驱动，依次调用 `xtdata_update.py → merge_live_features.py → deploy_predict.py → reconcile_trades.py → verify_model_panel_sync.py`；周更重训走 `refresh_panel_v3.py → train_optuna.py --model-tag _retrain_*`，再用 `promote_model.py` 提升候选为正式模型。实盘执行层包含 `qmt_trader.py`（按选股下单）、`qmt_monitor.py`（止损止盈提醒 / 可选 auto-sell）、`qmt_clear.py`、`strategy_capital.py` 等，QMT 账号固定为 `67014907`（miniQMT），并通过 broker/qmt_builder 生成 GBK 单文件策略。研究侧在 `research/` 下运行独立实验（如 `trim_rule_backtest.py`），结果归档于 `versions/V*_candidate_xxx/`；全量中间产物集中在 `data/`（panel parquet、选号 CSV/MD、日志）与 `data/schedules/`（定时任务日志）。依赖方向单向：scripts → data_config → astock parquet/TDX/DuckDB → LightGBM → selections → QMT。

## 2026-09 架构增量：G2 大 QMT 文件桥（与 V1.3 完全隔离）

9 月起并行第二条执行链路 **G2**，把交易执行从 miniQMT 外部进程搬进 **大 QMT 内置 Python 3.6.8**，账号从 `67014907` 切到 `70180771`：

- **文件桥**：信号层（外部 Python 3.10）与执行层（QMT 内置 Python 3.6）通过 `D:/QMT_POOL/g2_bridge/{cmd,state}` 目录交换 —— 外部 `qmt_bridge_client.py` 写 cmd / 读 state；内置源码 `strategy/strategy_p16_g2_bridge_src.py`（→ 构建产物 `build/strategy_p16_g2_bridge.py`，GBK + BUILD_TAG）消费。带 **seq 幂等**、**pending 状态机（300s / 最多 3 次重报）**、心跳快照恢复。
- **风控迁入内置**：`_check_risk_signals` 实现止损 / 止盈 / 追盈三规则，触发即 `passorder` 卖出并走 pending，fills 的 `reason` 透传 `SELL_STOP` / `SELL_TAKE_PROFIT` / `SELL_TRAILING` 供外部对账；成本锚来自外部每日写的 `cmd/positions_cfg_<date>.json`；追盈峰值 `state/peak.json` 跨日持久化；`risk_sold` 防重复；**孤儿持仓只纳管不误杀**。
- **G2 独立资产**：`g2_config.py`（绝不 import V1.3 的 `qmt_config`）、独立资金池 `D:/QMT_POOL/g2_bridge/g2_strategy_capital.json`、`rebalance_g2.py`（先卖后买，只认 G2 账本，T+1 自动跳过，dry-run 默认）、`reconcile_g2.py`（fills / pending / 持仓差额三路对账）、`G2_RUNBOOK.md`。候选用 `g2_top2` 而非 V1.3 的 `D_model_top10`，G2 绝不调 `qmt_trader`。
- **调度**：09:15 候选预生成 → 09:25 盘前预判 → 09:45 复核（V1.3）→ 09:50 G2 换仓 → 11:35 / 15:40 持仓卡 → 15:45 G2 对账 → 19:30 Tushare 刷新 → 01:00 夜间检修。候选预生成与实时复核**解耦**：候选离线定（combo 全离线），候选内排名由 9:45 实时复核 F2/F5（30%）重排。
- **数据源 v8**：基础行情锚定腾讯；扩展指标 `mcp_tdx → iFind → 东财 mx → 东财 curl`；悟道走**直连客户端** `scripts/wudao_client.py`（真实端点 `https://stock.quicktiny.cn/api/mcp`，此前文档里的 `/api/openclaw` 实测 404）；F2 模型特征层以 **Tushare `moneyflow` 为权威主源（T-1）**，评分卡 9:45 仍坚持当日口径（东财 / 悟道）；新增**熔断路由** `scripts/data_source_router.py`（连续失败 3 次熔断 300s，状态 `data/cache/datasource_health.json`）；9:25 双源对拍写 `crosscheck_<date>.json` 打 `[交叉验证]` 预警。
- **飞书推送**：`push_review_card` / `push_fill_card` / `push_holdings_card` / `push_past_review_card` / `push_alert_card` / `push_daily_summary_card`，数据不足显示「—」绝不编造。
- **前向验证**：`paper_forward.py` 曾因只依赖人工周更的 `Updatedata/` 而连续 4 天「回填 0 笔」仍 exit 0 静默；已改为 `load_open_panel()`（主库 + 增量合并，增量优先）+ 数据源停更 exit 2 + `forward_stats.py` 按同窗口全市场等权算超额。有效样本按 `n_eff = n / (1 + (m-1)ρ)` 计（G2 实测组内相关 ρ≈0.68）。

## 2026-09-10 架构增量：成本锚双层防御 + 三层门禁体系

- **成本锚时序缺陷与双层修复（T-20260908-002）**：原缺陷 = 成本表收盘后才生成 / 断档 → 桥 init 读不到当日表 → 风控全天静默（peak 全 0、止损止盈失效）。修复分两层：①**调度层根治**——`gen_positions_cfg_premarket.ps1` 由 schtasks `Quant_P16_CfgPremarket_0900` 工作日 09:00 触发，串跑 G2+V13 两生成器（G2 侧带持仓快照回退，V13 侧纯读 qmt_trade_log.csv FIFO 不依赖客户端存活）；②**策略端兜底**——当日表缺失自动回退最近历史表、account_id 不符拒绝加载、peak.json 恢复「只升不降」（防 0.0 污染把成本底覆盖回 0，2026-09-10 实测撞出的第三缺陷）。两层缺一不可。
- **三层门禁体系（T-20260910-002/003，防「周训越训越差」）**：
  1. **模型层**：`train_g2 --promote` G1-G4 strict（同窗口 IC / 尾部 IC / Top2 可执行模拟 / 观测期回滚）+ V1.3 `auto_promote` G0-G6——只保证预测质量（IC）不退步；
  2. **策略层**：`gate_strategy_layer.py` + 候选配置库 `data/strategy_cfg_lib.json`——在历史最优配置下直接回测比钱（daily_excess 容差 0.02pp），FAIL 自动回滚模型指针（G2 09-07 坏模型 +0.039% vs live +0.213% 被实测拦截）；
  3. **最终裁判**：纸面前向三臂（G2=N15、v3_enh/融合=N10）约 30 笔累积对比。
  - 核心认知：**训练目标（IC/分位单调）≠ 实盘目标（胜率/盈亏比/回撤）**——位置过滤/出场规则/融合/参数寻优是「如何使用模型输出」的决策层，LightGBM 重训天然找不到这些增益，必须走离线网格寻优 + 前向验证。
- **live_trail 追盈出场（2026-09-07 起，引擎与桥四处同步）**：激活 +8% 后回撤 8% 出场、保本底线 = 成本价；T+1 卫生——买入日不评估追盈、不把当日 high 并入峰值（用「截至昨日」峰值判今日）。回测引擎 `scan_rotate_cost_real.py` 与桥内实现口径必须逐条对齐（train-serving skew 检查）。
- **网格寻优结论（T-20260909-009，官方口径滑点 0.1%/open→open/测试期 2024-07~2026-08）**：G2 最优 = live_trail/N15/红线60/TOP2（+0.213%/日）；融合 = live_trail/N10/红线60/TOP2（+0.211%）；V1.4 = fixed/N10/红线58/TOP2+lite（+0.157%）。TOP1 全线弃用（防过拟合裁决）。**根本局限：测试期 100% 在模型训练窗内（双重使用），数字系统性偏乐观，实盘升级以纸面前向为唯一裁判，裁判标准看「样本外超额>0」**（审计报告 `results/策略优化审计_20260910.md`，三配置+分段数字已独立复算精确命中）。
- **touch_tp 触价止盈模式（T-20260911-001，研究开关，默认关闭）**：`scan_rotate_cost_real.py` 引擎新增第四种出场模式 = -7% 开盘止损 + 盘中高点≥成本×(1+TP) 即卖（跳空高开按开盘成交，T+1 锁 T+2 起可卖）。仅研究用途，官方口径零影响已由 4/4 基线锚点精确复现验证；两步实证结论=证伪（N=3/5 全部失败、N=10 版 +0.104% 不及 live_trail 基线 +0.132%，配对 t 不显著），详见 `results/TP5短持有期轮动回测_20260911.md`。「换票/出场规则不产生 alpha」清单再 +1（此前：PK_OUT/减仓/持有期软化/止盈线上移/追盈优先）。

## 2026-09-11 架构增量：G2 升级 N15-live_trail + 融合(ensemble) 上线 + 同账户双桥隔离

- **G2 升级（用户拍板 2026-09-10）**：`g2_config.HOLD_DAYS` 10→15，对齐网格最优 G2-live_trail/N15/红线60/TOP2（+0.213%）。桥出场逻辑（STOP→TP→TRAIL + 激活8%+保本底线）与回测 live_trail（TRAIL_FIRST=0）语义已由审计确认一致，**桥无需重建**（仍 184724），仅 rebalance 持有期变 15 日。
- **融合(ensemble) 独立管道（2026-09-10 全量搭建，09-11 00:10 交易模式上线）**：选股 = `deploy_predict_g2 --ensemble --ens-live`（v3_enh + G2 模型 rank 融合 w=0.5，评分卡红线60，候选 top10 by ens → 买入 top2 by total_new），对齐网格候选 融合-live_trail/N10/红线60/TOP2（+0.211%）。**全套独立资产与 G2 一一对应且完全隔离**：
  - 配置 `g2_ens_config.py`（N10/红线60/TOP2/10万）、桥客户端 `qmt_bridge_client_ens.py`、换仓 `rebalance_ens.py`、对账 `reconcile_ens.py`、成本锚 `gen_positions_cfg_ens.py`、构建 `build_p16_ens.py`、桥源码 `strategy/strategy_p16_ens_bridge_src.py` → 产物 `build/strategy_p16_ens_bridge.py`（BUILD 20260910-233536）
  - 独立目录 `D:/QMT_POOL/p16_ensemble_bridge/{cmd,state}` + 独立资金池 `ens_strategy_capital.json`（10万）+ 独立账本 `g2_ens_hold_dates.json`
- **同账户双桥隔离机制（关键设计）**：G2 与融合**同一券商账号 70180771、同一大QMT 客户端并跑**。隔离靠「**纳管全账户但只评估自家成本锚**」——两桥都 `get_trade_detail_data` 纳管全部持仓，但 `_check_risk_signals` 只评估 `cost>0` 的票；对方策略的票在自己成本表里 cost=0 → 跳过。另有**股票重叠规避**：融合换仓跳过 `g2_hold_dates.json` 已持有的票（G2 侧未加反向互斥，属已知残余风险，观察期盯同票双账本）。账本/资金池/成本锚/峰值全部分目录隔离，无文件共享。
- **调度**：盘前成本锚 ps1 增第三腿 `gen_positions_cfg_ens.py`（与 G2/V13 串行）；融合换仓 **09:52**（TW 任务 051bc2d9，09-10 由 09:50 后移 2 分钟避 G2 同刻下单）+ 融合日终对账 15:45（18c0c8d2）；16:45 `paper_forward_daily.ps1` 的 deploy 调用加 `--ensemble --ens-live` 一次产出 G2+融合双选股。
- **⚠️ 拍板覆盖防过拟合红线**：融合纸面前向 0 笔到期即上线（T-20260910-103 原要求等 30 笔裁判），看板 T-20260910-109 如实标注；观察期 ≥5 交易日，日终对账盯 orphan/持仓差额/同票双账本。

## 2026-09-12 架构修复：train_g2 enh 慢变量逐行 asof（T-20260910-106）

- **漂移根因（09-07 周更越训越差 → 09-09 回滚 08-25 的根因）**：`train_g2.py` 曾把 6 个 enh 慢变量（dv_year_sum/ex_days_since/ex_yoy/fc_pchange/industry_mom20/turnover_rank）按「每股最新值」广播到全部历史行——08-25 原版训练（overnight_opt 直接读 enh 面板=逐行 as-of）与实盘推理（build_g2_daily asof）均为逐行口径，唯独周更重训脚本偏了（训练吃未来值 + train-serving 分布不一致）。
- **修复**：`pd.merge_asof(on="trade_date", by="ts_code", direction="backward")` 逐行取值；附带修复 `--limit` 冒烟隐性 bug（head(N) 全落 2019 年训练窗 → 改跨期随机抽样）。
- **验证**：面板级 2 万行抽样 0 不一致、2021 年 100% 行值改变（漂移面实证）；**全量重训 20260912_1905t 同窗 strict 门禁全 PASS**（IC 0.1503 vs live 0.1273、尾部IC 0.1730 vs 0.1711、TOP2 +0.0174 vs +0.0117）——学习恢复。live 指针不动，09-14 周更经双门禁链（`train_g2 --promote` strict + `gate_strategy_layer` 复核 FAIL 自动回滚）自然生效。
- **遗留（T-20260912-004）**：`feature_panel_v3_enh.parquet` 全库无 writer（08-25 一次性构建，冻结 2026-08-14），`refresh_panel_v3.py` 只刷 27 特征基础面板；训练/实盘共用同文件故口径一致，但 6 个慢变量绝对新鲜度滞后近月，需补刷新链路。**→ 已于同日落地，见下节。**

## 2026-09-12 架构增量：enh 面板刷新链路（refresh_panel_enh.py）+ NaT 溢出修复（T-20260912-004）

- **enh 面板 writer**：`refresh_panel_enh.py` 为 `feature_panel_v3_enh.parquet` 唯一 writer，**全量重建**（非增量追加）——v2 面板切片（27+4 事件特征）+ turnover_rank（主库区原生 turnover rank pct / 增量库仅 OHLCV，用 vol÷float_share 反推）+ industry_mom20（近似口径：stock_basic.industry 当前映射 + 全市场未复权 close 20 日动量按行业等权，原 08-23 vintage 不可恢复，corr 0.983~0.9966）。选择全量重建的架构理由：单 writer、自愈（stock_basic 更新全期一致重聚合）、与 v2/v3 面板周重建机制同构。
- **校验门禁（fail-loud）**：v2 新鲜度≤7 天、NaN 率≤2%、days_since 负值回归=0、与旧面板重叠期末 3 日 5 特征复现≥99%、industry_mom20 corr≥0.90、行数≥90%；任一失败保留旧面板 exit 1。meta 写 `features_v3_enh.json`（refreshed_at + 口径注记）。
- **NaT 溢出根因修复（`build_features_v2.py` 两处）**：`NaT.astype('timedelta64[D]').astype('float64')` = -9.2e18 而**非 NaN**（numpy 语义坑），`np.isnan` 拦不住 → int32 溢出 -2147483648；修复 = `td[np.isnat(td_raw)] = np.nan` 再转换。污染面为 merge_asof 无匹配行（首事件晚于面板日），面板诞生即有但训练/推理同源一致（不构成 skew），尾部干净 → live 影响≈0。
- **挂链位置**：daily 16:45 链 `refresh_panel_v3 → refresh_panel_enh → deploy_predict`（失败告警不阻塞 V1.3——V1.3 读 `_v3` 基础面板不消费 enh）；retrain 周一 17:00 链 `refresh_panel_v3 → refresh_panel_enh → train_optuna → auto_promote → train_g2 --promote → gate_strategy_layer`（失败告警不跳过 G2，旧面板近日刷新可接受）。
- **数据天花板（链路固有约束）**：astock finance forecast/express ann_date 至 2026-07-29、dividend/fina_indicator/share_change 至 08-21、ths_daily 至 08-21；事件特征靠 asof 语义自然衰减（无新事件=旧值持续），industry_mom20 用主库+增量 close 无天花板。