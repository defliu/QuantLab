- 所有脚本以 `import data_config as DC` 取得 PROJECT_DIR、DATA_DIR、MODEL_DIR、UNIVERSE 等路径，禁止硬编码磁盘路径。
- 特征面板采用 versioned 命名约定：`feature_panel_vN.parquet` 对应 `features_vN.json`、模型文件 `lgb_model_vN.txt`、面板绑定写入 `data/model_panel_binding.json`。
- 每个版本的数据面板与参数记录保存为 JSON/Markdown 报告到 `data/`（如 `optuna_report.json`、`selections/...md`、`holdings_report_*.md`），便于回溯。
- 定时任务模式按 `run_scheduled.ps1` 中的 Mode 切分，日志统一落盘 `data/schedules/<mode>_<时间戳>.log`。
- 实盘/QMT 相关常量（ACCOUNT_ID=67014907、MODEL_DIR、路径）集中在 `qmt_config.py`，与 `data_config.py` 分工清晰。
- 回测与生产严格区分：研究脚本位于项目根或 `research/`，QMT 构建产物（GBK 单文件）必须带 BUILD_TAG 且不得混入 MOCK 代码。