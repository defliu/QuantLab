运行单测：`python risk/test_overlay.py`。在回测 config 中启用：`crash_guard=1 / crash_lookback=5 / crash_threshold=-0.07 / crash_cooldown=5`，`regime_gate=1 / regime_lookback=60 / regime_vol_percentile_threshold=0.50`。
