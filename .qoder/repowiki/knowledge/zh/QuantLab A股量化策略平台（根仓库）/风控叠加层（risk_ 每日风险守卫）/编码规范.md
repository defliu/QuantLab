- 模块内全部数值参数从 config 读取且带默认值，禁止在代码里硬编码阈值。
- 所有守卫必须 `enabled` 开关控制，未启用时 `update()` 立即返回 False，保证对既有回测逐笔一致（零影响是硬约束）。
- 状态保存在实例内部 deque，**必须在 run_backtest 内单实例贯穿整段回测**，不可每日新建（否则状态丢失、守卫失效）。
- 空仓覆写必须走框架既有空 target_weights 路径，不得自行拼卖出记录。
- fail-open 优先于 fail-close：数据不足时默认允许持仓，宁可漏防不可误杀。
