- 每个策略一份独立 yaml 文件，文件名以策略语义命名（如 atr_lowvol_fw、value_smallcap_v2_config），backtest.strategy 字段给出框架注册的策略名而非类名。
- 共享资源（账户总资产、基准指数、数据源路径、股票池）集中于顶层 config 文件，策略 yaml 仅声明本项目覆盖的子域（如 strategy_params、execution slippage/commission）。
- 股票池统一通过 `universe.csv` 引用绝对路径 `D:/QuantLab/config/full_a_sh_sz.csv`，新增股票只需在该 CSV 启用 enabled，无需改动策略配置。
- 资金分配清单放在 `capital_allocation.yaml` 的 strategies 列表中，新增策略遵循 key/name/capital_base/max_hold/config_file 字段顺序并追加注释说明来源与负责人。
- 注释采用中文 + 变更溯源格式（如“2026-08-14 由100只改8只，诚哥拍板”），使配置具备审计轨迹。