- **三件套缺一不可**：特征面板 + 正式模型 + 绑定记录必须一致，见 `PANEL_SYNC_部署配置说明.md`；面板重刷后必须同步重训或明确标注绑定关系，禁止「新面板 + 旧模型」上线。
- 正式模型只读：任何覆盖写必须先备份旧文件，promote 流程不得跳过备份。
- 实验候选一律新命名落盘（带版本/日期后缀），**禁止直接覆盖正式模型**。
- 模型与 Project_04 体系隔离：Project_04 自包含脚本不引用本目录。
