运行 `python broker/qmt_builder.py` 从 `config/settings.yaml` 读取 Project 01 配置并输出 GBK 策略到 `build/strategy_mfic.py`；本地验证通过各项目 `local_validate.py` 导入 `broker.local_context.LocalContext` 并用 miniQMT 虚拟环境执行，以断言选股管线。