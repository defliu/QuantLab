- 每个脚本通过 `sys.path.insert(0, r"E:\QuantLab\...")` 直接挂载共享代码，再以 `from research.multi_factor_ic.config/DATA_LOADER import ...` 取用数据接口，保持脚本自包含且可跨仓库复用。
- 财务数据统一经 `fin_snapshot(date)` 以 `ann_date <= date` 截取、缓存 per-date dict 的方式获取 PIT 安全截面快照，禁止直接 read_parquet 后无过滤使用。
- 候选池构造采用固定模板：市值 <300000（万元）+ PE>0 + PB>0 + ST/停牌过滤 + bps/profit_dedt/roe>0 质量门，再据此筛出 top 80 目标持仓。
- 回测交易模拟使用状态机（延续/顺延）与涨跌停受限买卖（limit_pct 按板块返回 0.10/0.20/0.30），并通过 `run_v2`/`run_independent` 两类 run 函数横向对比不同假设。
- 输出遵循一致约定：控制台日志经由本地 `log(_log)` 函数收集，关键结论追加到同级 `*_结果.txt` 或 `.md` 报告文件，便于版本化归档。
- 新增因子只需在 `build_factor_defs()` 中追加一个接受 `(d, dd, cand, ctx)` 的 callable 并返回 Series，即可被健康监控自动纳入分阶段 IC 评估。