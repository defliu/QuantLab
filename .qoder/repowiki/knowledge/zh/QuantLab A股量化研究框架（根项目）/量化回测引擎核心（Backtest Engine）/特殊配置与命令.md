默认基准数据库路径 `DEFAULT_BENCHMARK_DB = "F:/backtest_workspace/data/duckdb/benchmark_index.duckdb"`，需预先存在；结果目录默认 `RESULTS_DIR = "E:/QuantLab/reports"`，可通过 `report.set_results_dir(path)` 覆盖；策略模块位于 `strategy/<name>.py`，注册名即扁平文件名（如 `atr_lowvol`），带 category 前缀时自动取最后一段。