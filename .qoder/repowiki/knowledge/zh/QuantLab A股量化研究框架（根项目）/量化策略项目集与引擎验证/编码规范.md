- 每个策略项目以 `run_*.py` 为单一入口，内部完成数据加载→面板构建→信号计算→持仓管理→净值记录→指标计算→CSV/HTML输出的完整流水线。
- 回测数据统一从 `E:/astock/daily/stock_daily.parquet` 读取，使用 (trade_date, ts_code) 的MultiIndex面板，并过滤 ST 和停牌股票（`is_st` 与 `suspend_type ∈ {S,R,R&S}`）。
- 交易成本模型一致：佣金万三+印花税千一+滑点千一，买入时扣除佣金与滑点，卖出时额外扣除印花税。
- 结果输出固定写入各项目 `results/` 或 `reports/` 目录下的 `equity.csv`、`trades.csv` 等标准文件名，便于后续汇总分析。
- 验证脚本采用函数级命名（如 `test_1_known_result`、`test_2_transaction_cost`），每个函数返回包含 `test`、`description`、`result`、`pass` 字段的字典，统一打印 PASS/FAIL 状态。