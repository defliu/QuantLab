- 每个脚本以 `# coding=utf-8` 开头，顶部 docstring 说明审计编号、目标与输出文件名，并使用统一的 `OUT = r"E:\QuantLab\research_audit\auditXX_结果.txt"` 与 `_log` 列表收集日志后一次性写入。
- 数据加载采用固定模式：读 parquet → 提取 codes_all → 过滤 ST/停牌 → 构造 panel DataFrame（含 open/high/low/close/pe_ttm/pb/circ_mv/amount/prev_close）→ 过滤 ST 与 suspend_type ∈ {S,R,R&S}。
- 所有可复用函数（fin_snapshot、bp_hist_pct、get_candidates、score_v2、limit_pct、run_v2）均以模块级 `_xxx_cache` 字典实现按日期键的惰性缓存，首次计算后返回缓存值。
- 涨跌停处理通过 `limit_pct(code)` 按股票代码前缀区分：688/30 为 20%，8/4/92 为 30%，其余 10%，并在卖出侧用 `xo <= xpc*(1-limit)+0.005` 且 `xo <= xl*1.001` 判定跌停无法成交。
- 回测收益标记日期统一使用结算日 `e_date`（而非 x_date），通过 `pd.Timestamp(e_date)` 写入 rows 的 date 字段，确保净值法窗口对齐。
- 基准构建始终采用「同池等权」：对每期候选池取 e_open/x_open 均有效的股票等权计算区间收益，要求至少 30 只才记录一期。