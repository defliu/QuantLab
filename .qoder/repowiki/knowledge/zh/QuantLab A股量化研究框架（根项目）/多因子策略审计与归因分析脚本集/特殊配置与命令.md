每个脚本需先配置 `sys.path.insert(0, r"E:\QuantLab")` 和 `projects\Project_01_多因子IC小盘Alpha` 路径以导入 `research.multi_factor_ic.config` 与 `data_loader`；运行后输出日志同时打印到 stdout 并写入同目录 `auditXX_结果.txt`；外部数据文件路径硬编码为 `E:/astock/basic/stock_basic.parquet` 与 `E:/astock/finance/fina_indicator.parquet`。