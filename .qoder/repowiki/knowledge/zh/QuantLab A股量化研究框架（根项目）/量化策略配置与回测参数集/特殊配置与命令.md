运行回测前需确保本地路径 `E:/astock/daily/stock_daily.parquet`、`F:/backtest_workspace/data/duckdb/benchmark_index.duckdb` 存在且可访问；实盘模式需在 `settings.yaml` 中将 `trading.enabled` 改为 true 并填写 `trading_config.yaml` 中的 QMT 路径与 webhook 地址。