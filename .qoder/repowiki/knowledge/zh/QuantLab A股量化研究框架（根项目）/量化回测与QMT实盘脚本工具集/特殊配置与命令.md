所有脚本依赖固定绝对路径：`E:/astock/daily/stock_daily.parquet`（行情）、`D:/QMT_POOL/mfic_fin_data.csv`（财务）、`E:/量化/行情数据更新池`（增量源）。通过 `python scripts/update_data.py` 或 `update_data.bat` 更新数据；通过 `python -m scripts.run_backtest --config config/xxx.yaml` 运行回测；通过 `run_batch_ic_test.bat` 执行全量因子IC测试。QMT 策略需粘贴到 miniQMT 策略编辑器中运行。