- 每个脚本以 `if __name__ == "__main__": sys.exit(main())` 或 `main()` 直接调用的方式暴露命令行入口，参数统一用 argparse 或硬编码常量管理。
- QMT 策略脚本严格实现 `init(C)` 和 `handlebar(C)` 两个回调函数，账户ID通过全局 `ACCOUNT_ID` 常量定义，日志通过 `_log()` 同时打印并追加到 `LOG_FILE`。
- 数据读取统一先 `pd.read_parquet` 再检测 MultiIndex 并 `reset_index()`，日期列统一用 `pd.to_datetime(...).dt.date` 标准化。
- Windows 批处理脚本固定 `cd /d E:\QuantLab` 后调用 `python scripts\*.py`，并在末尾 `pause` 保持窗口。
- 脚本内通过 `sys.path.insert(0, PROJECT_ROOT)` 将仓库根目录加入 import 路径，从而直接引用 `backtest.*` 和 `data.*` 子包。