- 每个回测运行生成独立子目录，目录名采用 `时间戳_随机哈希_策略名` 的命名约定，确保结果可追溯且互不覆盖。
- `summary.json` 始终包含 `summary_schema_version` 字段，用于向后兼容不同版本的摘要结构。
- `trades.csv` 每行统一携带 `run_id` 作为第一列，使多策略结果可按 run_id 区分与合并分析。
- `report.md` 固定包含 Run 元信息、业绩指标、期末持仓、关键日志摘录、数据元信息与复现命令五个章节，便于快速审阅。