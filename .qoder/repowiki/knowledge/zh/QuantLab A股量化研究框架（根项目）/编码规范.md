- 所有策略与因子通过装饰器+自动发现机制注册，由 engine/factor_engine 统一调度而非硬编码 import
- 配置集中存放于 config/*.yaml，通过 yaml.safe_load 读取，避免散落的常量定义
- 每个 strategy/project 自包含 data_loader/backtest 入口，保持回测可独立运行
- 输出统一写入 reports_output/ 目录，包含 Markdown 报告、JSON 摘要与 CSV 明细