# QuantLab 实盘配置指南

## 账号信息
- 账号: 67014907
- QMT路径: E:\国金QMT交易端模拟
- 可执行文件: E:\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe

---

## 配置步骤

### 步骤1：安装xtquant库

**方案A：复制到系统Python（推荐）**

```powershell
# 以管理员身份运行PowerShell
$src = "E:\国金QMT交易端模拟\bin.x64\Lib\site-packages\xtquant"
$dst = "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\Lib\site-packages\xtquant"

# 检查源路径
if (Test-Path $src) {
    # 复制
    Copy-Item -Path $src -Destination $dst -Recurse -Force
    Write-Host "✅ xtquant已复制到: $dst" -ForegroundColor Green
    
    # 验证
    python -c "from xtquant import xttrader; print('✅ xtquant安装成功')"
} else {
    Write-Host "❌ 源路径不存在: $src" -ForegroundColor Red
    Write-Host "   请检查QMT是否完整安装"
}
```

**方案B：使用QMT内置Python（兼容性最好）**

修改 `start_trading.bat`：
```batch
:: 使用QMT内置Python 3.6.8
set PYTHON=E:\国金QMT交易端模拟\bin.x64\python3\python.exe
%PYTHON% live_trading.py
```

---

### 步骤2：启动QMT客户端

**手动启动：**
1. 双击 `E:\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe`
2. 登录账号 **67014907**
3. 等待完全启动（看到行情界面）

**命令行启动：**
```powershell
Start-Process "E:\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe"
```

---

### 步骤3：测试连接

```powershell
cd E:\QuantLab
python test_connection.py
```

**预期输出：**
```
✅ xtquant库已安装
✅ QMT路径存在
✅ 连接成功
✅ 账户查询成功
   总资产: ¥XXX,XXX.XX
   可用资金: ¥XXX,XXX.XX
   持仓市值: ¥XXX,XXX.XX
✅ 持仓查询成功
   持仓数量: X只
```

---

### 步骤4：配置通知（可选）

编辑 `config/trading_config.yaml`：

```yaml
notification:
  enabled: true
  method: "wechat"  # 或 "dingtalk"
  webhook_url: "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY"
```

**企业微信Webhook获取：**
1. 打开企业微信群聊
2. 群设置 → 群机器人 → 添加机器人
3. 复制Webhook地址

**钉钉Webhook获取：**
1. 打开钉钉群聊
2. 群设置 → 智能群助手 → 添加机器人 → 自定义
3. 复制Webhook地址

---

### 步骤5：启动实盘

**方式1：批处理脚本**
```powershell
cd E:\QuantLab
.\start_trading.bat
```

**方式2：命令行**
```powershell
cd E:\QuantLab
python live_trading.py
```

**方式3：后台运行（推荐生产环境）**
```powershell
# 使用pythonw避免弹出控制台窗口
Start-Process pythonw -ArgumentList "live_trading.py" -WindowStyle Hidden
```

---

## 执行流程

```
09:15  盘前准备
  ├── 检查QMT连接
  ├── 下载最新数据
  ├── 查询账户状态
  └── 撤销昨日未完成委托

09:35  策略执行
  ├── 计算因子 → 生成目标组合
  ├── 对比当前持仓 → 生成交易指令
  ├── 先卖后买
  └── 追踪执行进度

每30分钟  风控检查
  ├── 个股止损（-8%）
  └── 组合回撤检查

14:50  尾盘处理
  ├── 持仓对账
  ├── 保存状态
  └── 发送日报
```

---

## 常见问题

### Q1: xtquant导入失败

**原因：** xtquant未安装或路径不对

**解决：**
```powershell
# 检查QMT的xtquant路径
$paths = @(
    "E:\国金QMT交易端模拟\bin.x64\Lib\site-packages\xtquant",
    "E:\国金QMT交易端模拟\Lib\site-packages\xtquant",
    "D:\miniQMT\userdata_mini\Lib\site-packages\xtquant"
)

foreach ($p in $paths) {
    if (Test-Path $p) {
        Write-Host "✅ 找到: $p" -ForegroundColor Green
    }
}
```

### Q2: 连接失败（错误码非0）

**可能原因：**
1. QMT未启动 → 启动QMT客户端
2. 账号未登录 → 在QMT中登录
3. 路径错误 → 检查 `config/trading_config.yaml` 中的 `account.path`

### Q3: 查询账户返回0

**原因：** 账号ID不正确

**解决：**
1. 在QMT中查看账号（右上角）
2. 修改 `config/trading_config.yaml` 中的 `account.id`

### Q4: 程序崩溃后如何恢复

**QuantLab自动处理：**
1. 状态持久化到 `data/broker_state.json`
2. 重启后自动加载上次状态
3. 对账持仓，发现差异会告警

### Q5: 如何停止程序

**优雅退出：**
- 控制台：`Ctrl+C`
- 后台进程：任务管理器结束 `pythonw.exe`

**退出时会自动：**
1. 撤销所有未完成委托
2. 保存状态
3. 断开连接

---

## 安全建议

1. **先模拟盘验证**
   - 使用QMT模拟端测试至少1周
   - 确认所有功能正常后再切换实盘

2. **设置风控阈值**
   - 个股止损：-8%（已配置）
   - 组合回撤：-15%（已配置）
   - 单日换手：30%（已配置）

3. **监控告警**
   - 配置企业微信/钉钉通知
   - 异常时第一时间收到通知

4. **定期对账**
   - 每日盘后检查持仓对账
   - 发现差异立即处理

5. **备份配置**
   - 定期备份 `config/` 和 `data/`
   - 记录每次参数调整

---

## 下一步

配置完成后：

1. **测试连接**
   ```powershell
   python test_connection.py
   ```

2. **接入策略**
   - 将QMT_STRATEGIES的6+2评分器迁移到 `factors/`
   - 或使用QuantLab内置的多因子策略

3. **启动实盘**
   ```powershell
   .\start_trading.bat
   ```

4. **监控运行**
   - 查看日志：`logs/trading_YYYYMMDD.log`
   - 查看持仓：`data/positions.json`
   - 查看净值：`data/equity_curve.csv`
