# 2026-08-05 工作日志

## 工作区主战场确认
- 用户明确指示：**以后以 `D:\QuantLab`（QuantLab 量化框架枢纽）作为主工作空间**。
- 已同步更新跨项目长期记忆（`~/.workbuddy/MEMORY.md` 第六节"项目地图"）：
  - 把 QuantLab 标为 🔴 主工作区；
  - 保留 `D:\QMT_STRATEGIES` 为 ATR 专项开发/国金部署仓，二者并行不默认合并。
- 含义：涉及"新建任务 / 框架 / 多因子 / QMT实盘工程"默认从 QuantLab 起手；只有 ATR 策略源码与国金部署才回 QMT_STRATEGIES。

## 待办/观察
- QuantLab 尚有 ~990 万模拟资金闲置（ATR 仅占 10 万）→ 后续多策略分配或上调 ATR 本金是潜在议题。

## 因子健康监控体系上线
- 决策：backtrader 2020 停更、A 股规则需手搓，本地 `backtest/` 引擎更优，不引入 backtrader。
- 选定研究方向：**因子健康监控体系**（替代只出全期聚合 IC 的既有工具）。
- 交付 `research_audit/factor_health.py`：调仓期 Spearman IC 时序 + 分段（全期/2024+/2026/近8期）+ 衰减告警（FAILED/DEGRADED/WATCH/HEALTHY），PIT 安全，口径与 audit14 结算一致，REBAL_FREQ 支持 M/2M。
- 首期实跑（~413-457s，月频 100 期）结论：BP行业中性z **HEALTHY**（近8期 IC +0.130 走强）；BP历史分位 **WATCH**；ROE、波动率60d **FAILED**；动量1m、VWAP量价 **WATCH**。印证 Project_01 审计结论，ROE/动量/波动仅宜作排雷/门控。与 audit14/16 交叉验证一致。
- 调度：`scripts/run_factor_health.bat`（钉死 hermes venv python）+ 计划任务 **`QuantLab_FactorHealth_Monitor`** 每周五 19:30（已注册，下次 2026-08-07 19:30）。

## 云端仓库落档（defliu/QuantLab）
- 处置：旧仓 `defliu/QMT`（旧实盘工程镜像）留存归档，不参与本仓开发；**新仓 `git@github.com:defliu/QuantLab.git`** 承载本仓库（SSH，账号 defliu）。
- 提交 3 笔并已推送（master 与 origin/master 一致，`git status` 干净）：
  - `985b34d` feat(monitor) 因子健康监控体系 + 首期报告 + 调度
  - `d62951c` docs(roadmap) 云端仓库信息记录
  - `e0086f3` chore(repo) 全量研究资产入仓（649 文件：backtest/factors/strategy/broker/projects/config/知识库/旧史料）
- **经验**：GitHub 不支持 push 自动建仓，必须先在网页创建空仓；本机仅 SSH（id_ed25519），无 gh/PAT。
- **坑**：`archive/` 下 286MB `panel.parquet` + 73MB `scored.parquet` 超 GitHub 100MB 单文件限制 → `.gitignore` 加 `*.parquet` 排除（留在本地未推，远程留档需 Git LFS 或压缩）。
