# MIMO 通宵作战任务书：v2.1 → v3.0 收益增强冲刺（最终版）
> **签发时间**：2026-07-19 00:10
> **截止时间**：2026-07-19 08:00（共8小时）
> **执行人**：MIMO
> **基线状态**：v2已确认，年化1.8%，最大回撤-24.1%，夏普0.07

---

## 🚨 启动前必须读的8个关键事实
这些是HERMES实测确认的，**不要修改，不要怀疑**：

| # | 关键事实 | 验证方式 |
|---|---------|---------|
| 1 | **单组回测真实耗时 = 70秒**（不是3分钟） | 实测3次，69秒、70秒、72秒 |
| 2 | **8并行 = 约9秒/组**，8小时可跑 **~3200组** | 计算：8×3600s ÷ 70s × 8并行 ≈ 3200组 |
| 3 | **circ_mv 单位 = 万元** | 茅台2024-12-31 circ_mv = 1.91e8万 = 1.91万亿 |
| 4 | **市值区间真实股票数**（直接从panel算）：0-20亿=820只，20-50亿=1462只，50-80亿=445只，80亿+=719只 | 已验证 |
| 5 | **API真实返回值**： |
|   | `backtest()` → `(equity_df, trades_df, metrics_dict)` 3元组 | 实测确认 |
|   | `backtest_stop_loss()` → `(equity_df, trades_df, sl_events_df, metrics_dict)` 4元组 | 实测确认 |
| 6 | **metrics真实键名**（中文，不要自己翻译）： |
|   | `"夏普比率"`, `"年化收益"`, `"总收益"`, `"最大回撤"`, `"胜率"`, `"调仓次数"` | 实测确认 |
| 7 | **scoring.py filter_func 有已知Bug** | 见Phase 0 |
| 8 | **Python路径硬编码**：执行任何代码前必须先跑这两行： |
|   | `import sys; sys.path.insert(0, 'D:/QMT_STRATEGIES'); import os; os.chdir('D:/QMT_STRATEGIES')` | 已验证 |

---

## 🚨 8小时硬里程碑（必须严格执行）
| 时间点 | 必须完成 | 剩余时间 |
|--------|---------|---------|
| **00:40** | ✅ Phase 0 全部完成（Bug修复+基线验证） | 7小时20分 |
| **01:40** | ✅ Phase 1 全部完成（v2.1 P0微修） | 6小时20分 |
| **02:40** | ✅ Phase 2 全量参数矩阵跑完 | 5小时20分 |
| **04:10** | ✅ Phase 2.5 鲁棒性验证完成 | 3小时50分 |
| **05:10** | ✅ Phase 3 因子层优化完成 | 2小时50分 |
| **06:10** | ✅ Phase 4 高级增强完成 | 1小时50分 |
| **07:30** | ✅ Phase 5 战报+代码整理完成 | 30分钟 |
| **08:00** | ✅ 全部交付物就绪 | 0分钟 |

---

## 🔴 Phase 0：环境校准 + 已知Bug修复（必须00:40前完成，硬阻断）
> **⚠️ 本阶段不完成，后面所有回测全是垃圾！**

### 任务0.1：Python路径 + 模块导入验证
```python
import sys
import os
sys.path.insert(0, 'D:/QMT_STRATEGIES')
os.chdir('D:/QMT_STRATEGIES')

# 验证模块能正常导入
from research.multi_factor_ic.data_loader import load_universe, build_panel
from research.multi_factor_ic.scoring import MultiFactorScorer
from research.multi_factor_ic.backtest import backtest, backtest_stop_loss
print("✅ 所有模块导入成功")
```

### 任务0.2：修复 scoring.py filter_func Bug（最高优先级）
**已知症状**：
- 调用 `scorer.score(panel, fin_ffill, date, filter_func=filter_20_50)` 返回3446只股票
- 与不传filter_func的结果完全相同
- 直接从panel计算是正确的：0-20亿=820只，20-50亿=1462只，50-80亿=445只

**根因方向**：
- `final_mask` 与 factor Series 的索引对齐问题
- `compute_all_factors()` 可能返回不同的索引顺序
- `where()` 操作后 `dropna()` 未能正确过滤

**验收标准（必须全部满足才能进下一阶段）**：
1. 4个市值区间必须返回 **820 / 1462 / 445 / 719** 只股票（±5%容差）
2. v2基线回测必须返回 **1.8% ± 0.5%** 年化
3. 4个区间的交集必须为0（或接近0）

**30分钟修不好的降级方案**：
- 放弃 `filter_func` 参数，**直接在回测外层过滤股票池**
- Phase 2 市值区间测试改为：`panel[panel['circ_mv'] < 5e5]` 等方式
- 任务量从360组降到120组

### 任务0.3：Panel构建 + 基线回归验证
```python
codes = load_universe()
panel, fin_ffill = build_panel(codes)  # 约19秒
print(f"✅ Panel 构建完成，形状: {panel.shape}")

# 跑一次完整基线
eq, trades, metrics = backtest(panel, fin_ffill, top_n=20, freq="2M")  # 约70秒
print(f"✅ 基线回测完成: 年化={metrics['年化收益']}, 最大回撤={metrics['最大回撤']}")
assert float(metrics['年化收益'].replace('%','')) > 1.0, "基线年化异常"
assert float(metrics['年化收益'].replace('%','')) < 2.5, "基线年化异常"
```

### 任务0.4：并行数自动校准
根据真实回测耗时设置并行数：
- < 80秒/组 → **8并行**（跑满360组全量矩阵）
- 80~120秒/组 → **4并行**（跑180组核心矩阵）
- > 120秒/组 → **2并行**（跑54组简化矩阵）

---

## 🔴 Phase 1：v2.1 P0验收微修（必须01:40前完成）
> **目标**：把执行价从close改成open，修复看板百分比Bug

### 任务1.1：backtest.py 执行价全部改为次日开盘价
**修改位置**（共5处）：
1. `backtest()` 普通回测入场
2. `backtest()` 普通回测出场
3. `backtest_stop_loss()` 止损卖出价
4. `backtest_stop_loss()` 替代股入场价
5. `backtest_stop_loss()` 周期末出场价

**验证**：修改后搜索文件中不应再有 "entry_close" 或 "exit_close" 变量（注释除外）

### 任务1.2：修复 HTML 看板百分比渲染 Bug
**问题**：CSV 中 `3.1%` 字符串被二次 × 100 后变成 `310%`
**修复**：检测数值是否包含 `%`，已含则直接显示，不再转换

### 任务1.3：v2.1 基线再验证
修改完成后必须再跑一次双月TOP20无止损回测，确认：
- 年化落在 **0.5% ~ 3%** 区间
- 最大回撤 **-22% ~ -26%**
- 输出到 `reports/v3_optimize/v2_1_baseline.csv`

---

## 🔴 Phase 2：第一层参数调优（必须02:40前完成）
> **任务量**：360组全量正交矩阵，8并行约54分钟跑完

### 测试矩阵（所有组合全覆盖）
| 参数维度 | 档位 | 具体取值 |
|---------|------|---------|
| 📊 市值区间 | 5档 | 0-20亿, 20-50亿, 50-80亿, 80亿+, 全量 |
| ⏰ 调仓频率 | 4档 | W (周度), 2W (双周), M (月度), 2M (双月) |
| ⚖️ 加权方式 | 3档 | 等权, 市值倒数加权, 市值平方倒数加权 |
| 🛑 止损规则 | 3档 | 无止损, -12%固定止损, -8%固定止损 |
| 🎯 持仓数量 | 2档 | TOP 20, TOP 50 |

**总计**：5 × 4 × 3 × 3 × 2 = **360组**

### 输出要求
1. `param_matrix_all_360groups.csv`：所有组合的全部metrics
2. `param_matrix_top20.csv`：按 **夏普比率** 排序前20
3. `param_matrix_by_market_cap.csv`：按市值区间分组的对比

### 验收标准
- 360组回测全部完成，无报错
- 所有组合年化 < 15%（超过则是前视Bug）
- 前5名组合的夏普比率 > 0.3

---

## 🟡 Phase 2.5：鲁棒性验证（必须04:10前完成）
### 任务2.5.1：分行情区间回测
取Phase 2最优的前10个参数组合，分别在3个区间回测：
1. **牛市**：2019-01 ~ 2021-12
2. **熊市**：2022-01 ~ 2022-10
3. **震荡市**：2023-01 ~ 2024-12

输出：`robustness_by_market_regime.csv`

### 任务2.5.2：蒙特卡洛稳健性测试
取Phase 2最优组合，做：
1. 调仓日期随机偏移 ± 5天（100次蒙特卡洛）
2. 持仓数量随机波动 ± 20%（100次蒙特卡洛）
3. 交易成本波动 ± 50%（100次蒙特卡洛）

输出：`monte_carlo_distribution.csv` + 分布图

---

## 🟡 Phase 3：因子层优化（必须05:10前完成）
### 任务3.1：3个高Alpha因子验证 + 替换
砍掉 ROE 因子（IC仅0.02，纯噪音），替换为：
1. **量价一致度**：(收盘价-开盘价)与成交量的20日秩相关系数
2. **60日真实波动率**：日内涨跌幅标准差的60日均值
3. **20日资金净流入**：(收盘价-开盘价)×成交量 的20日均值

先单独跑每个因子的IC序列，IC < 0.05的因子直接砍掉。

### 任务3.2：行业中性化验证
对Phase 2最优组合，分别测试：
1. 无行业中性化（基准）
2. 申万一级行业中性化
3. 申万二级行业中性化

输出：`industry_neutralization_result.csv`

---

## 🟢 Phase 4：高级增强（必须06:10前完成）
### 任务4.1：动态仓位择时
测试中证1000指数20日均线动态仓位：
- 站上20日均线 → 满仓100%
- 跌破20日均线 → 仓位降为50%，剩余按年化2%计息

叠加到Phase 3最优组合。

### 任务4.2：涨跌停/停牌过滤
测试：
1. 入场时过滤：涨停/停牌股票不买入
2. 出场时过滤：跌停/停牌股票顺延到下一个交易日出场

叠加到Phase 3最优组合。

### 任务4.3：全增强叠加测试
把所有增强层全部叠加，跑最终组合，输出HTML看板。

---

## 🟢 Phase 5：战报生成 + 代码整理（必须07:30前完成）
### 任务5.1：通宵作战总结报告
输出 `Overnight_Run_Summary_20260719.md`，必须包含：
1. 各阶段完成情况（哪些完成了，哪些跳过了，原因）
2. 核心结果对比表（从v2基线到最终增强的完整链路）
3. Top 10组合的详细参数 + 绩效指标
4. 存在的问题 + 后续攻坚建议

### 任务5.2：代码整理
1. 所有修改过的文件备份到 `code_backup_v3/`
2. 生成 `changes_from_v2_to_v3.diff` 差异文件
3. 清理临时文件

---

## 📦 08:00必须交付的完整清单
| 序号 | 交付物 | 路径 | 优先级 |
|------|------|------|--------|
| 1 | v2.1 基线回测结果 | `reports/v3_optimize/v2_1_baseline.csv` | 🔴 P0 |
| 2 | 360组参数矩阵全量结果 + TOP20 | `reports/v3_optimize/param_matrix_*.csv` | 🔴 P0 |
| 3 | 鲁棒性验证结果 | `reports/v3_optimize/robustness_*.csv` | 🟡 P1 |
| 4 | 因子优化结果 | `reports/v3_optimize/factor_*.csv` | 🟡 P1 |
| 5 | 全增强最终组合 + HTML看板 | `reports/v3_optimize/final_combined.*` | 🟢 P2 |
| 6 | 通宵作战总结报告 | `specs/Overnight_Run_Summary_20260719.md` | 🔴 必交 |
| 7 | 代码差异 + 备份 | `code_backup_v3/` | 🟢 P2 |

---

## ⚠️ 通宵铁律（违反直接不合格）
1. **基线不能动**：v2.1 基线必须始终保持 1.8% ± 0.5% 年化
2. **年化 > 15% 必须双重验证**：99%概率是前视Bug，必须换股票池 + 换时间区间重跑
3. **每30分钟存一次进度**：不要8小时跑完才存，万一中间崩了全没了
4. **卡壳30分钟直接跳**：不要死磕一个问题一整晚，标注原因做下一个
5. **结果优先于完美**：宁可少跑几组，也不要全跑错了

---

🌙 晚安，明早8点见结果！
