# SPEC：黄氏主升浪「双中军版」生成聚宽 JoinQuant 回测代码

版本：v1.0  
日期：2026-06-24  
发起人：诚哥  
整理：Hermes  
执行：CC  
目标平台：聚宽 JoinQuant  
原始公式文件：`F:\天翼云盘同步盘\Obsidian\量化知识库\20_策略知识库\黄氏主升浪策略.txt`  
相关知识库：

```text
F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\通达信\通达信公式系统实用手册.md
F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\通达信\通达信公式函数到QMT映射速查.md
```

---

## Objective

### 目标

把黄氏主升浪通达信公式里的「双中军版」转换成**聚宽 JoinQuant 可运行的回测策略代码**，方便诚哥在聚宽平台上验证该主升浪选股逻辑。

本任务只做：

```text
双中军版 → 聚宽回测代码
```

本任务不做：

```text
1. 不做 QMT 代码
2. 不做通达信公式修改
3. 不做箱体突破 + 双中军窗口组合
4. 不做 529 / 424 / 低吸 / 430 / 505 版本
5. 不做实盘交易
```

### 核心验收目标

聚宽代码应能在 JoinQuant 回测环境中运行，并输出：

```text
1. 每日候选股票列表
2. 每日买入/卖出记录
3. 回测收益、最大回撤、胜率等聚宽基础回测结果
4. 日志中能看到每只候选股通过/未通过的关键条件
```

---

## Commands

### CC 执行前必读

```text
D:\QMT_STRATEGIES\specs\SPEC_HUANG_ZHONGJUN_JOINQUANT_BACKTEST.md
F:\天翼云盘同步盘\Obsidian\量化知识库\20_策略知识库\黄氏主升浪策略.txt
F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\通达信\通达信公式系统实用手册.md
F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\通达信\通达信公式函数到QMT映射速查.md
```

### CC 工作要求

```text
请遵循 ODM 工作流执行本 SPEC。
请生成聚宽 JoinQuant 平台可直接复制运行的策略代码。
不要生成 QMT 代码。
不要调用任何真实交易接口。
不要修改原始 Obsidian 文件。
不要把箱体突破版加入本次实现。
```

### 交付文件建议

```text
D:\QMT_STRATEGIES\joinquant\huang_zhongjun_main_uptrend.py
D:\QMT_STRATEGIES\joinquant\README_黄氏双中军聚宽回测.md
```

如目录不存在，由 CC 创建。

---

## Structure

### 聚宽策略结构要求

聚宽代码至少包含：

```text
initialize(context)          # 初始化
before_trading_start(context) # 盘前选股或准备数据
handle_data(context, data)   # 日内/每日处理，如不需要可简化
或 run_daily(...)             # 每日定时执行选股/调仓
```

CC 可按聚宽推荐风格实现，但必须保证代码能在聚宽策略编辑器中直接运行。

### 策略逻辑模块建议

建议拆成以下逻辑块，便于检查：

```text
1. get_stock_pool(context)
   取 A 股候选池，并过滤 ST、停牌、新股等

2. calc_zhongjun_signal(stock, date)
   计算单只股票在某日期是否满足双中军版条件

3. select_stocks(context, date)
   批量筛选所有候选股票

4. rebalance(context, selected)
   根据候选结果调仓
```

注意：函数名只是建议，CC 可按聚宽习惯调整，但必须保留清晰的“选股逻辑”和“调仓逻辑”边界。

---

## Code Style

### 复刻原则

1. 尽量 1:1 复刻通达信「双中军版」语义。
2. 不擅自优化参数。
3. 每个条件要有清晰变量名和注释。
4. 聚宽代码中要明确说明每个通达信函数如何映射。
5. 不要把没进入最终公式的变量擅自加入最终选股条件。

### 通达信函数映射要求

| 通达信函数 | 聚宽/Python 映射要求 |
|---|---|
| `MA(CLOSE,N)` | N 日简单移动平均 |
| `EMA(CLOSE,N)` | 按通达信 EMA 口径：`Y=(X*2+Y'*(N-1))/(N+1)` |
| `REF(X,N)` | N 日前数据，严禁未来函数 |
| `HHV(HIGH,N)` | 最近 N 日最高值 |
| `CROSS(A,B)` | 今日 A>B 且昨日 A<=B |
| `AVEDEV(X,N)` | N 日平均绝对偏差，用于 CCI |
| `INDEXC` | 聚宽中明确选择大盘指数作为环境过滤，建议默认 `000001.XSHG` 或 `000300.XSHG`，必须在 README 说明 |

### 聚宽数据口径要求

CC 需在 README 中明确：

```text
1. 使用前复权/后复权/不复权哪种价格口径
2. 股票池范围：全 A / 沪深300 / 中证全指 / 自定义
3. 是否过滤 ST、停牌、退市、新股、科创/北交所
4. 大盘环境过滤使用哪个指数
5. 调仓频率：每日 / 每周 / 信号日
```

### 风险边界

```text
禁止实盘交易说明
禁止接入真实券商接口
本代码仅用于聚宽历史回测
```

---

## Testing

### 聚宽端必须可验证

CC 交付后，诚哥应能把代码复制到聚宽策略编辑器运行。

代码需支持至少以下回测设置：

```text
回测频率：日线
初始资金：可配置，默认 100000 或聚宽默认
股票池：默认全 A 或可配置指数成分
调仓频率：每日检查信号
持仓数量：可配置，建议默认 3~5 只
买入规则：候选股按等权或评分排序买入
卖出规则：先做简单规则，例如不再满足双中军条件 / 跌破 MA20 / 持有 N 日后退出；如 CC 认为卖出规则必须简化，需在 README 标明
```

### 必须输出的调试字段

每只候选股至少能解释以下条件：

```text
多头排列条件
均线发散条件
MACD条件
CCI条件
突破压力条件
MA20向上
MA60向上
大盘条件
最终 主升浪启动
```

### 验收标准

```text
1. 聚宽代码无语法错误
2. 代码能在聚宽回测环境运行
3. README 说明参数、数据口径、调仓/卖出规则
4. 日志能看到每日候选数量和最终买入股票
5. 不包含 QMT/passorder/xtquant/实盘接口
6. 不包含箱体突破窗口组合逻辑
```

---

## Boundaries

### Always

- 只做「双中军版」聚宽回测代码。
- 保留原始通达信公式作为注释或 README 映射依据。
- 保持参数默认值与原公式一致。
- 代码必须能复制到聚宽运行。
- 所有交易都是聚宽回测虚拟交易，不是真实交易。

### Ask First

- 是否加入箱体突破窗口组合。
- 是否加入 529/424 等其他版本。
- 是否改变参数。
- 是否改卖出规则为更复杂风控。
- 是否限定股票池为沪深300/中证500/全A。

### Never

- 不写 QMT 代码。
- 不调用 QMT 下单接口。
- 不调用真实券商接口。
- 不修改原始 Obsidian 文件。
- 不把本任务扩展成完整实盘系统。

---

# 原始通达信公式：双中军版

```text
#双中军版----------------------

{=== 主升浪启动选股公式（通达信，已删除温和放量+筹码条件）===}
{----- 1. 均线多头排列 -----}
MA5 := MA(CLOSE,5);
MA10 := MA(CLOSE,10);
MA20 := MA(CLOSE,20);
MA60 := MA(CLOSE,60);
MA120 := MA(CLOSE,120);
多头排列条件 := MA5 > MA10 AND MA10 > MA20 AND MA20 > MA60 AND MA60 > MA120;

{----- 2. 均线刚发散 -----}
发散确认 := CLOSE > MA20;
MA5角度 := ATAN((MA5/REF(MA5,1)-1)*100)*180/3.1416;
均线发散条件 := MA5角度 > 30 AND MA5/MA20 > 1.05;

{----- 3. MACD零轴上金叉或双线向上 -----}
DIF := EMA(CLOSE,12) - EMA(CLOSE,26);
DEA := EMA(DIF,9);
MACD红柱 := (DIF - DEA) * 2;
MACD条件 := (CROSS(DIF,DEA) AND DEA > 0) OR (DIF > DEA AND DIF > REF(DIF,1) AND DEA > REF(DEA,1));

{----- 4. CCI突破100 -----}
TYP := (HIGH + LOW + CLOSE)/3;
CCI14 := (TYP - MA(TYP,14))/(0.015 * AVEDEV(TYP,14));
CCI条件 := CROSS(CCI14,100) OR (CCI14 > 100 AND CCI14 > REF(CCI14,1));

{----- 5. 突破压力位 -----}
N := 20;
近期高点 := REF(HHV(HIGH, N), 1);
突破压力条件 := CLOSE > 近期高点 AND CLOSE/近期高点 < 1.08;

{----- 8. 中期趋势确认 -----}
MA20向上 := MA20 > REF(MA20,5);
MA60向上 := MA60 > REF(MA60,5);

{----- 9. 大盘环境过滤 -----}
大盘指数 := INDEXC;
大盘MA20 := MA(大盘指数,20);
大盘MA60 := MA(大盘指数,60);
大盘条件 := 大盘指数 > 大盘MA20 AND 大盘MA20 > 大盘MA60;

{----- 最终选股条件 -----}
主升浪启动: 多头排列条件 AND 均线发散条件 AND MACD条件 AND CCI条件
            AND 突破压力条件
            AND MA20向上 AND MA60向上 AND 大盘条件;
```

## 复刻注意事项

1. `发散确认 := CLOSE > MA20;` 在原公式中定义但没有进入最终 `主升浪启动` 条件。不要擅自加入最终条件。
2. `MACD红柱` 在原公式中定义但没有进入最终条件。不要擅自加入最终条件。
3. `INDEXC` 必须明确映射成聚宽指数代码。
4. `近期高点 := REF(HHV(HIGH, N), 1)` 不包含当日 high，防止当日自引用。
5. `CROSS` 必须按“今日上穿，昨日未上穿”实现，不能只判断今日大于。
