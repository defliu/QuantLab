# Agent Harness 记忆工具选型对比

> 调研日期：2026-09-04 | 数据源：GitHub 实时 API（star/活跃度均为当日快照）
> 目标：评估哪些开源 harness 记忆工具能补强 QuantLab + WorkBuddy 的跨会话记忆链路

---

## 一、结论先行

**推荐接入 `engram`**（Gentleman-Programming/engram，Go，6.3k★）。它是本次扫描中唯一同时满足以下全部条件的方案：

1. **客户端无关** —— 只要求 MCP stdio，不绑定任何特定 agent 的生命周期钩子；
2. **Windows 原生** —— 单一 exe，无需 WSL、Node、Python、Docker；
3. **零运行时依赖** —— 纯 Go SQLite（modernc.org/sqlite，无 CGO），一个 db 文件；
4. **本地优先** —— 无账号、无 API key、无云强制；云同步为可选 opt-in；
5. **项目级隔离** —— 支持 `.engram/config.json` 锁定项目名，天然匹配 QuantLab 的 `projects/Project_XX` 结构；
6. **带记忆治理** —— 冲突检测（`mem_judge` / `supersedes` 标注）直接对治「陈旧结论污染决策」这一核心痛点。

**不推荐 `claude-mem`**（93k★，体量最大但方向最不匹配）：强绑 Claude Code 的 5 个生命周期钩子，安装需浏览器登录，且已更名 Grok Mem 并捆绑社区代币推广。

---

## 二、候选全景

| 项目 | Star | 语言 | 存储 | 客户端绑定 | 自托管 | 最近更新 |
|---|---|---|---|---|---|---|
| mem0ai/mem0 | 64.7k | Python | 向量库 | SDK 接入 | 部分 | 2026-09-04 |
| volcengine/OpenViking | 35.4k | Python | 上下文数据库 | 插件体系 | 是 | 2026-09-04 |
| topoteretes/cognee | 30.5k | Python | 知识图谱 | 通用 | 是 | 2026-09-04 |
| getzep/graphiti | 30.6k | Python | 时序知识图 | 通用 | 是 | 2026-09-04 |
| TencentCloud/TencentDB-Agent-Memory | 25.9k | TypeScript | 向量 + 四层记忆 | 插件 | 是 | 2026-09-04 |
| MemoriLabs/Memori | 16.4k | Python | 企业级分层 | 通用 | 是 | 2026-09-04 |
| NevaMind-AI/memU | 14.4k | Python | Markdown | harness 插件 | 是 | 2026-09-04 |
| **Gentleman-Programming/engram** | **6.3k** | **Go** | **SQLite+FTS5** | **任意 MCP** | **是** | **2026-09-04** |
| plastic-labs/honcho | 7.0k | Python | Postgres | 通用 | 是 | 2026-09-04 |
| doobidoo/mcp-memory-service | 1.9k | Python | sqlite-vec + 图 | 任意 MCP/HTTP | 是 | 2026-09-04 |
| cytostack/openwolf | 2.3k | TypeScript | 本地文件 | Claude/Codex/OpenCode | 是 | 2026-09-03 |
| zilliztech/memsearch | 2.6k | Python | Markdown + Milvus | 插件 | 是 | 2026-09-04 |
| alash3al/stash | 767 | Go | Postgres | MCP | 是 | 2026-06-14（趋缓） |
| grpcer/ownmem | 235 | JavaScript | Git 原生 BM25 | 多客户端 | 是 | 2026-09-04 |
| thedotmack/claude-mem | 93.1k | TypeScript | SQLite + Chroma | **Claude Code 钩子** | 否（需登录） | 2026-09-04 |
| affaan-m/ECC | 247.5k | JavaScript | 套件含 memory 层 | 多客户端适配器 | 是 | 2026-09-04 |

---

## 三、四档判定

### 第一档：推荐接入 —— engram

| 维度 | 情况 |
|---|---|
| 架构 | Agent → MCP stdio → Go 二进制 → SQLite + FTS5（`~/.engram/engram.db`） |
| 工具面 | 默认 22 个 MCP 工具，`--tools=agent` 精简为 18 个 |
| 核心工具 | `mem_search` / `mem_context` / `mem_timeline` / `mem_save` / `mem_update` / `mem_judge` / `mem_compare` / `mem_session_summary` / `mem_doctor` |
| 项目隔离 | `.engram/config.json` 写 `project_name`；monorepo 支持子项目就近解析 |
| 记忆治理 | 每次 `mem_save` 自动 FTS5 相似度扫描，命中即返回 `candidates[]` + `judgment_required`；`mem_judge` 落定后检索结果显示 `supersedes` / `conflicts` 标注 |
| 稳定键 | `topic_key`（如 `architecture/auth-model`）复用更新，避免同一主题产生竞争记忆 |
| 交接 | `mem_session_summary` 保存目标/发现/产出/下一步/相关文件 |
| 许可证 | MIT |

**为什么它匹配 QuantLab 的既有痛点：**

| QuantLab 痛点 | engram 对应机制 |
|---|---|
| 调研结论未落盘导致重复调查（Project_12/18/01 反复） | `mem_search` 前置检索约定 + `mem_session_summary` 强制交接 |
| 「防复发机制」靠手写 MEMORY.md 条目 | `topic_key` 稳定键 + 结构化 `What/Why/Where/Learned` 四段式 |
| 陈旧结论污染新决策 | `mem_judge` 冲突检测、`supersedes` 检索标注 |
| 项目多、结论分散（Project_01/10/16…） | `.engram/config.json` 项目隔离 + `mem_search(project=...)` 定向查询 |

### 第二档：功能备选 —— mcp-memory-service

能力最全：知识图谱（typed edges: causes/fixes/contradicts）、76 个 REST 端点、Web 看板、自动整合（decay + compression）、BM25 + 向量混合检索、多 agent `X-Agent-ID` 隔离。有公开基准（LongMemEval R@5 80.4%、DevBench 91.1%）。

**代价与坑：**
- 需 Python 环境 + ONNX 嵌入模型；QuantLab 已有 Python 3.13 venv，但会新增一条依赖链；
- **中文硬伤**：默认嵌入模型 `all-MiniLM-L6-v2` 为 **English-only**。存放中文记忆必须切 `paraphrase-multilingual-MiniLM-L12-v2`，且**切换模型必须重算全部历史向量**（跨语言余弦相似度会从 ~0.95 掉到 ~0.10）；
- 近期发布集中在安全修复，迭代节奏快（v11.10.0，2026-08-28）。

适合「要图谱、要看板、要多 agent 共享」的场景；若只要「别再重复调查」，过重。

### 第三档：设计参考 —— ECC（247.5k★）

不是记忆工具，是 harness 全家桶（skills / instincts / memory / security / research-first）。**亮点是它已内置 CodeBuddy（腾讯）适配器**，与 WorkBuddy 同系，其 `memory hooks` 与 skills 组织方式可直接借鉴。本地自托管、核心功能无需 API key。

不建议整体引入（会与 WorkBuddy 自带 skill 体系冲突），但值得扒它的 memory hook 设计。

### 第四档：排除 —— claude-mem（93.1k★）

- 依赖 Claude Code 的 SessionStart / UserPromptSubmit / PostToolUse / Stop / SessionEnd 五个钩子，WorkBuddy 无对应机制；
- 安装要求浏览器登录（邮箱 magic link）以换取 memory key；
- 已更名 Grok Mem，README 显著位置推广 CMEM 社区代币（附 BASE 合约地址）；
- 需 Node ≥20 + Bun + uv + Chroma。

体量最大但与本场景适配度最低。

**同样排除的重型方案**：mem0 / OpenViking / cognee / graphiti / TencentDB-Agent-Memory —— 均需额外向量库或图库，对「单人本地量化项目」属于过度工程。

---

## 四、落地路径（engram → WorkBuddy）

### 步骤 1：安装二进制

Windows 三条路径，**优先 A**（本机编译不触发杀软误报；用户机器 VBS + Defender 开启，预编译 exe 有被启发式误报为 `Trojan:Script/Wacatac.H!ml` 的已知记录）：

```powershell
# A. go install（推荐）
go install github.com/Gentleman-Programming/engram/cmd/engram@latest

# B. 源码构建
git clone https://github.com/Gentleman-Programming/engram.git
cd engram
go install ./cmd/engram

# C. 预编译（需自行加 Defender 白名单）
# https://github.com/Gentleman-Programming/engram/releases
```

数据目录默认 `%USERPROFILE%\.engram\engram.db`，可用 `ENGRAM_DATA_DIR` 覆盖。

### 步骤 2：注册到 WorkBuddy

编辑 `C:\Users\Administrator\.workbuddy\mcp.json`，合并进 `mcpServers`（**不要覆盖既有条目**）：

```json
{
  "mcpServers": {
    "engram": {
      "command": "engram",
      "args": ["mcp", "--project=QuantLab"]
    }
  }
}
```

> **必须显式指定 `--project`**。MCP 服务器进程通常不继承 shell 的 cwd，仅靠 cwd 探测会落到目录名兜底或解析失败。等价写法是 `env: {"ENGRAM_PROJECT": "QuantLab"}`；两者同时给则 flag 优先。

写入后需在连接器管理页对新 server 点击「信任」才会激活，且不会自动生效，要重启会话。

### 步骤 3：注入 Memory Protocol（关键）

只注册 MCP 不足以让 agent 主动用记忆——**必须有指令告诉它何时存、何时查**。engram 官方提供可复制的 Protocol 全文（`DOCS.md#memory-protocol-full-text`）。最小版写入 `AGENTS.md`：

```markdown
## Memory

已接入 engram 持久记忆（MCP 工具：mem_save / mem_search / mem_context / mem_judge / mem_session_summary）。

- 开工前：先 mem_search 检索本次任务相关历史，避免重复调查。
- 完成后：主动 mem_save 落盘结论，不要等被问。
- 会话结束前：mem_session_summary 保存目标/发现/产出/下一步。
- 上下文压缩后：先 mem_session_summary 持久化，再按需 mem_context 恢复。
```

### 步骤 4：项目锁定

在 `D:\QuantLab\.engram\config.json` 写入：

```json
{ "project_name": "QuantLab" }
```

若希望按策略项目细分（推荐，匹配 `projects/` 目录结构），在各子目录放各自的 config，engram 取**最近的一层**。

---

## 五、必须验证的风险点

### 风险 1：中文全文检索（高优先级）

engram 检索核心是 **SQLite FTS5**。FTS5 默认 `unicode61` 分词器按空格与标点切词，**中文连续字符串会被整体视为一个 token** —— 纯中文查询可能检索失效。

**验证方法**（装好后立刻做）：
1. `mem_save` 一条纯中文记忆，例如「Tushare moneyflow 的 amount 字段单位是万元」；
2. 分别用「万元」「moneyflow」检索，比较命中差异；
3. 若中文查询失败，评估影响面。

**缓解策略**：QuantLab 记忆中天然含大量英文锚点（Tushare、moneyflow、Project_16、QMT、passorder、xtquant、IC、ICIR），以英文术语作为检索键即可覆盖绝大部分场景；记忆条目采用「英文锚点 + 中文说明」的混合写法。

### 风险 2：与 WorkBuddy 既有三层记忆的关系

engram 是**增量**，不是替代。分工建议：

| 层 | 职责 | 载体 |
|---|---|---|
| 云记忆 | 跨会话语义检索（服务端排序） | conversation_search |
| 用户级 | 跨项目硬规则 | ~/.workbuddy/MEMORY.md |
| 工作区 | QuantLab 项目日志 | .workbuddy/memory/YYYY-MM-DD.md |
| **engram（新增）** | **结构化结论库 + 冲突治理 + 项目隔离检索** | ~/.engram/engram.db |

不做迁移，只做增量。现有「防复发」条目继续留在 MEMORY.md，engram 承接的是「可检索、可判冲突、可按项目隔离」的结构化结论。

### 风险 3：杀软误报

走 `go install` 路径可规避。若用预编译包，需提前在 Defender 加排除项。

---

## 六、持续跟踪源

- `ai-boost/awesome-harness-engineering`（3.9k★）—— harness 工程总清单，含 memory 专章
- `NirDiamant/awesome-harness-engineering` —— 另一份侧重记忆架构与论文的清单
- `IAAR-Shanghai/Awesome-AI-Memory`（1.2k★）—— 中文团队的 AI 记忆知识库
- `NirDiamant/Agent_Memory_Techniques`（1.0k★）—— 30 个可运行 notebook，含 Mem0/Letta/Zep/Graphiti 实操对比

---

## 七、下一步建议

1. **低成本试错**：只做步骤 1-2（约 10 分钟），先验证 MCP 连通性 + 风险 1 的中文检索表现；
2. **中文检索若不可用**，回退方案为 `mcp-memory-service`（配 multilingual 嵌入模型，从零开始无历史向量包袱，正好避开重算问题）；
3. **验证通过后**，再补步骤 3-4 并迁移历史结论。

不建议跳过验证直接全量迁移。

---

## 八、落地结果（2026-09-05）

### 安装与配置（已完成）

| 项 | 状态 |
|---|---|
| 二进制 | `C:\Users\Administrator\bin\engram.exe` v1.20.0（winget 装 Go 失败 → 改预编译包；本机实测未触发 Defender 误报，已入用户 PATH） |
| 数据库 | `~/.engram/engram.db`（纯 Go SQLite+FTS5，无 CGO） |
| WorkBuddy MCP | `~/.workbuddy/mcp.json` → `engram --project=QuantLab --tools=agent` |
| Trae MCP | `TRAE SOLO CN\User\mcp.json` → `engram-quantlab`(QuantLab) + `engram-traework`(Traework) 双 server |
| 工作区锁定 | `D:\QuantLab\.engram\config.json` + `D:\Traework\.engram\config.json`（均写 `project_name`） |
| Memory Protocol | 已注入 `AGENTS.md` 新增「持久记忆（engram MCP）」章节 |

### 实测验证结论

1. **MCP 存→查闭环正常**：经 `engram mcp --project=QuantLab` 路径 `mem_save` 后 `mem_search` 可命中，条目正确绑定 `project=quantlab`。
2. **中文检索确认失效（高风险已兑现）**：纯中文查询（资金流 / 万元 / 撤单）必然 miss；中英混排仅当中文片段恰与英文连成同一 token 才偶然命中。→ 强制「英文锚点标题 + 中文说明正文」写法，检索一律用英文锚点。
3. **项目隔离严格且大小写归一**：`QuantLab→quantlab`。CLI 裸 `engram save` 不绑定项目（落 `project=""`），MCP 的 `quantlab` 作用域会排除它 → WorkBuddy 检索不到。已发生 id5 游离一次并清理。→ **记忆一律经 MCP 工具落库，禁止 CLI 裸存**。
4. **冲突治理已 live**：第二条示例记忆写入时触发 `mem_judge` 候选，治理机制可用。
5. **Traework 端到端验证通过（2026-09-05 补充）**：`engram mcp --project=Traework` 存→查闭环正常（条目绑定 `traework`），且 `search "moneyflow"` 在 Traework 作用域返回空 → 与 QuantLab 严格隔离确认。

### 已播种示例记忆（quantlab 可见）

- `#7 decision` — `moneyflow amount unit = 万元 (not 元)`
- `#8 bugfix` — `QMT passorder is async, not order id`

### 待办 / 可选

- 若需中文原生检索：需从源码以 FTS5 `trigram` 分词器重建 engram（需先装 Go 工具链），属可选增强，当前英文锚点方案已可满足量化域检索。
- WorkBuddy / Trae 侧需在连接器管理页对 engram server 点击「信任」并重启会话方可激活。
