# 全局复利与踩坑日志

> 记录跨项目通用的踩坑经验，避免重复犯错
> 创建时间：2026-07-22
> 2026-08-01：合并迁移自原 QMT_STRATEGIES 工程的 Claude memory（40+ 条蒸馏），来源标注 `CM`（Claude Memory）

---

## QMT 红线（编码/语法/API）

### 1. 编码必须 GBK
- **现象**: `# coding=utf-8` 在 QMT 策略编辑器中报红线
- **原因**: QMT 内部文件读写默认 GBK
- **修复**: 所有 QMT 策略首行必须是 `# coding=gbk`
- **日期**: 2026-07-22

### 2. passorder 是全局函数
- **现象**: `C.passorder()` 报 `'__PyContext' object has no attribute 'passorder'`
- **原因**: `passorder` 不是 ContextInfo 的方法，是全局函数
- **正确写法**: `passorder(23, 1101, ACCOUNT_ID, code, 11, price, volume, C)`
- **参数顺序**: opType, orderType, accountid, orderCode, prType, modelprice, volume, ContextInfo
- **日期**: 2026-07-22

### 3. 账号ID需硬编码
- **现象**: `C.accountid` 不存在
- **原因**: QMT ContextInfo 没有 accountid 属性
- **修复**: 在策略顶部定义 `ACCOUNT_ID = '67014907'`
- **日期**: 2026-07-22

### 4. C.get_market_data_ex 不支持财务字段
- **现象**: pe_ttm/pb/circ_mv 返回 NaN
- **原因**: QMT API 限制，仅支持基础 OHLCV
- **修复**: 从本地 astock parquet 预生成 CSV，策略用 pd.read_csv 加载
- **日期**: 2026-07-22

### 5. QMT Python 3.6.8 无 pyarrow
- **现象**: `No module named 'pyarrow'`
- **原因**: QMT 内置 Python 版本太旧
- **修复**: 不能读 parquet，改用 CSV
- **日期**: 2026-07-22

### 6. QMT 内置 Python 不保证有第三方包（CM）
- **现象**: 重装 QMT 后策略在 `import yaml` 处直接崩（`ModuleNotFoundError: No module named 'yaml'`），策略根本没初始化，但 UI 不显眼报错
- **后果**: 600641 当日跌 8.41% 也没触发硬止损——真正原因是策略压根没起来
- **修复**: 任何第三方 import 都要 try/except fallback（内置轻量解析 + 默认配置）；遵守"QMT 只依赖 xtquant/pandas/numpy"约束
- **验证**: 重装/换设备后必看 `XtClient_FormulaOutput_*.log` 确认 `[trade]start trading mode` + `[策略名] 初始化完成` + 启动 cooling-off 三连，缺一就是没起来
- **排查顺序**: 策略没下单 → 先确认策略在跑 → 再看持仓可卖量（老挂单占 `enableAmount=0`）→ 最后看触发逻辑

### 7. QMT 运行产物语法红线
- 首行必须是 `# coding=gbk`
- Python 3.6.8 兼容：禁止 `dict[str,...]` / `list[str]` / `str | None` / `:=` / `match/case` / f-string
- `passorder()` 是全局函数，不是 `C.passorder()`

---

## QMT 下单与异步（实盘执行层，CM）

### 8. passorder 是异步接口，返回 0/None 不是订单号（CM）
- 正常返回 `0` 或 `None`，**把返回值当 order_id 用 → 0 流进跟踪字典 → 孤儿委托**
- 拿真实订单号：`passorder` 后立即 `get_trade_detail_data(account, account_type, 'order')` 反查，倒序遍历委托簿
- 反查硬条件只保留五条 AND：`code / volume / status / direction / time`；字段缺失/解析失败时放宽不卡，不一票否决
- 日志出现"订单号无效(0)，清理"循环挂死 → 100% 是这个 bug 模式

### 9. 买卖委托后即时反查撞 ~100ms 异步延迟（CM）
- **现象**: 600641 卖单真实成交（QMT 主日志可证），策略却报"卖出反查失败/换仓失败"
- **根因**: `sell_order` passorder 后**立即**反查 order_id，QMT 异步分配有 ~100ms 延迟（主日志 `processOrderIDResp` 时间差可证）→ 查不到 → return None → 不登记 pending → 后续成交回写/撤单/市价重试全够不着
- **修复**: passorder 后加短轮询（`SELL_LOOKUP_RETRIES=4/INTERVAL=0.2` 起步，实测不够加到 `RETRIES=15`/轮询 3s）
- **排查**: 看到"买卖反查失败但 QMT 实际成交"，先查 `XtClient_YYYYMMDD.log` 的 `processOrderIDResp` 与策略反查时间差，确认轮询次数/间隔是否不足

### 10. 订单反查 remark 不能硬过滤（CM）
- `m_strRemark` 含策略名**不能作为硬 continue 过滤条件**，只能作为多候选优先级信号
- 反查硬条件只留 code/volume/status/direction/time 五条 AND；remark 改为候选打分，唯一候选即使 remark 空也返回
- **Why**: QMT 版本/字段格式一变，硬卡 remark 会让全部委托被误判失败

### 11. XtOrder 字段速查（CM）
- 已实测可用：`m_nOrderID` / `m_strInstrumentID` / `m_strExchangeID` / `m_nOrderVolume` / `m_nVolumeTraded` / `m_strRemark` / `m_nOrderStatus`（54/55/57=已撤/已废/已拒）/ `m_strOptName`（"买入"/"卖出"中文）/ `m_strInsertTime`（HHMMSS 字符串）
- **判方向优先 `m_strOptName`**（含中文买/卖）
- 未实测避免主用：`m_nOffsetFlag` / `m_nDirection` / `m_strOpType`
- 时间换算：`time.time()` epoch → localtime → `hour*10000+min*100+sec`，与 `int(m_strInsertTime[-6:])` 比较，-1 秒容差

### 12. 反查失败会静默断链，市价重试/撤单永不触发（CM）
- 设计"限价不成交→撤单→retries>=1 市价重试"，但触发前提是首次限价单反查成功+登记 pending；**反查失败断在最前** → `_check_pending_sells` 无对象 → 后续分支永不触发
- 表象"限价单不成交也不撤单换市价"根因不是缺机制，是反查断链
- 修复要点: 待报单（status=50 未申报、`m_nOrderVolume` 空）不能被 `vol!=expected_vol` 误 continue

### 13. "卖出只在10点触发"是静默假象（CM）
- 卖出评估全天有效（0930-1130/1300-1500 + now<1458 门控），"只在 10 点"是 `[卖出评估]` 每票每天**首次去重打印**造成的假象
- 排查"没卖出"不能依赖去重打印，要看心跳行/每个操作点日志

---

## QMT 持仓与状态同步（CM）

### 14. 孤儿持仓导致"信号触发不卖"（CM）
- **定义**: 账户有票、但持仓文件没记录 → 卖出引擎不评估这只票 → 止损/止盈永不触发（603283 -7.7% 硬止损当天没卖）
- **修复**: 从账户全量持仓纳管（`get_holdings()` 拿 volume>0 且不在 `_g_my_codes` 的票纳入，成本取 m_dOpenPrice）；卖出评估每轮前补一次 sync（幂等，已在集的跳过）
- **坑**: init 首帧调 sync 时 QMT 交易通道未就绪可能空返 → 需要后续轮次兜底补纳管

### 15. 清仓票当天赖在持仓表（CM）
- **根因**: 成交确认后 pop 被 `if pos.volume<=0` 卡住——`get_position` 读 QMT 端 position 缓存，卖出成交后缓存刷新延迟（秒~分钟级），当天 volume 还是旧值
- **修复**: pop 改无条件 + 成交确认后立即写持仓文件 + 缓存仍显量时打 warning；成交确认即移除，立即释放持仓名额

### 16. 买入 volume<100 静默跳过 = 观测死区（CM）
- **现象**: 买入循环 `if volume >= 100:` 不成立就静默跳过什么都不打印；高价股（688802 约 770 元）预算买不起 1 手，日志完全空白看不出原因
- **根因**: 选股阶段没按"预算可买性"过滤——per_stock_amount 不够买 1 手的高价股不该进 buyable
- **修复**: ①静默处补 else print（买不起 1 手）②选股时 `per_stock_amount >= price*100` 才进 buyable
- **排查**: "没买入+日志无异常" → 先查第一名股价×100 vs per_stock_amount（净值×单只比例）

### 17. 策略时间必须用 QMT 行情时间（CM）
- **根因**: 设备主板 CMOS 电池没电，断电后时钟错乱，`datetime.now()` 按错时钟触发尾盘
- **修复**: 用 `C.get_current_time()`，三级兜底（行情时间 year>=2020 → K线末根日期 → 设备时间+警告）；相对计时（冷却/耗时）用 `time.time()` 不受影响
- 启动打 `[时间校验] 行情时间=... 设备时间=...` 日志

### 18. 函数内给全局变量赋值必须先 global 声明（CM）
- **现象**: init 里给 `_g_exported_today` 赋值但没开头 `global` 声明 → Python 当局部变量 → 别处读报 `UnboundLocalError`
- 实盘日志暴露后才发现，属于低级但致命 bug

---

## QMT 数据与限制（CM）

### 19. 模拟端交易数据隔日清空（CM）
- `get_trade_detail_data` 的 **deal 成交 / order 委托** 模拟端柜台**只保留当日**，隔日重启后查不到；position/account 是实时快照，看不到历史
- **Why**: 每日收盘导出 CSV 到 `D:/QMT_POOL/` 是刚需不是便利——不导出就永久丢失
- **How**: 查 N 天前模拟端成交/委托 → 只能翻 `成交明细_YYYYMMDD.csv`，别想从 QMT API 现查

### 20. QMT 分钟线历史深度只有近 1 年（CM）
- 模拟端 xtdata 1min/5min 首日都是 20250625（服务器端硬上限），与下载请求 start_time 无关
- **日线能到 2010**（约 16 年），这是 QMT"无限制"的真正部分
- astock 买断数据 1min 真实从 2009-01-05 起，是分钟级任务的唯一数据基础

### 21. 账户资金字段映射（CM）
- 国金 QMT account 对象：**总资产 = `m_dAssetBalance`**（`m_dTotalAsset`/`totalAsset`/`m_dTotal` 不存在）；可用资金 = `m_dAvailable`；持仓市值 = `m_dStockValue`/`m_dInstrumentValue`
- ⚠️ `m_strStatus` 实测显示"登录失败"但通道正常，不要据此判断通道状态
- fallback 链写错会把可用资金误当总资产

### 22. 收盘后 handlebar 不再触发（CM）
- QMT 收盘（15:00）后 handlebar **不再触发**，最后一帧是 15:00
- 把导出/收盘任务绑在 `now >= '1505'` 永不触发 → 改 1500
- 盘后部署时 init 不被 cooling-off 挡 → "策略启动时执行一次"的任务放 init 末尾；"盘中定时"放 handlebar；两者用标志联动防重复

### 23. 策略必须自包含 config（CM）
- **坑**: `_load_config()` 用 `__file__` 算 config 路径，QMT exec 环境 `__file__` 指向运行设备 → 读不到 → 走默认配置 → 实盘显示错误策略名
- **修复**: 去 `__file__` 依赖，候选路径 + 完整 `_DEFAULT_CONFIG` fallback；拿到哪个设备都能跑对
- **部署认知**: 策略代码粘到 QMT 终端运行后自动加密成密文 `STRATEGY.py`，本地改代码必须**重新粘**才生效

### 24. 日志文件位置与编码（CM）
- 实盘/模拟策略日志真实落点在局域网机器：`\\192.168.31.131\国金qmt交易端模拟\userdata\log`（本机只有 06-30 前的）
- **策略 print 在 `XtClient_FormulaOutput_YYYYMMDD.log`**（业务日志），**不是** `XtClient_Formula_*.log`（引擎层 INFO，不含业务 print）
- FormulaOutput 编码是 **UTF-8**（策略源码是 GBK 但落盘是 UTF-8），用 gbk 解会乱码
- CMOS 日期错乱时 `strategy_log_*.txt` 文件名不可信（可能显示 202311xx），看内容里 `today=`（行情时间）才是真实日期

### 25. C.get_turnover_rate() 不存在（CM）
- QMT 策略上下文对象没有此方法；换手率数据仅回测 DataFrame 有 `df['turnover_rate']` 列
- 生产版用捕获 AttributeError 后跳过换手率过滤

### 26. 导出逻辑独立部署，不集成主策略（CM）
- 主策略只做交易+日志/对账/SAFEMODE；导出成交/持仓/资金 CSV 独立成可部署文件（`数据导出.py`），K线1分钟，init 即导出
- **Why**: 导出 bug 会牵连主策略；导出独立可单独更新/重启不影响交易
- 构建产物 SOURCE_FILES 只保留日志函数，导出函数一律不进主策略

### 27. 新设备部署最小集（CM）
- QMT Python 解释器在 `bin.x64\pythonw.exe`（不是 python 目录），Python 3.6.8，自带 numpy/pandas/xtquant，**唯一缺 pyyaml**
- 必拷 `strategy_main.py`（GBK，二进制拷）+ config；完全不用拷 core/adapters 等源目录
- 历史 K 线要在 QMT 客户端补全 ≥120 交易日，否则"外部池无股票通过技术信号"（不是 bug 是数据不足）

---

## 数据源与财务 PIT（CM）

### 28. astock 是第一数据源
- `E:/astock/` parquet：日线+1/5/15min 2009 至今 + 财务 2005 起 + 5793 只，买断离线不依赖客户端
- 实时补充 `qmt_self_owned`（F:/backtest_workspace/data/duckdb/）；`huicexitong` 已停更（2026-04-03）只作交叉验证；`jince_zhisuan` 备用
- 配置必须显式声明 `data_source`，不做 auto detector；看到 yaml 里 `source: jince_zhisuan` 要警觉（历史遗留）
- astock 另有 `is_st / listed_days / up_limit / down_limit`（ST/新股过滤/精确涨跌停）

### 29. adj_factor 是后复权因子（CM）
- astock 的 `adj_factor` 是**后复权因子**（以上市首日为基准）；前复权价 = 原始价 × (adj_factor / 最新 adj_factor)
- 直接用后复权乘 adj_factor 会导致长牛股（600519）筹码 WINNER 算反
- **回测用 hfq（当日 adj_factor，PIT 安全），qfq 才 look-ahead**——qfq 需要未来数据做归一

### 30. 通达信筹码：COST 可对齐，WINNER/SCR 不可（CM）
- COST(N)=累积成交量达 N% 时的价格（线性插值），1min bar 代表价 (O+H+L+C)/4 × vol 前复权，250 日 lookback → 与通达信 MAE 1-2%
- **SCR 是系统技术指标不是公式函数，算法未公开**；知识库所有 SCR 公式互相不一致，只能方向近似
- WINNER 通达信用换手率衰减的移动成本分布，简单直方图在长牛股差异巨大
- `COST_ratio(C95/C5)` 是比值不受复权漂移影响，可长期复用

### 31. 财务防未来函数用 ann_date（CM）
- 用 `ann_date`/`f_ann_date` 对齐行情，**不能用 `end_date`**（否则用未来财务数据）

### 32. knowledge_base 是 NTFS junction（CM）
- 原 QMT_STRATEGIES 工程下 `knowledge_base` 是 junction，target 为 `F:\天翼云盘同步盘\Obsidian\量化知识库`
- 不要 git add / rm -rf / 纳入打包构建；中文路径在 cmd/Python stdout 显示乱码是终端 codepage 不匹配，不是路径错

---

## 回测与研究（CM）

### 33. look-ahead 是最大杀手（击中 4 次）
- 复权 look-ahead：后复权用 `groupby("code")["factor"].transform("last")`，2019 年价格含 2020-2023 分红 → 全因子污染（P0 H12 教训）
- 宇宙选择 look-ahead：静态 top 池（如 305 池）套到全历史周期 → "池子>>参数"结论被污染
- 撮合 look-ahead：1d 频率盘中取 close = 当日收盘价，却按盘中撮合
- **修根因优先，不要只打 caveat**

### 34. 盘中 vs 尾盘对照是验证 look-ahead 的干净方法（CM）
- 同策略两口径（盘中/尾盘），差异 >30pp 且方向反转 = look-ahead（黄氏双中军：盘中 +64% vs 尾盘 -9%，74pp 差异坐实）

### 35. 撮合时点是策略代码职责，回测工厂只提供 next_open（CM）
- 信号 @ T 收盘 → 成交 @ T+1 开盘（next_open 唯一撮合）；要别的时点在策略层自己用 bar["close"]/["open"] 决策
- **Why**: 工厂预制成交时点会让回测与实盘语义割裂，一套逻辑两处维护
- close 买/next_open 卖等隔夜策略走独立脚本，不要塞工厂

### 36. 回测工厂不适合事件型策略（CM）
- 组合型（评分排序+max_positions+replace+stop_loss）→ 工厂 Registry
- 事件型（条件触发+固定持有期+信号日后 N 日强制卖）→ 独立脚本
- 判断: 策略是否需要"基于当前持仓和新信号做替换决策"？是→组合型，否→事件型

### 37. 长回测后台跑，不占会话（CM）
- `nohup python -m ... > log 2>&1 & disown` 让进程脱离 shell；跑完再看日志/结果
- 不轮询、不 tail 中间日志——浪费 token
- 注意: 部分工具环境的 bash 会杀 `nohup &` 子进程，此时用工具的 run_in_background 托管

### 38. 仓位/风险公式要数学验证（CM）
- 海龟 SPEC 笔误: `单笔风险/(2×ATR/当前价×100)` 数学推导得 `2×ATR×shares=100×价`（与 ATR 无关），单笔风险=100×价、占比=价/10000 → 0.31% 而非声称的 1%
- 代入典型值看单笔风险是否=声称值

### 39. 多因子 A 股近 5 年证伪（CM）
- 462 因子 × 3 universe（大盘/中盘过拟合 2024-2026 牛市、小盘训练集有效但样本外失效）
- 最稳健分散设计全期（2021-2026）实测 -41.6% 总收益 / -7.9% 年化 / -48.7% 回撤 / -32.3% 超额 vs 沪深300
- **结论**: 分散框架下"年化 50% 多因子"是主动毁灭价值；要摸高收益只能集中+杠杆+事件驱动
- harness 留作因子验证工具（IC 训练集→样本外），能科学证伪因子

### 40. 追涨隔夜负期望（CM）
- 尾盘 30 分钟选股法全 A 回测 -92.63% 归零，胜率 21.79%
- 根因: 强势股 T+1 平均低开 0.70%，75.6% 交易 T+1 开盘低于 T 日收盘——追涨隔夜方向错，非参数非 bug
- **How**: 见"买强势股做隔夜"类策略，先验隔夜跳空分布（sell_open/buy_close-1），平均负就是方向错，别跑全回测

### 41. 海龟 A 股保本微赚（CM）
- 标准海龟 1% 风险，沪深300/2022-2025：年化 0.5%，胜率 34%，100% 跑赢基准但基准 -19.96%——不可作"海龟有效"证据
- 前复权优先用 astock（adj_factor 内置），避免 mootdx 自实现除权有 look-ahead 风险

### 42. 单笔 t 值无法识别时序集中风险（CM）
- 529 组合 alpha 本质 = 2024 年 9-10 月政策行情连板股 beta 暴露，时序集中不可分散、不可复制
- **方法教训**: 任何"单笔统计显著"的策略都要验证 alpha 时序分布；必须看组合净值 + 月度超额，剔除极端月份后年化转负

### 43. 回测工厂用 Python310（CM）
- 系统 python / git-bash python 没有 duckdb；项目 `.venv` 是 Linux 风格不可用
- 用 `/c/Users/Administrator/AppData/Local/Programs/Python/Python310/python`
- 读 summary.json 要 `encoding='utf-8'`（终端 codepage 问题，文件本身是 UTF-8）

---

## 数据细节

### 44. astock parquet 日期是 datetime.date 类型
- **现象**: `df[df['trade_date'] == '2026-06-18']` 返回空
- **原因**: trade_date 列是 datetime.date 对象，不是字符串
- **修复**: 用 `df[df['trade_date'] == date(2026, 6, 18)]`
- **日期**: 2026-07-17

### 45. circ_mv 单位是万元
- **现象**: circ_mv 值 700000 以为是元
- **原因**: astock parquet 中 circ_mv 单位是万元
- **换算**: 30亿 = 300000 万元
- **日期**: 2026-07-19

### 46. suspend_type 值
- **现象**: `suspend == ""` 过滤无效
- **原因**: 空字符串不是有效值，有效值是 'N', 'S', 'R', 'R&S'
- **修复**: 用 `~suspend.isin(["S","R","R&S"])`
- **日期**: 2026-07-17

---

## 策略研究

### 47. filter_func 必须传入 scorer.score() 内部
- **现象**: 外部过滤 codes 列表后年化只有 4.6%，正确用 filter_func 后年化 15.5%
- **原因**: filter_func 在 scorer.score() 内部应用，会正确处理评分和排序；外部过滤丢失评分信息
- **正确用法**: `mv_func = lambda p, f, d: (p.loc[d]['circ_mv'] > 0) & (p.loc[d]['circ_mv'] < 300000)`
- **调用**: `backtest_stop_loss(panel, fin_ffill, top_n=80, freq='2M', stop_loss=-0.12, filter_func=mv_func)`
- **日期**: 2026-07-22

### 48. env_scorer 择时对小盘alpha有害
- **现象**: env_scorer 实验组收益比 baseline 差 15.87%
- **原因**: 小盘alpha是持续结构性溢价，择时减仓错过反弹
- **结论**: 暂停 env_scorer 研究
- **日期**: 2026-07-22

### 49. Open定价 vs Close定价差异巨大
- **现象**: 双月TOP20 年化从 7.9%(close) 坍塌至 0.6%(open)
- **原因**: close→open 差异 -1.2%，策略对成交价假设高度敏感
- **结论**: v3.0 用 close 定价做基线
- **日期**: 2026-07-19

### 50. pandas to_period("2M") 不是双月
- **现象**: `s.dt.to_period("2M")` 返回月度周期
- **原因**: pandas 的 "2M" 不是双月，行为与 "M" 相同
- **修复**: 用 `s.dt.year * 24 + s.dt.month // 2` 手动分组
- **日期**: 2026-07-17

### 51. Project_01 全面审计：零超额 = 小市值 beta（2026-08-01，CC 证伪再次验证）
- **审计方法**: 代码逐层审计 + 幸存者偏差实证 + 财务 PIT 实证 + 分时段 IC + 训练集/样本外回测 + 小盘等权基准对比
- **决定性证据**: 2024+ 策略累计 +153.5% vs 小盘等权基准 +154.2%（超额 -0.7%）；2026 年超额 -7.3%；综合评分 IC 训练期 0.101 → 样本外 0.052 → 2026 转负 -0.118
- **结论**: 全期 412% 收益 = 小市值 beta 暴露，非因子 alpha；与 07-04 多因子证伪归档结论一致。不建议实盘
- **硬缺陷清单**（都高估收益）:
  1. 幸存者偏差残留：`load_universe()` 按最新日 top4000 建池，2018-2026 退市股 249 只全部被排除（249/249）；退市股最后市值中位 3.6 亿，全在小盘候选内 → 小盘策略高估最重
  2. ROE 财务前视：fina_indicator 有 `ann_date` 但代码用 `end_date+45天`；实际披露滞后中位 53 天/年报 88 天 → 年报提前 1-2 个月被使用；且 ROE 因子 IC≈0（去 ROE 年化 19.5%→21.7%）
  3. `dynamic_universe=False`：滚动市值池（301~1800）与小盘过滤（<30亿）交集≈0 从未生效，P0 修复未进主入口
  4. 无涨跌停处理：一字涨停买不进被假定可成交
  5. VWAP 因子含当日数据，与其他因子口径不一致（轻微乐观）
  6. 主回测无止损（AGENTS.md 风控底线 8% 未体现）；净值仅调仓点记录，回撤低估
- **2026 年风险信号**: 2026-05 起候选不足 80 只（小盘涨出 30 亿/不在池），策略"无票可买"
- **日期**: 2026-08-01

### 52. 幸存者偏差量化实证：4000 宽池 19.5% vs 全股票池 10.1%（2026-08-01）
- **方法**: 宽池从"最新日市值 top4000"改为"2018 后窗口内全部出现过的股票"（5770 只，含 249 只退市股），重跑全部回测，对比同池等权基准（<30亿+PE>0+PB>0，调仓日次日开盘买入等权）
- **量化结果（全期 2018-2026）**:
  - 5 因子全池：年化 10.1%（原 4000 宽池 19.5%），超额 +4.0%/年；2024+ 超额 **-4.6%**，2026 超额 **0.0%**
  - 单因子（全池口径）年化超额：**BP +6.1%**（唯一正 alpha，2018-2025 每年正超额 8/9 年）/ 低波 -0.7% / **反转 -9.0%** / **ROE -11.1%**
  - 分年度超额（5 因子）：2020 -6.1 / 2021 +7.0 / 2022 +12.2 / 2023 +11.6 / 2024 -6.1 / 2025 +3.1 / 2026 -0.0 → 超额集中在 2021-2023，2024+ 转负
- **结论**: ①幸存者偏差贡献约 9.4pp/年虚高；②超额唯一来源是 BP（价值），反转/ROE/低波全是拖累（5 因子 = BP + 三个负贡献）；③BP 2026 也转负（-3.7%）→ 全策略无实盘价值
- **备注**: backtest 对退市/停牌票 `continue` 静默跳过（不计亏损），全池结果仍偏乐观
- **日期**: 2026-08-01

### 53. 流动性过滤在市值分层内是"负筛选"（2026-08-01）
- **现象**: 小盘池内加"日均成交额>3000万"过滤后，同池等权基准从 +47.5% 塌到 -8.1%（58 期累计）；再加质量排雷到 -13.8%
- **原因**: 小盘池内"高流动性"≈"被资金炒作的热门小票"，2018-2023 壳价值/小盘行情里冷门低流动性小票涨幅更大 → 3000 万阈值实质做了负向筛选，且 V2 策略"超额+12.8%"大部分是基准塌陷的假超额
- **修复**: 策略与基准必须严格同口径过滤；流动性阈值对小资金（10万级）用 500-1000 万即可（V2-D 500万与 V2-C 1000万 结果一致：2024+ 超额 +6.7~6.8%）
- **日期**: 2026-08-01

### 54. 价值小盘 V2 验证通过：行业中性 BP + 历史分位 + 质量排雷（2026-08-01）
- **方法**: 全股票池（5770 只含退市）+ 同条件等权基准（策略与基准同一过滤集）；因子 = 行业中性化 logBP（50%）+ BP 36 月历史分位（50%）；质量排雷 = bps>0 & 扣非净利润>0 & roe>0（ann_date 真 PIT）
- **结果**: 全期年化超额 **+9.9%**（策略 203.4% vs 基准 40.9%）；**2024+ 超额 +5.8%/年**（vs 原5因子 -4.6%、BP单因子约+1.5%）；9 年 8 正超额，仅 2026 负（-2.6%）
- **分年度超额**: 2018 +5.7 / 2019 +2.9 / 2020 +8.7 / 2021 +9.5 / 2022 +23.9 / 2023 +17.0 / 2024 +3.4 / 2025 +18.1 / 2026 -2.6
- **结论**: ①修正版 BP 是全池口径下唯一稳健 alpha（全期 +8.6%，比 raw BP +6.1% 提升 2.5pp）；②ROE 做排雷（非打分）显著提升样本外（2024+ 从 +1.5%→+5.8%）；③与 CC 证伪不冲突——证伪的是"多因子"，价值单因子在小盘内有据可依；④2026 转负提示仍需仓位/风格择时，待立项
- **日期**: 2026-08-01

### 55. V2 三项硬验证通过：成本/涨跌停/鲁棒性（2026-08-01，audit12_hard.py）
- **方法**: 状态机模拟（调仓日次日 E 日先卖后买、E 日 open 成交、持仓跨期延续盯市基价、涨跌停顺延/跳过）+ 成本敏感性 + 牛熊/压力窗口 + 容量估算
- **状态机收益时序陷阱**: 首版把"跨期持有票的累计收益"当单期收益复利（结果 -63% 荒谬）；盯市版又踩"新买票当期收益=0"坑（69% 换手的新票当期无收益，结果 -4.4%/年）；正确 = 期 i 结算上一期末持仓的区间收益 [E_{i-1}→E_i]，交易与结算分两阶段
- **成本敏感性（单边成本，含涨跌停）**: 0.0005 超额+14.1% / 0.001 +13.9% / 0.002 +13.3% / 0.003 +12.8% / 0.005 +11.7% → 成本不敏感，平均换手 0.69
- **涨跌停对比（0.001）**: 处理 18.2% vs 忽略 19.0% 年化（差 0.8pp，可忽略）
- **牛熊/压力（0.001+涨跌停）**: 牛市2019-21 超额+25.4% / 熊市2022-23 +28.2% / 震荡2024-25 +62.7% / 2026至今 +14.7%（此口径含顺延与持仓延续，vs 逐期独立口径 2026 -2.6%）；**2024Q1 微盘崩盘 +51.5%、2026Q2 微盘暴跌 +14.1%** → 压力窗口不崩
- **容量（持仓80只，最新一期）**: 10万~1000万资金单票占日均成交额 P90 0.007%~0.748%，全部 <1% → 1000 万内无冲击成本问题
- **幸存者偏差确认**: 全池 5770 只含退市 228 只已纳入回测
- **结论**: 换手 0.69/期、涨跌停影响 ~0.8pp、压力窗口全正超额 → 状态机口径全期年化 18.2%、超额 +13.9%（比逐期独立 +9.9% 高，因换手低），三硬验证全部通过
- **⚠️ 勘误（2026-08-02，audit14）**: 本条目的 run_v2 存在"收益结算日误标记为下期 x_date"bug（收益序列滞后一期）。修正后三口径 2026 超额收敛一致（-1.3%~-1.8%，差距 0.5pp），本条目的 "2026 +14.7%、压力 2026Q2 +14.1%" 是假象（跨年持仓收益被误计入 2026）；修正后 **2024Q1 +3.1% 仍正、2026Q2 -2.0% 转负**。成本敏感性/涨跌停对比/容量不受日期位移影响，结论保持。全期年化超额修正口径约 16.5%（状态机延续口径）
- **日期**: 2026-08-01（勘误 2026-08-02）

### 57. V2 因子权重微调：降 hp 升 z 全面改善（2026-08-02，audit17_权重.py）
- **背景**: audit16 发现 V2 评分 = 0.5×行业中性BP(z) + 0.5×历史分位(hp)，hp 在 2024+ 已失效（Q5-Q1 仅+2.2%），2026 反向（-7.8%）；z 在 2024+ 仍有效（+63.9%）
- **方法**: 测试五种 z/hp 配比，各跑状态机 run_v2，对比全期/2024+/2026 超额
- **结果**:
  - z=0.5/hp=0.5（原版）：全期+208.1%、2024+ +29.6%、2026 **-1.3%**
  - z=0.6/hp=0.4：全期+227.4%、2024+ +38.8%、2026 -0.7%
  - z=0.7/hp=0.3：全期+239.5%、2024+ +40.4%、2026 -1.3%
  - **z=0.8/hp=0.2：全期+241.6%、2024+ +43.7%、2026 +0.1%（唯一转正）**
  - z=1.0/hp=0.0：全期+258.1%、2024+ +46.2%、2026 -0.2%
- **结论**: 降 hp 权重全面改善——全期超额随 hp 降低单调递增（+208→+227→+239→+241→+258），2024+ 超额也单调递增。z=0.8/hp=0.2 是唯一让 2026 转正的配比，且全期超额反升 +33pp。**V2 微调版可立项**
- **注意**: 2026-06 单月 -7.4% 仍存在（z=0.8 比原版 -8.7% 略改善），说明 2026 转负不仅是 hp 问题，还有事件冲击成分
- **日期**: 2026-08-02

---

## Python 环境

### 51. pandas 3.0 DataFrame 构造器陷阱
- **现象**: `pd.DataFrame({'col': series}, index=dates)` 值全为 NaN
- **原因**: pandas 3.0 改为索引对齐赋值，Series 默认 RangeIndex 不匹配
- **修复**: 用 `pd.Series([...], index=dates)` 或直接用 list
- **日期**: 2026-06-23

### 52. PowerShell 中文编码
- **现象**: PowerShell here-string 中嵌入中文导致文件内容变 `?`
- **原因**: PowerShell 编码处理问题
- **修复**: 用 Python 写文件，避免 PowerShell 直接处理中文
- **日期**: 2026-07-01

---

## 测试与协作约定（CM 迁移，opencode 开发约定）

### 53. MOCK 只测信号逻辑，测不到 QMT 集成层 BUG
- 8 成 bug 在集成层（时序/异步/字段/文件/生命周期），MOCK 一个抓不到
- MOCK 按文档/猜测写错的字段反而埋新坑（如按文档写 `m_dTotalAsset`，实盘根本没有）
- **A 方案**: 模拟端验证流程化（部署实盘前强制跑 1 交易日过 8 项 checklist）
- **C 方案**: 可观测性日志（时段/导出/init/对账），部署后快速定位集成层问题

### 54. 大量修复后必须跑测试
- 一次任务含多处代码修复/跨文件修改/评审后修复/重建产物时，完成前默认运行相关测试；影响面大优先全量 pytest，如实汇报失败

### 55. 测试报告记录 warning 分类
- 不只写 warning 数量，补充主要类型（PytestReturnNotNoneWarning、DeprecationWarning、第三方依赖等）和大致来源文件

### 56. 任务完成必须明确汇报
- 以清晰结论开头：完成了/未完成/部分完成；列出 commit、验证结果、是否有未完成尾巴
- 失败测试或历史 dirty 必须区分"本任务相关"与"无关历史遗留"

### 57. 遇异常必停，不得自判"无关"
- 任何 stderr / 非 0 退出 / Unicode 异常 / 报错堆栈，都必须停下报告，等确认后再继续或重跑
- 结论恰好对，不代表流程合法

### 58. 可视化配色用 A 股惯例红涨绿跌
- CSS 变量 `--pos: 红(#ef4444)`、`--neg: 绿(#22c55e)`；净值/回撤/KPI 数字全部红涨绿跌
- 不只改 CSS 变量，Chart.js dataset 里硬编码 borderColor/backgroundColor 也要逐个改

### 59. 不自动往知识库 70_Agent协作记录 同步报告
- 报告只放工程区（projects/Project_XX/）；只在明确交代"放知识库/给评审"时才复制

### 60. 冻结期/观察期代码任何理由不能动
- 观察期/冻结期内的代码不允许修改（除非红线触发回退）；"看起来合理的延续任务"不是动冻结代码的理由

### 56. V3 风格择时三信号全部证伪：2026 转负修复代价不可接受（2026-08-02，audit15_择时.py）
- **方法**: 在 V2（状态机延续+顺延、单边成本 0.001）上叠加择时开关（signal 0=空仓/1=满仓，空仓期收益=0 参与连乘、清仓/建仓计成本、只用 d 及以前数据 PIT），三信号 + 组合（任一触发即空仓）
- **S1 BP滚动IC**（近 8~12 个已完结调仓期收益与 BP 分位 spearman 秩相关，均值<0 空仓）: 2026 超额 **+3.8%**（唯一拉正）但代价巨大——靠 2026 全年空仓（4/4 期空仓）换得；全期超额从 +208.1% 塌到 +49.0%（相对 V2 损失 -159.1pp），2024+ 从 +29.6% 转负 **-19.1%**，牛市 2019-21 从 +30.9% 转负 -10.0%（空仓 14/21 期）
- **S2 候选池动量**（60 日等权收益<0 空仓）: 全期超额 -37.4%，2026 仍 -0.7%，牛熊市全毁（牛市 -54.4%）→ 动量过滤与价值小盘互斥，直接淘汰
- **S3 拥挤度**（候选池平均成交额 36 月分位>0.9 空仓）: 全期超额 +179.6%（最接近 V2，仅损失 -28.5pp），但 2026 超额仍 -1.3% 未解决，且 2024-25 的 4 次空仓全错过正收益 → 无效
- **组合**: 空仓 42/58 期，全期超额 -57.3%，灾难
- **PIT 陷阱（本轮新踩）**: S1 首版用 p["e_date"] < d 过滤历史期，但最近一期收益要算到 x_date=d 次日开盘才完结，决策时该期收益未实现（1 天前视）；改 p["x_date"] < d 后 S1 全期累计 172.8%→89.9%、空仓 23→22 期。教训："收益已完结"必须以**结算日 x_date** 判定，不能以入场日 e_date
- **清仓成本陷阱（本轮新踩）**: 状态机 signal=0 清仓时 prev_cost 已计提但空仓期行固定 0.0 不扣 → 空仓行应为 -prev_cost；首版 S2 2026-01-05 空仓行收益 +0.0% 实为 -0.1%（80 只清仓 × 0.001）
- **结论**: 三信号择时都无法在不显著损害全期/2024+ 超额的前提下修复 2026 转负；S1 修复 2026 的代价（全期超额腰斩、2024+ 转负）不可接受 → **V3 择时增强路线证伪，不建议立项**；2026 转负可能非"价值因子失效"（S1 的 BP IC 持续为负本身即证据），但需另寻思路（如仅在 IC 连续 N 期为负时降仓而非全空仓，或换非价值信号）
- **日期**: 2026-08-02

## 58. 2026-08-02 17:50 -- audit14 微调版重跑确认(z=0.8/hp=0.2)

- **任务**: audit14 权重修改后重跑基线, 验证与 audit17 一致
- **执行**: opencode 直接执行(单脚本~200s)
- **输入**: audit14_修正口径.py(已由 DE 改为 z*0.8+hp*0.2)
- **输出**: audit14_结果.txt

### 核心数据

| 指标 | 原版(0.5/0.5) | 微调(0.8/0.2) | 变化 |
|---|---|---|---|
| 全期超额(状态机) | +208.1% | **+241.6%** | +33.5pp |
| 2024+超额(状态机) | +29.6% | **+43.7%** | +14.1pp |
| 2026超额(状态机) | -1.3% | **+0.1%** | +1.4pp |
| 牛市超额 | -- | +35.0% | -- |
| 熊市超额 | -- | +50.2% | -- |
| 震荡超额 | -- | +45.2% | -- |

### 结论

1. audit17 数据完全确认: 全期+241.6%/2024+ +43.7%/2026 +0.1%
2. 微调版正式可立项: 2026 转正(+0.1%), 全期超额 +33.5pp
3. 状态机口径最优: 延续+顺延模式超额最高(+241.6%), 换手率最低(0.68)
4. 压力窗口全正: 2024Q1 +7.3%, 2026Q2 -0.3%(基本持平)
5. 事故备忘: 本次重跑前日志因编码冲突被清空, 已从 opencode.db 恢复 411 行/66 条

## 59. 2026-08-02 18:10 -- Project 10 V2微调版正式立项

- **任务**: 价值小盘V2微调版(z=0.8/hp=0.2)正式立项
- **执行**: opencode直接执行

### 产出

- config/strategy.yaml — 策略参数(含风控)
- strategy/scoring.py — V2Scorer评分模块
- strategy/risk.py — RiskController风控模块(止损8%/回撤15%/持有60天)
- runner.py — 回测入口(状态机+风控)
- qmt_strategy_v2.py — QMT单文件策略(GBK/Python3.6)
- PROJECT_README.md — 立项文档

### 回测结果(含风控)

| 指标 | 值 |
|---|---|
| 全期累计超额 | +174.2% |
| 全期年化 | 15.1% |
| 2024+超额 | +30.4% |
| 2026超额 | -0.2% |
| 平均换手 | 0.91 |

### 结论

1. 策略代码完成，审计基线确认(+241.6%/17.9%)
2. 含风控后年化15.1%(风控成本~2.8pp)，合理
3. QMT单文件GBK编码通过验证
4. 待办: 预生成CSV数据管道 + 模拟盘验证

### 60. 因子IC实证数据已全面汇总归档（2026-08-05）

- **背景**: 因子动物园扩展讨论中，翻遍本地研究资产发现3套独立IC验证已跑过，且互相印证，无需重跑
- **已跑过的IC验证**（详见`研究总览与路线图.md`第二节）:
  1. **P01 IC统计**（中证500+1000, 100期月频）: 11个因子完整IC/ICIR，低流动性最强(-0.687)、反转-0.567、低波-0.524、BP 0.448、ROE -0.123
  2. **harness 462因子×3 universe**: 大盘/中盘训练集0有效（全期=牛市过拟合）；小盘训练集11有效但样本外-24.54%；基本面6因子全期+训练集ICIR<0.5
  3. **稳健多因子实盘全期证伪**: 中盘1200只+6训练集因子+中性化+风控，全期-41.6%/-32.3%超额
  4. **P01 0-30亿回测参数矩阵**: 最优17.1%年化（双月TOP80止损-12%+2000万阈值）
  5. **6+2评分器纯选股alpha**: 测试集IC=-0.2239（反预测）
  6. **有效因子库5年spread**: ATR%<6最强(+0.313%)，29个技术因子全中性或负
- **关键教训**: IC有效≠回测有效；加因子≠加收益；BP是唯一稳健alpha
- **史料位置**: `archive/legacy_qmt_strategies/research/multi_factor_ic/reports/` · `F:\天翼云盘同步盘\Obsidian\量化知识库\40_回测与实验\` · `D:\QMT_STRATEGIES\Project_12_6加2评分器优化\`
- **日期**: 2026-08-05

### 61. "加因子提升策略"的实证上限（2026-08-05）

- **P01 ROE→低流动性替换**: ICIR从0.74→0.82（理论），但harness已证明小盘IC有效到回测无效，此提升未必落得了地
- **P10加反转+低波**: IC理论预估+2-4pp（→18-20%），但无0-30亿小盘实测数据支撑；harness前车之鉴：中证500+1000反转ICIR=-0.56很漂亮，0-30亿样本外-24.5%
- **ATR加因子**: 过滤型策略非评分型，加因子=改架构，风险大收益不确定
- **唯一确定性收益**: P01的ROE替换（改权重零风险），但估计仅+1-2pp且P01天花板约15%
- **结论**: 基于已有IC数据，"加因子"路线提升空间有限（1-3pp），不值得大工程投入；应聚焦P10 V2a上实盘和ATR运维
- **日期**: 2026-08-05


---

## RPS 主升浪研究教训（2026-08-14 归档）

### 58. 候选池枯竭是趋势选股的第一杀手
- **现象**: RPS>90 + 板块前5 + 回调买入 三重过滤叠加后，`candidate_passed_avg_per_day=0.18`（每天只通过 0.18 只）
- **后果**: 持仓高度集中 → 收益 = 单票运气（2020 单独跑 +95% 实为满仓押中 002626 暴涨 2.6 倍，全周期跑根本没选中它 → -22%）
- **诊断方法**: 所有策略必检 `candidate_passed_avg_per_day`，若 < 1 说明候选枯竭，结果不可信
- **教训**: 过滤层叠加会让候选指数级萎缩，宁可放宽条件保证候选池充足（≥ 几十只/天）

### 59. "单独区间回测 +95%" 必须全周期复核 + 单票集中检查
- **现象**: 单独跑 2020-2021 显示 +95%（看似优秀），全周期 2019-2025 显示 -60.8%（真相）
- **原因**: 单独跑起始资金 100 万 vs 全周期继承 2019 亏损后的 90.8 万，持仓状态不同 → 选股路径分叉 → 单票集中度天差地别
- **教训**: 短区间高收益必须检查 (a) 全周期 PIT 复核 (b) 是否单票集中 (c) 持仓数是否过少；三者任一异常即不可信

### 60. 引擎 sizing 语义：equal 无视传入权重
- **现象**: 等权模式 `base = {c: 1/len(sel)}` 无视策略传入的权重，持仓因止损减少时，资金全部分给剩余票 → 单票放大到满仓 → 加仓到高位 → 止损 -8% 保护不了（单票 -23.7% 都没触发）
- **修复**: 改用 `position_sizing: custom`，策略对已持仓票传"当前市值占比"（引擎不动它），新票分剩余资金
- **教训**: 使用框架组合层前必须先读清 sizing 语义，否则策略权重意图被覆盖

### 61. 移动止盈必须用入场后最高价
- **现象**: 用当日最高价算回撤 → `drawdown = (last-today_high)/today_high` 永远接近 0 → 移动止盈永不触发 → 只止损不赚钱（胜率 59% 却亏 46%）
- **修复**: 用 entry_date 之后的历史最高价（`_peak_since_entry`）
- **教训**: 止盈逻辑的"最高价"必须是持仓期间最高价，不能是当日

### 62. 非调仓日不得返回 target_weights
- **现象**: `_hold_decision` 无触发时返回 `target_weights: keep` → 引擎每天做 diff → 疯狂加仓（1216 笔交易/2年）
- **修复**: 无触发时不返回 target_weights（引擎保持持仓不动），只有触发退出时才返回
- **教训**: target_weights 是"目标组合"，只在需要变更时返回，否则引擎会持续 rebalance

### 结论
RPS 主升浪独立选股经 PIT 回测证伪（与通宵研究一致：趋势选股不可行）。可复用价值：板块 RPS 作择时/轮动信号、大盘门控（000300>MA60）、候选池枯竭诊断指标。完整报告：`projects/Project_12_RPS主升浪/results/RPS主升浪回测研究报告_归档结题.md`


---

## RPS 加速度因子独立验证（2026-08-14 证伪）

### 63. 提取因子必须独立 IC 验证，不能盲信原策略的局部增量
- **现象**: MCRPS 策略回测显示 RPS 加速度因子（ΔRPS20）贡献 +4.62pp（2024-2025 两年），看似有效；提取为 `factors/rps_acceleration.py` 后，用真实 A 股（500只, 2023-2025, astock qfq）独立测 IC，**两种实现（排名差/涨幅加速度）IC 均为负**（-0.0154 / -0.0097 @20日，ICIR<0.15）
- **原因**: MCRPS 的 +4.62pp 是 2024-2025 特定两年 + 牛市场景的局部现象，不具备全周期稳健性
- **教训**: 从某个策略里"看起来有效"的因子，必须用独立全周期 IC 验证后才能复用；原策略的回测口径（区间/场景）可能掩盖因子真实预测力
- **结论**: RPS 加速度因子在 A 股全市场无预测力，与通宵研究"动量/趋势因子 IC 全负"一致。文件保留为方法参考，不作投入因子


---

## ATR 10万真钱加价格过滤回测（2026-08-14）

### 64. 价格/资金类过滤必须用"真实价"，引擎全程在 hfq 复权价上运行
- **现象**: 引擎成交/估值/整手折算全部基于 hfq 复权价（如建设银行真价 7 元 → hfq 23.8 元；平安银行真价 10.49 → hfq 1224）。若直接 `hfq价 < 50` 做"股价<50元"过滤，会把分红送转多的高价股误放行、把低分红的长牛股误排除，口径完全失真。
- **修复**: 真实价 = 复权价 / adj_factor（`astock_reader.py` 窗口透传 `adj_factor` 列；`atr_lowvol.py` 新增 `max_price` 用真实价过滤）。验证：平安银行 1224.32/116.71=10.49 ✓。
- **教训**: 任何涉及"具体价格/资金可用性/整手"的过滤或约束，都必须换算回真实成交价，绝不能拿 hfq/qfq 合成价直接比。

### 65. 引擎 target_weights 转换会静默丢弃策略侧 strategy_specific 诊断
- **现象**: 策略在 decision.diagnostics 塞的 `strategy_specific`（如 price_filtered 计数）在 `_evaluate_day` 返回后，被引擎 `target_weights_to_decision` **整体重建 decision** 而丢失，summary 只显示 `_present:true` 甚至空。
- **修复**: `backtest/engine.py` 在转换前取出 `strategy_specific`、转换后回填；且策略侧诊断须用框架约定的 3 层结构 `{sname: {metric: {inner: value}}}` 才会被 `_aggregate_strategy_specific` 聚合成 `_avg_per_day`。
- **教训**: 用框架的 diagnostics 扩展时先读聚合约定（dict 三层结构），且任何"决策被下游重建"的链路都要验证诊断是否穿透。


---

## ATR 双策略隔离审计修复（2026-08-14）

### 66. 改 GBK 生产文件：`open("w")` + 编码异常会把目标文件截断为 0 字节
- **现象**: 用 `io.open(path, "w", encoding="gbk")` 改写 ATR build（GBK）时，写入的中文注释含 `↔`（U+2194，GBK 无法编码），`f.write(text)` 抛 UnicodeEncodeError。`"w"` 模式**先截断文件**再写，异常发生后目标文件被留成 0 字节——部署明文源整份丢失。
- **根因**: 1) 注释里用了 GBK 不支持的字符（`↔`/`→`/emoji 等）；2) 直接 `open(path,"w")` 覆盖写，没有"先写临时文件、成功再替换"的保护。
- **恢复**: 靠 `__pycache__/*.pyc`（损坏前的编译产物）用 marshal 提取全部函数/类名（30 个），与从上下文重建的源码逐一比对（missing/extra 均 NONE），确认转录无误后才写回——pyc 是"文件被破坏时最可靠的函数级校验源"。
- **修复**: 所有 GBK 改写统一改**原子写入**：写 `path + ".new"` → `os.replace(".new", path)`；注释禁用 GBK 不兼容字符。
- **教训**: 任何对"生产/部署明文源"的自动改写，一律先写临时文件 + os.replace；中文注释只用 GBK 安全字符；改前最好留 `.bak`。


---

## 审计修复任务书 T-20260816-001（2026-08-16）

### 67. 框架年化口径是"线性年化"不是 CAGR，长期回测被系统性虚高 1.4~1.7 倍
- **现象**: 外部审计复核 MAX5（`atr_10w_price50_a_max`）与 P13 参数矩阵，逐项可复现，但发现框架 `annual_return = total_return * (252/ndays)` 是**线性年化**，非复利 CAGR。MAX5 标称 32.99%、真实 CAGR 仅 18.40%（比 1.79）；P13 n16 标称 11.42%、真实 8.69%。`calmar = 线性年化/|mdd|` 同步虚高（MAX5"1.65"实为 0.92）。
- **根因**: 引擎 `backtest/analyzer.py` 只用线性年化单口径，未暴露复利口径；研究侧 `backtest_atr_lowvol_v3.py` 手写真 CAGR，两套口径在全仓库被并排比较（如"状态机 18.2% vs 框架 28.99%"），相对比较有效、绝对水平系统性虚高。
- **修复（T-20260816-001 T1）**: `analyzer.py` 新增 `cagr=(1+total_return)^(252/ndays)-1` 与 `cagr_calmar`（**全精度存储**，不 round，满足 <1e-9 独立验算），保留旧 `annual_return/calmar` 字段；`report.py` 指标表双口径（年化(线性)/年化(CAGR)/卡玛(CAGR)）；全仓库读引擎 summary 的报告脚本改 CAGR 主口径+线性括注；`研究总览与路线图.md` 与受影响历史报告顶部加勘误块。
- **教训**: 1) 收益"年化"必须声明口径——线性 vs 复利；长期（>2 年）回测结论一律用 CAGR，线性只能作辅助。2) 任何"跨文件并排比较"的指标必须先统一口径，否则绝对水平失真。3) 新增字段验收要「独立验算 diff=0」，round 到 6 位会放大到 ~1e-4 触发不了 <1e-9 的干净验收。

### 68. 看板记录的"已修复"必须以产物实际代码验证，审计修复可能只改了记录没改代码
- **现象**: 2026-08-16 双策略隔离核查（T-20260816-002）发现 T-20260814-006 看板记录"R1 已落地：V2 买入兜底改 `min(acct_vol, od['amount'])` 按当日下单量封顶"，但 build `20260814-210622` 与源 `strategy_v2.py` 实际仍是 `_holdings[code] = acct_vol`——把共享账户内他人（ATR）同 code 的持仓份额纳入 V2 虚拟 ledger。
- **风险**: ATR 上线后与 V2 同 code 重叠持仓 + deal 反查为空（模拟端漏记）→ 兜底把 ATR 份额记入 V2 ledger → V2 后续卖出该 code 时卖超自己份额 → 误卖 ATR 份额、资金串账。
- **修复（T-20260816-002）**: 源买入兜底改 `_holdings[code] = min(acct_vol, od["amount"])`（R1 口径），日志加 min封顶标记；重建 build `20260816-150356`（GBK/LF/py3.6 核验 PASS，备份 `.bak_20260816-150221`）；7 项针对性断言 PASS（重叠 3000→1000 / 部分成交 800 / 满额 1000 / 卖出兜底不纳管他人份额 1500→恢复自己 500 / 现金净变化），V2 MOCK 48/48 仍过。
- **教训**: "修复任务"的验收必须是产物实际代码 + 针对性断言测试，不能只信看板/文档记录；若任务跨天且涉及重建构建产物，先 diff build 与源确认同步，再谈"已落地"。
