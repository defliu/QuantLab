# MCP 数据链路说明

> 适用范围：QuantLab 全部项目，重点是 Project_16_LightGBM股票大师 的实盘链路
> 数据依据：2026-08-26 全量连通性实测 + 2026-08-27 v4 优先级 + 2026-08-28 增量目录接入 + **2026-08-30 晚 TDX 服务端恢复（远程 tdx-connector 与 full-link 内置 TDX 均恢复可用，`tdx_lookup_stock`/`tdx_quotes` 实测通过）；此前 2026-08-30 下午复核的 full-link 端点下线已解除**
> 结论速览：**链路健康，双闭环覆盖，无单点依赖；8 级降级链全部在线（full-link 与独立 TDX 均已恢复）；有 3 处需留意的高危降级点（资金流/竞价/东财反爬）；主力源 mcp_tdx 稳定在线**

---

## 一、链路全景图

当前数据体系分两条并行链路：

```
【实时链路】（盘中，4 个定时任务）
  mcp_tdx → 腾讯 → iFind → 东财mx → 东财curl → full-link → 独立TDX → miniQMT
      ↑基础行情    ↑独有字段    ↑金融主力    ↑资金流    ↑冗余位
  + 悟道（信息面，与行情链并行）
  + 万得 / 盈米 / TuShare / AKShare / tradingagents（备用深研）

【离线链路】（训练/回测/因子构建）
  D:\astock 主仓（2009 至今）
  + D:\astock\Updatedata 每周增量（按需合并，read_main_daily 读取）
```

两条链路互不依赖：实时链路靠 MCP/HTTP，离线链路靠本地 parquet。实时缺数据不影响训练，离线缺数据不影响盘中。

---

## 二、实时链路详解（八级降级）

取数顺序固定，前一级失败或字段缺失才降级：

| 级 | 数据源 | 承担职责 | 实测速度 | 失败时降级到 |
|---|---|---|---|---|
| 1 | mcp_tdx | 基础行情（现价/开高低/量额/五档/竞价字段） | 约 40ms | 腾讯 |
| 2 | 腾讯 API | 量比/PE/PB/流通市值/涨跌停价 | 约 291ms | iFind/full-link |
| 3 | iFind | 主力资金/财务/历史K线/公告/指数/智能选股 | 约 1-2s | 东财mx |
| 4 | 东方财富 mx | 全量金融补充（资金流/选股/指数板块/研报） | 约 1-2s | 东财curl |
| 5 | 东财 curl | 主力资金流第三顺位 | 约 174ms | full-link |
| 6 | full-link 内置 TDX | 冗余校验（单股字段最全） | 秒级（2026-08-30 晚随 TDX 服务端恢复） | 独立TDX |
| 7 | 独立 mcp_plugin_TDX_tdx | 冗余位（已恢复可用） | 秒级 | miniQMT |
| 8 | miniQMT | 兜底（本地 xtdata） | 视场景 | - |

### 三个高危降级点（需留意）

1. **主力资金流依赖 iFind**：`main_net_inflow` 主源是 iFind，若其免费额度 429 限流则需降级东财mx/东财curl，但后两者也可能被东财反爬断连——**资金字段是链路中最脆弱的一环**
2. **竞价字段只 mcp_tdx 独有**：`call_auction_amount/rate` 仅自装 mcp_tdx 提供，该源若故障则竞价预判任务无替代（iFind/东财均无此字段）
3. **东财系反爬**：东财 curl 需带 UA/Referer 伪装、AKShare 东财实时接口直连被断、cn_free_quote 的东财工具需代理——**凡走东财源的通道都可能被断连**

### 信息面（并行，不进降级链）

悟道 `mcp_wudao`（63 工具）承担新闻热榜/涨停梯队/龙虎榜/题材热度/公告，与行情链独立并行；iFind 和东财mx 作备选（新闻/公告/龙虎榜）。

---

## 三、离线链路详解

### 主仓（只读）

`D:\astock`：2009 至今日线（5826 只）、1/5/15 分钟线（5793 只）、财务 8 表（2005Q1~2026Q1）、资金流/龙虎榜/北向/筹码/板块/竞价/研报/指数成分 8 类专题数据。

### 每周增量（2026-08-28 接入）

- 落点：`D:\astock\Updatedata\<周区间>\`（如 `8.17-8.21`），全市场单文件格式
- 内容：日线 + 1/5/15/30/60min + 基础信息（7 个 parquet）
- 读取：`data_config.read_main_daily()` 主仓+增量合并（增量优先），`AstockParquetReader` 自动合并；主仓保持只读
- 已接入：`build_features.py` 特征构建

---

## 四、链路健康状况评估

| 维度 | 状态 |
|---|---|
| 连通性 | 8 级降级链全部在线（2026-08-30 晚 TDX 服务端恢复，full-link 与独立 TDX 均可用）；万得 CLI / AKShare 实测连通 |
| 冗余度 | 实时每个字段 ≥ 2 源；主力资金三重冗余；大盘风控四重通道 |
| 覆盖度 | 实时 10 项 + 离线 7 项全覆盖，无"需要但无源"缺口 |
| 速度 | 一级 40ms、二/五级 174-291ms、三/四/六级 1-2s，全部在线 |
| 风险 | 3 处高危降级点（资金流/竞价/东财反爬），无端点下线 |

---

## 四点五、2026-08-30 复核：full-link 内置 TDX 曾端点下线，当晚恢复

> 2026-08-30 下午复核曾判定 `txmcp.tdx.com.cn` 端点下线（下述诊断为当时记录）；**2026-08-30 晚服务端恢复，远程 tdx-connector 与 full-link 内置 TDX 均实测可用（`tdx_lookup_stock` 返回正常）**，第 6 级回归在线。

| 检查项 | 结果（2026-08-30 下午） |
|---|---|
| 域名 DNS | ✅ 解析正常 → 121.5.179.215 |
| TCP 3001（配置端口） | ❌ 5s 超时 |
| TCP 443 / 80 | ❌ 拒连/超时 |
| HTTPS GET `/` | ❌ WinError 10061 积极拒绝 |
| 同 host 其他 path（`/mcp` `/sse` `/traemcp`） | ❌ 一致拒绝 |
| 对照 `www.tdx.com.cn:443` | ✅ 200（同一厂商官网仍在线） |

**当时诊断**：`txmcp.tdx.com.cn` 整个子域入站端口全部不通（属服务端/网络阻断，非鉴权问题）。

**当晚恢复结论**：2026-08-30 晚 TCP 3001 恢复可达，`mcp_plugin_TDX_tdx` 与 full-link 内置 TDX 的 `tdx_lookup_stock`/`tdx_quotes` 实测通过（茅台现价 1297.4 + 五档 + PE 18.22 + 涨停/跌停价）。**8 级降级链 1→2→3→4→5→6→7→8 全部在线，第 6 级回归。**

**后续**：若再次出现 `txmcp.tdx.com.cn` 3001 不可达，按前述诊断表排查网络/服务端，或先用腾讯/iFind/东财降级并如实标注来源。

---

## 四点六、2026-08-30 复核：mcp_tdx 主力源正常

> 本地自建 TDX 服务（`tdx_get_quote` 等 23 工具）2026-08-30 已重装到独立 venv `D:\QuantLab\mcp\tdx-mcp-venv`（Python 3.13 隔离，`start.bat` 指向 `tdx-mcp.exe`），端到端实测可用；远程 `mcp_plugin_TDX_tdx`（tdx-connector）当晚亦恢复，双通道就绪。

| 接口 | 调用 | 返回 |
|---|---|---|
| `tdx_get_quote` | codes=`sh600519,sz300684` | ✅ 200 · 双股各 28 字段全拿（价/量/额/五档/集合竞价/服务器时间） |
| `tdx_get_kline` | code=`sh600519`, period=day, count=5 | ✅ 200 · 5 日 OHLC + 量额，毫单位字段齐全 |
| `tdx_get_company_info_categories` | code=`sh600519` | ✅ 200 · 45 条历史期间（2002-07-25 至 2026-06-26） |

**关键踩坑**（同步已写入 `MCP数据源配置说明书.md` 第二章）：
- **代码格式**：必须 `sh`/`sz` 小写前缀 + 6 位数字（如 `sh600519`），**不接 `.SH`/`.SZ` 后缀**（实测 `600519.SH` 被后端 reject，错误信息 `invalid code: '600519.sh'`）
- 服务端在 `error_msg` 中回显的就是它认为不合法的小写形式，可据此定位格式
- 其他踩坑：`tdx_get_quote` 一次最多约 100 只代码（实测 600/300 双股正常），单股 > 一次取行情更好走 `tdx_get_kline`

**对项目影响**：v4 第 1 级主力源实测可用，今天 4 个定时任务正常推进的链路证据来源。

---

## 五、建议

1. **资金流加一道保险**：考虑将 TuShare `moneyflow`（本地 D 盘离线库已有 moneyflow.parquet 2007 至今）作为资金流第四顺位，或盘中优先用东财mx 验证 iFind 429 频率
2. **竞价字段双保险**：mcp_tdx（远程 tdx-connector / 本地 tdx-mcp `tdx_get_call_auction`）为主源；若 TDX 系故障，悟道 `intraday_main_flow` 提供 `auctionMainNetAmount` 竞价主力净额可部分替代，9:25 任务可退化为开盘价预判
3. **离线增量稳定性**：`Updatedata` 依赖人工每周放入；可考虑加一个定时任务检查新周目录是否存在并提醒
4. **东财反爬通道**：若东财 curl/mx 频繁断连，优先走 iFind（不依赖东财反爬策略）

---

*本说明与 `MCP数据源配置说明书.md` 配套：明细看说明书，链路看本文档。*
