# 审计修复任务书（T-20260816-001）

> 下发：2026-08-16（周日）｜执行：**opencode（全部任务，单一执行者）** ｜主目录：`D:\QuantLab`
> 背景：外部审计（TRAE）复核了 `Project_ATR_lowvol` MAX5 优化与 `Project_13_529主升浪` 参数矩阵两项研究结论。**数字全部可复现、无前视/无状态污染/无幸存者偏差**，但存在两类问题必须修正：①框架年化口径虚高（影响全仓库历史数字）；②P13 扫描参数混杂 + 近 3.5 年亏损被总收益掩盖。
> 本任务书自包含，不依赖原对话。执行前先读 `AGENTS.md` 红线与 `全局控制台.md` 三、看板。

---

## 〇、审计核心发现（修复依据）

### F1 框架年化是线性年化，非 CAGR（系统性，最高优先级）

`backtest/analyzer.py` 约 L136：`annual_return = total_return × 252/n_days`。这是**线性年化**，不是复利 CAGR。总收益越高虚报越大：

| 回测 | 总收益 | 报告"年化"(线性) | 真实 CAGR |
|---|---|---|---|
| ATR 基线（`20260815_110119_ccfb82_atr_10w_price50`） | +208.8% | 28.99% | **16.95%** |
| ATR MAX5（`20260815_174019_e77de8_atr_10w_price50_a_max`） | +237.6% | 32.99% | **18.40%** |
| P13 n16（`20260816_093145_7071c2_v3_n16_h60_s12`） | +82.3% | 11.42% | **8.69%** |
| P13 gate_hold（`20260816_094200_6dfa6d_v3_n8_gate_hold`） | +69.7% | 9.68% | **7.62%** |

连带问题：现有 `calmar = 线性年化/|mdd|`（MAX5 的"1.65"实为 0.92）。且 `research/backtest_atr_lowvol_v3.py` L414 手写口径是真 CAGR——两套口径在 `研究总览与路线图.md` 中被并排比较（如"状态机 18.2% vs 框架 28.99%"），内部相对比较有效，绝对水平系统性虚高 1.4~1.7 倍。

### F2 MAX5（A 方向）：定性成立，幅度与预期需修正

- 增益真实幅度：CAGR **16.95% → 18.40%（+1.45pp）**，非 +4pp；卡玛 0.86 → 0.92。
- 增益来源极薄：651/658 笔交易仅 **27 笔不同**，几乎全部增益来自 2021（年度收益 14.9%→22.2%，约 4 笔差异交易）；2023-2026 段两版完全一致（通宵报告批次3结论经独立复核**成立**——全样本 2023-2025 的微小年度差异是 2021 分歧持仓传导痕迹，非过滤器近期生效）。
- 阈值 0.10~0.30 五档逐字节相同（复核 4 个 summary 全等），稳健性无问题。
- **定位修正：零成本尾部保险（预期 +0~1.5pp CAGR），不是收益增强。部署不改结论，但报告与看板预期值必须改。**
- 勘误：项目 README 中"实盘=等权前100、无价过滤"的描述已过时（控制台 T-20260814-005：2026-08-14 build 已改 8只+max_price=50，与研究基线 `atr_10w_price50.yaml` 一致）。README 需更新，**不是**要复跑 100 只口径。

### F3 P13 参数矩阵：结论不成立，禁止按原报告拍板

1. **近 3.5 年亏钱**：2023-2026 段 n16 CAGR **-2.18%**、gate_hold 仅 +2.33%；全部收益来自 2019-2022（n16 段内 CAGR 18.84%）。2026H1 所有变体 -6%~-16%。"+82.26%/年化11.42%"是前段遗产。
2. **n_hold 扫描参数混杂**：`config/huang529_v2.yaml` 约 L36 硬编码 `max_single_pct: 0.125`，n8/n12/n16 未按 1/n 调整。n16 实际是"16 槽 × 12.5% 上限（现金约束下≈满仓）"，并非"分散到 16 只各 6.25%"。"n 越大越好"的真实机制是现金利用率/槽位放宽，不是分散化。
3. **全样本内选优**：5+ 变体同一样本挑最优，无样本外验证。

审计复核通过项（不必重查）：分年度表与净值逐年吻合、无杠杆、gate_hold 语义正确、止损次日开盘执行、103 笔涨跌停拒单=真实约束生效、信号表 ATR 升序切片 PIT 安全、universe 由 parquet 全历史生成（5821 只，无幸存者偏差）。

---

## 一、红线（必读）

1. 遵守 `AGENTS.md` 全部红线；本任务额外强调：
2. **带 `state_file` 的回测入口 run 前必须重置 state**（防残留污染，P10 曾因此虚高 18%→35.7%）。
3. 本任务**不触碰** `D:\QMT_STRATEGIES`、不动 `capital_allocation.yaml`、不下任何实盘/模拟盘指令；T4 的 build 修改只改 `projects/Project_ATR_lowvol/build/` 源文件，**QMT 上传并入周一 2026-08-17 同跑核对（T-20260814-007），不要在周一盘中操作**。
4. 生产 build 源文件为 GBK+CRLF+py3.6 语法，**禁止 patch/编辑器改编码，必须原子写入**（曾发生 open("w") 截断 0 字节事故，踩坑#66）。
5. T3 复跑**预注册判定规则见任务内**，跑完不得按结果挑参数、不得追加变体找"更好看"的档位。
6. 看板硬规则：任务状态变化当场更新 `全局控制台.md`。

---

## 二、任务分解（全部 opencode 执行）

### T1【opencode｜P0｜先行】框架 CAGR 双口径

- 文件：`backtest/analyzer.py`（annual_return 定义处，约 L136）、`backtest/report.py`
- 改动：
  1. `performance` 新增 `cagr = (1+total_return)**(252/n_days) - 1`、`cagr_calmar = cagr/|max_drawdown|`；**保留** `annual_return`/`calmar` 旧字段不改名不改值（下游兼容）。
  2. `report.md` 指标表双口径展示：`年化(线性)`、`年化(CAGR)`、`卡玛(CAGR)`。
  3. grep 全仓库 `annual_return` 引用点（`compile_max_sweep.py`、`gen_*_report.py` 等），凡**新生成**的报告脚本一律以 CAGR 为主口径、线性年化作括注；历史 report 目录不改。
- 验收：
  - 冒烟 `python -m scripts.run_backtest --config projects/Project_ATR_lowvol/config/atr_10w_price50.yaml`，新 summary.json 含 `cagr`/`cagr_calmar`；
  - 用产物 equity_curve.csv 独立验算 `(nav[-1]/nav[0])**(252/len)-1` 与 `cagr` 字段差 <1e-9；
  - 基线冒烟的 cagr ≈ 16.95%（样本一致性验证，若数据更新导致差异需在报告注明）；
  - 旧字段仍在，`git diff` 不含任何回测逻辑改动（analyzer 只加不改）。

### T2【opencode｜P0】文档勘误（依赖 T1 完成，引用新字段名）

1. `研究总览与路线图.md`：定位含 `28.99 / 32.99 / 82.26 / 11.42 / 69.73 / 9.68` 的结论行，统一改为 CAGR 口径（MAX5：16.95→18.40%、+1.45pp，定位"零成本尾部保险"；P13：加红字"2023-2026 段 n16 CAGR -2.18%，信号近 3.5 年未恢复，全样本选优无样本外"）。
2. 三份历史报告顶部插入勘误块（不改正文，只加"20260816 审计勘误"段）：`projects/Project_ATR_lowvol/results/ATR三方向优化回测_20260815.md`、`results/通宵工作报告_20260816.md`、`projects/Project_13_529主升浪/results/v2v3参数矩阵扫描_20260816.md`。
3. `全局控制台.md`：T-20260815-005（DONE）与 T-20260815-007（DOING）两行末尾追加勘误指针（一行以内，指向本任务书 F2/F3）。
- 验收：grep 上述关键数字，每处命中均有 CAGR 括注或勘误指针；`git diff` 不触碰正文结论段以外内容。

### T3【opencode｜P1｜依赖 T1 合入】P13 1/n 修正复跑 + 时段判定

- 目录：`projects/Project_13_529主升浪/`（config + research/scan_huang529.py + results）
- 改动与跑法：
  1. 修正 `max_single_pct = 1/n_hold`（n8=0.125 / n12≈0.0833 / n16=0.0625），实现方式读 `scan_huang529.py` 后自定（改基础 yaml 或 scan 内按 n 传参），其余参数冻结（h60、s12、gate_hold 逻辑不变）。
  2. 跑 v3 矩阵核心档位：n∈{8,12,16} × {无门控, gate_hold}，共 6 个配置；每档报**全样本 + 2019-2022 + 2023-2026** 三段 CAGR/回撤/夏普（T1 合入后新 summary 自带 CAGR；分段用 sample start/end 配置）。
  3. run 前重置 state_file（红线#2）。
  4. 产出 `results/v3_1overN复跑判定_20260816.md`：含修正前后对照表 + 判定结论。
- **预注册判定规则（跑前生效，不得改）**：
  - 若 2023-2026 段所有变体 CAGR ≤ 2% → **Project_13 归档**：README 写归档结论、控制台 T-20260815-007 移 DONE 并注明"1/n 修正后近 3.5 年未恢复，信号衰减"，不分配资金。
  - 若存在变体 2023-2026 CAGR > 5% 且卡玛优于同期 ATR 基线 → 出对比报告**待拍板**，仍不分配资金。
  - 中间地带（2%~5%）→ 维持 DOING，挂"信号衰减监控"标签，30 个交易日后复评。
- 验收：6 配置的 report 目录落 `D:\QuantLab\reports\`；结果 md 三段表齐全；无论结论如何，`研究总览与路线图.md` P13 行同步更新。

### T4【opencode｜P1】MAX5 部署一致性核对 + build 落地（并入周一核对）

- 前提：诚哥已知悉"预期 +0~1.5pp CAGR 尾部保险"的修正口径（T2 已写入文档）。
- 改动：
  1. `projects/Project_ATR_lowvol/build/strategy_atr_lowvol_equalweight.py`：加 `max_exclude_pct`（默认 0.20，实现口径与研究侧 `strategy/atr_lowvol.py` L216-224 一致：当日收盘取近 20 日最大单日涨幅 >20% 剔除、次日生效）。GBK+CRLF+py3.6 语法核验（`ast.feature_version=(3,6)`）+ 原子写入 + `.bak_时间戳` 备份。
  2. 参数一致性核对表：build 的 n_hold/max_price/max_exclude_pct vs `config/atr_lowvol_equalweight_config.yaml` vs 研究基线 `atr_10w_price50_a_max.yaml`，三方一致才放行。
  3. 更新 `projects/Project_ATR_lowvol/README.md`：删除过时的"等权前100"描述，写明当前实盘口径=8只+max_price50+MAX5。
- 验收：py_compile 过；核对表三方一致；**QMT 上传动作并入 T-20260814-007 周一核对清单第九节，本任务不执行上传**。

### T5【opencode 收尾｜P2】踩坑日志 + 看板收尾

1. `全局复利与踩坑日志.md` 追加：**#67 年化口径混用**（框架线性年化 vs 手写 CAGR 并排比较，全仓库年化虚高 1.4~1.7 倍；教训：跨引擎比较前先统一指标定义；修复=analyzer 双口径）。
2. `全局控制台.md`：T-20260816-001 状态更新（子任务完成情况）；T-20260815-007 按 T3 判定结果移 DONE 或挂监控标签。
3. git commit + push（信息含 `T-20260816-001`）。

---

## 三、执行顺序（单一执行者 opencode，串行推进）

```
T1 框架 CAGR（先行） ──> T2 文档勘误
                    ├──> T3 P13 1/n 复跑（等 T1 合入，用新 CAGR 口径出报告）
                    └──> T4 MAX5 build 核对（与 T3 无文件交集，顺序执行即可）
T5 收尾（最后）
```

- 建议 T1→T3→T2→T4→T5：先让 T3 的复跑直接产出 CAGR 口径报告，T2 勘误一次写到位，避免文档返工。
- T3 若在 T1 合入前开跑，需先用现有口径出数字，T1 合入后重跑最终批次；最终报告必须以 CAGR 口径为准。

## 四、完成定义（DoD）

1. 新回测 summary.json 双口径字段齐备且公式验算通过；
2. 全部文档/看板/README 的 ATR 与 P13 结论行以 CAGR 为主口径，历史报告有勘误块；
3. P13 六配置复跑完成且按预注册规则给出归档/待拍板/监控结论；
4. MAX5 build 改动通过 py3.6/GBK 核验与三方一致性核对（不上传 QMT）；
5. 踩坑日志 #67 落笔，commit+push 完成。

## 五、审计证据索引（复核用）

- 审计脚本（只读重算）：`research_audit/audit_max5_p13_20260816.py`
- 关键产物：`reports/20260815_110119_ccfb82_atr_10w_price50/`（基线）、`reports/20260815_174019_e77de8_atr_10w_price50_a_max/`（MAX5）、`reports/20260816_093145_7071c2_v3_n16_h60_s12/`、`reports/20260816_094200_6dfa6d_v3_n8_gate_hold/`
- 差异交易统计：基线/MAX5 仅 27 笔不同，集中于 2021；2023-2026 独立分段两版逐字节相同
