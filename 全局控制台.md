# 全局控制台（统一看板）

> 开发中枢与生产工程：`D:\QuantLab`（实盘策略工程资产已收拢至此）
> 更新时间：2026-08-04（opencode 五项任务收尾）
> 本文件是**唯一**任务/项目看板。原 `D:\QMT_STRATEGIES\全局控制台.md` 已收拢，本文件为准。

---

## 一、工作区定位

| 区域 | 路径 | 定位 | 使用者 |
|---|---|---|---|
| 开发中枢 | `E:\QuantLab` | 因子研究 / 回测 / 风控 / QMT 构建 / 验证 | opencode |
| 生产工程（已收拢） | `E:\QuantLab`（各项目 `build/` 子目录） | 实盘策略构建 + 分层卖出风控（QMT 单文件） | 生产部署 |
| 第一数据源 | `E:/astock/` | 买断离线 parquet：日线+1/5/15min(2009起)+财务全量(2005起) | 回测/研究 |
| 实时补充 | `qmt_self_owned`（`F:/backtest_workspace/data/duckdb/`） | 最新交易日数据，需 QMT 在线，1min 仅近 1 年 | 实盘/回测 |
| 文件通信 | `D:/QMT_POOL/` | 两项目共用运行时文件交换（selected/holdings/nav/sector_heat 等） | 实盘 |
| 知识库 | `F:\天翼云盘同步盘\Obsidian\量化知识库` | 长期沉淀（`knowledge_base` 为 junction） | 所有 |
| 回测资产 | `F:\backtest_workspace\` | 回测数据 / 结果 / 池快照 | 回测 |
| QMT 模拟端 | `E:\国金QMT交易端模拟` | 账号 67014907，Python 3.6.8 | 实盘 |

> 统一工作区：`E:\QuantLab`，所有资产在本目录下，产物生成到各子项目 `build/` 目录。

---

## 资金分配总表与账户总额约束（硬规则，必读）

> 多策略共存的资金纪律。完整规则 + 校验器见 `D:/QMT_STRATEGIES/资金分配总表与约束.md`。

| 策略 | 锁定本金(元) | 最大持仓 | 独立 config |
|---|---:|---:|---|
| ATR低波等权不杠杆 (`atr_lowvol_equalweight`) | 100,000 | 100 | `config/atr_lowvol_equalweight_config.yaml` |
| **合计** | **100,000** | — | ≤ 账户总资产（待填 `total_capital`） |

> `dual_band_6plus2`（主升浪6+2）已于 2026-08-05 淘汰，不再占用额度，已从分配表移除。

- **硬规则**：`Σ 各策略 capital_base ≤ 账户实际总资产`；改分配必须跑 `python scripts/check_capital_allocation.py`（退出码 0 才许部署）。
- 每个策略只能动自己的票和额度，绝不抢占他人资金/持仓；共享账户无法物理阻止别策略花掉你的额度，真隔离需开子账户/多账号。
- 账户总额在国金QMT客户端「查询→资金股份→总资产」查后填入 `config/capital_allocation.yaml`。

---

## 二、策略项目总表（合并两份控制台）

### QuantLab 研究层（`E:\QuantLab\projects\`）

| 编号 | 名称 | 年化 | 夏普 | 回撤 | 状态 |
|---|---|---|---|---|---|
| 01 | 多因子IC小盘Alpha | 10.1%（全池真实口径） | 0.41 | -24.4% | ?? **已审计（2026-08-01）：幸存者偏差修复后超额仅BP因子+6.1%/年，2024+转负，不建议实盘** |
| 02 | 双均线趋势 | -8.9% | -1.03 | -48.4% | 失效（已验证） |
| 03 | PEAD盈余漂移 | -7.8% | — | — | 不推荐 |
| 04 | ML多因子 | -21.4% | — | -65.8% | 已淘汰 |
| 05 | 红利低波 | 3.7% | — | -11.3% | 推荐（防御） |
| 06 | 质量小市值 | 4.1% | — | -20.1% | 可选 |
| 07 | 低换手反转 | -6.9% | — | -39.0% | 不推荐 |
| 08 | 指数增强 | 0.4% | — | -8.2% | 防御 |
| 09 | 组合策略 | — | — | — | 组合（70%红利+30%小盘） |
| 10 | 价值小盘V2（微调版待立项） | 全期超额16.1%/年（状态机口径17.9%，z=0.8/hp=0.2） | — | — | ?? **验证通过（2026-08-01）：行业中性BP+历史分位+质量排雷，9年8正超额，2024+超额+5.8%/年；三项硬验证通过；2026 超额 -1.3%~-1.8%（两口径收敛一致），待立项；V3 择时证伪（2026-08-02，audit15，不立项）；2026 转负归因完成（2026-08-02，audit16：历史分位BP反向-7.8%、行业中性BP扁平+2.2%、2026-06单月-4.1%事件冲击、排雷中性；立项风险中高，方向=降hp权重/换成分）；V2因子微调验证通过（2026-08-02，audit17：z=0.8/hp=0.2配比2026超额+0.1%转正、全期超额+241.6%（原版+208.1%）、2024+超额+43.7%（原版+29.6%）；微调版可立项，audit14权重已更新(0.8/0.2)并重跑确认(2026-08-02)，Project_10已立项(2026-08-02)）** |

### 实盘生产层（已收拢至 `E:\QuantLab`，原 `D:\QMT_STRATEGIES\Project_*`）

| 项目 | 状态 |
|---|---|
| P12 6+2评分器优化 | 战略锁定，6+2 降级为风险过滤器 |
| P15 ATR低波动策略 | 新建，待模拟验证 ← **当前活动策略** |
| P14 MACD指标策略 | 新建 |
| P11 通用风控引擎 | 纸面观察期，不动代码 |
| P01 黄氏主升浪 | 历史策略（筹码/双中军已证伪归档） |
| P02~P09 | 已归档或已合入 |

### 其他

| 项目 | 路径 | 状态 |
|---|---|---|
| 多因子策略50% | `E:\Codexspace\Project_多因子策略50` | 已完结（跨年全亏证明） |

---

## 三、当前任务看板


### TODO 待处理

| ID | 任务 | 优先级 | 入口 |
|---|---|---|---|
| T-20260717-001 | 多因子选股策略 50% 研究 | P1 | ? 已完结，见 E:\Codexspace\Project_多因子策略50\ |
| T-20260703-001 | 周频轮动 AI 选股系统落地可行性评估 | P1 | 三维度评估完成，**等待诚哥决策**是否启动 Phase 0 月频验证 |
| T-20260706-001 | 主升浪 V1.1 可行性评估 + VT 因子库结论汇总讨论 | P1 | 三条线都卡在"干净股票池"问题，待综合评估定方向 |
| T-20260702-002 | 买点信号作第 9 维度加入 6+2 评分 | P1 | 诚哥已拍板四点，**待最终确认后出 SPEC** |

### DOING 正在做

| ID | 任务 | 当前动作 | 入口 |
|---|---|---|---|
| T-2026-0801-001 | 开发任务迁移到 opencode（QuantLab） | 知识迁移 ? → 控制台合并 ? → 全景说明指向 opencode（进行中） | `E:\QuantLab\全局复利与踩坑日志.md`、`AGENTS.md` |
| T-2026-0802-001 | V3 择时增强验证（audit15） | ? 三信号全部证伪，不立项；结论已同步日志/控制台 | `research_audit\audit15_择时.py` |
| T-2026-0802-002 | 2026 转负归因审计（audit16） | ? 完成（199s）：历史分位BP反向-7.8%、行业中性BP扁平+2.2%、2026-06单月-4.1%事件、排雷中性；立项风险中高，方向=降hp权重/换成分 | `research_audit\audit16_归因.py` |
| T-2026-0802-003 | V2因子微调验证（audit17） | ? 完成（228s）：z=0.8/hp=0.2配比2026超额+0.1%转正、全期+241.6%（+33pp）、2024+ +43.7%（+14pp）；微调版可立项 | `research_audit\audit17_权重.py` |
| T-2026-0802-004 | audit14 权重修改+重跑确认 | ? DE修改score_v2权重(0.5→0.8/0.2) + 重跑确认(189s)：全期+241.6%/2024+ +43.7%/2026 +0.1%，与audit17完全吻合；微调版基线确立 | `research_audit\audit14_修正口径.py` |
| T-2026-0802-005 | V2微调版正式立项 | ? Project_10完成；CSV管道已上线(2026-08-04)；待模拟盘验证 | `projects\Project_10_价值小盘V2\` |

### DONE 已完成（2026-08-04，opencode）

| ID | 任务 | 结论 |
|---|---|---|
| T-2026-0803-001 | P0-1 因子层重构 | ? 完成：method参数化(bp/ep/bp_ep/multi)。网格验证：**砍hp正确**（V2a纯BPz1.0 年化16.2%/超额+200.9%/2026+0.4%全窗口优于基线，已设为默认）；**EP替代证伪**（V2b超额-13.8%，Wharton结论在A股小盘不成立）；strategy.yaml 已切到 V2a |
| T-2026-0803-002 | P0-2 因子层扩展 | ? 完成：新建 strategy/factors.py（ATRFactor/TurnoverFactor）。V3系列回撤略降(-28.6%~-28.9%)、2026全正，但牺牲1-2pp年化，未设默认；开关保留 |
| T-2026-0803-003 | P1-1 风控与组合重构 | ? 代码完成但**实测不达标**：分层降仓回撤恶化至-32.7%（基线-29.1%，15%一刀清仓断路器更有效）；ATR止损年化降0.6pp；仅R3信号补仓降换手有效(0.91→0.36)；均默认关闭 |
| 新增 | git dubious ownership 修复 | ? safe.directory 已配置，git 恢复可用 |
| 新增 | Project_01 权重口径统一 | ? trading_config.yaml + 控制台对齐代码 5 因子（BP27/反转22.5/低波22.5/ROE18/VWAP10） |
| 新增 | 文档编码/README | ? 全局控制台+4份specs GBK→UTF-8；README 重写反映真实结构 |
| 新增 | P10 预生成CSV管道 | ? scripts/update_p10_csv.bat + 计划任务 QuantLab_P10_CSV_Pipeline（工作日18:30）；首次生成验证通过（6文件，2026-07-31数据） |

### WAITING / BLOCKED

| 状态 | ID | 任务 | 说明 |
|---|---|---|---|
| WAITING | T-20260702-001 | 603283 孤儿持仓修复验证（orphan-adopt2 已部署） | 等开盘日志验证 `[持仓纳管]`/`[卖出评估]`/BUG5 反查 |
| BLOCKED | T-20260624-002 | 黄氏主升浪 3 年回测复盘重新评审 | 等 Codex 重新评审报告 |

---

## 四、开发环境

| 项目 | 版本/路径 |
|---|---|
| Python（研究） | 3.11 |
| Python（回测工厂） | 3.10（`C:\Users\Administrator\AppData\Local\Programs\Python\Python310`，有 duckdb） |
| Python（QMT 生产） | 3.6.8（`E:\国金QMT交易端模拟\bin.x64\pythonw.exe`） |
| QMT | E:\国金QMT交易端模拟 |
| 账号 | 67014907 |
| 数据 | E:/astock/daily/stock_daily.parquet |

## Project_01 参数固化（与代码一致）

> 2026-08-04 口径统一：权重以 `Project_01/strategy/scoring.py` 的 FACTOR_WEIGHTS（5 因子）为唯一权威源，
> 旧 4 因子数值（BP0.30/反转0.25/低波0.25/ROE0.20）已废弃；`config/trading_config.yaml` 已同步。

```yaml
# 唯一权威源: Project_01/strategy/scoring.py FACTOR_WEIGHTS
factors:
  weights: {BP: 0.27, reversal_1m: 0.225, volatility_60d: 0.225, ROE: 0.18, vwap_volume_corr: 0.10}
  filter_func: mv_0_30b  # 0-30亿市值过滤

backtest:
  top_n: 80
  rebalance_freq: "2M"  # 双月
  stop_loss: -0.12      # 止损-12%
  tx_cost: 0.002        # 单边成本

results:  # 2026-08-01 审计后真实口径（幸存者偏差修复）
  annual_return: 10.1%  # 旧宣传 15.5% 为审计前口径，已废弃
  sharpe_ratio: 0.41
  max_drawdown: -24.4%
  verdict: 超额仅BP因子+6.1%/年，2024+转负，不建议实盘
```

---

## 五、文档导航

| 文档 | 路径 |
|---|---|
| 知识中心（接手者入口） | `.qoder/repowiki/`（模块知识卡 + 体系化文档，见 AGENTS.md「知识中心」节） |
| 开发规则与红线 | `E:\QuantLab\AGENTS.md` |
| 踩坑经验库（66条） | `E:\QuantLab\全局复利与踩坑日志.md` |
| 全景说明（opencode 工作流） | `F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\量化开发全景说明.md` |
| QMT 新设备部署 | `E:\QuantLab\实盘配置指南.md` |
| 运行手册 | `E:\QuantLab\快速启动.md` + `实盘配置指南.md` |
| 回测成果总表 | `F:\天翼云盘同步盘\Obsidian\量化知识库\90_索引MOC\回测实验总表.md` |
| 实盘配置 | `E:\QuantLab\config\trading_config.yaml` + `实盘配置指南.md` |

---

## 六、操作规则

1. `Project_*` 是导航/控制层，不替代原始代码、SPEC、agent_hub。
2. 现有工程文件默认链接，不搬家；只有明确要重构路径时才整体移动。
3. 大型回测结果、DuckDB、行情 CSV 不进 `Project_*`，只写路径引用。
4. Obsidian 知识库只放长期沉淀；`knowledge_base` 是 junction，只链接不复制。
5. 超过 10 分钟的任务必须入板；没有交付物路径不算完成；没有验收不进 DONE。
6. 开发任务入口统一在 QuantLab（opencode）；生产部署按 `DEPLOY.md` 执行。

### 状态流转

```text
INBOX → TODO → DOING → WAITING/REVIEW → DONE
                    ↘ BLOCKED
```

---

## 七、模块依赖关系（2026-08-01 理清）

### 数据层（无分歧）

- 所有脚本实际数据源统一指向 `E:/astock`（`Project_01\research\multi_factor_ic\config.py` 与 `E:\QuantLab\config\settings.yaml` 一致），输出路径已本地化。

### Project_01 research/ 自包含（2026-08-01 完成）

- `multi_factor_ic` 包已 **vendor 到本地**：`Project_01\research\multi_factor_ic\`（config/data_loader/factors/ic_test/scoring/timing/backtest/run/run_final/run_indneu/run_timing）。
- `main.py` 改为注入 `Project_01` 目录，`research.multi_factor_ic.*` 解析到本地 vendored 包；`strategy\scoring.py`（5 因子，import `factors.base`）继续自包含。
- `build_prod.py` 改为读本地 `mfic_strategy\strategy_mfic_dev.py`，部署目标 `Project_01\build\strategy_mfic.py`（QMT 生产部署位，产物在子项目内）。
- 已删除：`research\` 下 13 个幻影副本（含 `taskA_phase0b.py`、`run_env_scorer.py` 等实验脚本，后者依赖 D 盘 `backtest.strategies...` 与 `agent_hub`，均为一过性实验）。

### Project_02 目录已合并（2026-08-01 完成）

- 规范目录 `Project_02_双均线趋势策略`，`run_backtest.py` 改本地加载 `E:/astock` 构建 panel（去 ST/停牌），保留 `from strategy.dual_ma import ...`；已验证 reports 已迁入其 `reports\`。
- 旧目录 `Project_02_双均线策略` 已删除。

### 自包含脚本（无 D 盘依赖）

- `Project_01\strategy\scoring.py`（新范式：import `factors.base`，5 因子权重）
- `Project_01\research\multi_factor_ic\*`（vendored 包）
- `Project_02_双均线趋势策略\run_backtest.py` + `Project_03~09\run_*.py`（本地 Path/pandas）
- `projects\verification\*.py`（本地）

### 遗留（刻意保留 / 待拍板）

- `build_prod.py` 写 `E:/QuantLab/projects/Project_01_多因子IC小盘Alpha/build/strategy_mfic.py`：QMT 生产部署位（2026-08-04 已从 D 盘收拢）。
- **权重口径不一致**：`strategy\scoring.py` 是 5 因子（BP27%/反转22.5%/低波22.5%/ROE18%/VWAP10%，与 AGENTS.md 一致）；本文件"Project_01 最优参数"段写的是 4 因子旧值（BP0.30/反转0.25/低波0.25/ROE0.20）。**权重改动须先确认 SPEC，本表记录差异但不擅改。**

---

## 八、实盘调度与部署

### 每日调度（与 AGENTS.md 一致）

```text
09:15  盘前准备：检查连接、下载数据、查询账户、撤销遗留委托
09:35  策略执行：计算因子 → 目标组合 → 先卖后买执行
10:05+ 风控巡检：每 30 分钟 — 止损(-8%)、回撤检查(-15%)
13:05  午后检查：验证持仓
14:50  日终处理：对账、保存状态、发送日报
```

### 部署红线速查

- 构建产物必须 GBK + `# coding=gbk` + Python 3.6.8 语法；改源模块后**重新构建**，不手工改产物。
- 改代码后验证链：MOCK+单测 → rebuild+validate 6 项 → **模拟端跑 1 交易日过 8 项 checklist** → 才上实盘。
- 部署/重装后必看 `XtClient_FormulaOutput_*.log` 确认 `[初始化完成]`（见踩坑日志 #6）。
- 实盘日志落点在局域网 `\\192.168.31.131\国金qmt交易端模拟\userdata\log`（本机只有 06-30 前的）。
- 每日收盘导出 CSV 到 `D:/QMT_POOL/`（模拟端 deal/order 隔日清空，不导出就丢）。
- 详细清单：`E:\QuantLab\实盘配置指南.md` + 踩坑日志 QMT 章节。

## 九、本地策略逻辑验证（miniQMT 驱动，2026-08-02）

> 经验来源：本地已部署 miniQMT（本项目 `test_connection_qmt.py` 即验证链路），用 `xtquant` SDK 在外部 Python 直接驱动策略的真实选股/评分函数，免去「改完→部署远程→等交易日」的长周期。完整说明见 F 盘 Obsidian 知识库公共资产（vault 根 `量化知识库/workflows/`）：`WORKFLOW-miniqmt-local-validate.md`。

- **价值**：远程部署一天只能验证一次；本地 10 秒跑一次，随时多次验证策略逻辑、快速迭代。研究层与实盘工程通用（只要策略选股/评分是纯函数）。
- **前置**：本地 miniQMT 已登录（`XtMiniQMT.exe` / `miniquote.exe` 行情 58610 / `XtItClient.exe` 交易 58600 在跑）。QMT 自带 Python 3.6 无法被外部加载，需在外部 venv 装 `xtquant numpy pandas` 连本地服务。
- **三步**：
  1. 只读探针：验证行情/交易链路、账户、样本行情（**绝不下单**）。
  2. 本地实跑脚本：`LocalContext` 适配器把策略 `C.*` 映射到 `xtdata`，UTF-8 解码源文件后 `import`，调策略真实选股函数实跑。
  3. 判读 `筛选完成: X只` 与拒绝统计（数据不足 / ATR过高 / 换手越界 / 换手未知）。
- **?? 必踩坑**：源文件头标 `# coding=gbk` 但**实为 UTF-8**（部署时才由构建脚本转 GBK）。本地 import 必须 `_raw.decode('utf-8')` 并改文件头，否则中文 `'沪深A股'` 字面量被改坏 → 返回 0 只，误判成「0 候选」。
- **适用 / 不适用**：
  - ? 本地能验证：选股/评分管线是否通、换手率 fail-open 是否生效、ATR/成交额/数据过滤、参数调整后候选数量与排序、编码/导入/适配器问题。
  - ? 必须上远程：验证 `is_last_bar()` 守卫（依赖历史 K 线回放）、真实换手率 1–8% 过滤（xtdata 无 `get_turnover_rate`，本地只触发 fail-open）、真实下单/SAFEMODE、持仓纳管反查、全天调度/心跳节奏。
- **实测结果（ATR 低波动策略）**：全市场 5206 只实时数据，9.9s 扫完，`筛选完成: 3 只`；换手越界 0、换手未知 3337（本地无换手 API，fail-open 放行）。
- **公共资产说明**：该 WORKFLOW 文档是跨项目公共资产，不属于任一项目仓库，统一维护在 F 盘 Obsidian 知识库；本项目若写同类脚本，复用该文档即可，不要在各自 `docs/` 下重复建副本。




