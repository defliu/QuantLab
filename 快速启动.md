# 快速启动指南（账号 70180771）

## 一键配置

### 1. 安装xtquant
```powershell
# 复制xtquant到系统Python
Copy-Item -Path "E:\国金QMT交易端模拟\bin.x64\Lib\site-packages\xtquant" -Destination "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\Lib\site-packages\xtquant" -Recurse -Force
```

### 2. 启动QMT
```powershell
Start-Process "E:\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe"
```
等待登录完成（账号：70180771）

### 3. 测试连接
```powershell
cd E:\QuantLab
python test_connection.py
```

### 4. 启动实盘
```powershell
.\start_trading.bat
```

---

## 检查清单

- [ ] QMT已启动并登录
- [ ] xtquant已安装（`python -c "from xtquant import xttrader"`）
- [ ] 连接测试通过（`python test_connection.py`）
- [ ] 配置文件正确（`config/trading_config.yaml`）
- [ ] 通知已配置（可选）

---

## 目录结构

```
E:\QuantLab\
├── config\
│   ├── trading_config.yaml    # 实盘配置（已创建）
│   └── settings.yaml          # 全局配置（已创建）
├── data\
│   ├── cache\                 # 数据缓存
│   ├── broker_state.json      # 状态持久化（运行时生成）
│   ├── positions.json         # 持仓（运行时生成）
│   └── equity_curve.csv       # 净值曲线（运行时生成）
├── logs\
│   └── trading_YYYYMMDD.log   # 交易日志（运行时生成）
├── test_connection.py         # 连接测试（已创建）
├── start_trading.bat          # 启动脚本（已创建）
├── live_trading.py            # 实盘主程序（需创建）
└── 实盘配置指南.md             # 详细指南（已创建）
```

---

## 下一步

1. 执行上述一键配置
2. 阅读 `实盘配置指南.md` 了解详细配置
3. 接入你的策略（参考 `A股量化框架_五大扩展模块.md`）
