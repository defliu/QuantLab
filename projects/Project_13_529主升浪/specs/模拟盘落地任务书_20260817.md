# Project_13 黄氏529 · QMT 模拟盘落地任务书（T-20260817-001）

> 拍板链：诚哥 2026-08-17 提议「跑模拟盘看效果」并指定 opencode 生成可部署代码。
> 前置证据：`results/策略分析与收益提升方案_20260817.md`（交易级解剖 + trail15 唯一正优化 +
> 止盈2~5%换票证伪）。执行者：opencode（研究侧支持：本任务书作者）。

## 〇、模拟盘定位（管理预期，先读）

1. **目的 = 管线验证 + 信号衰减监控**，不是收益验证。n12 口径期望日收益 ~0.02-0.03%，
   30 个交易日的模拟盘净值在统计上说明不了赚亏；真正要收集的是：
   - 每日信号数 / 成交数 / 实际滑点 vs 回测假设（0.1%）；
   - **滚动「到期均收」与「单笔净收益」**（回测基线：2023-2026 到期均收 +4.4%，
     回到 +8% 以上才值得重新评估策略--R3 复评的核心指标）。
2. 30 个交易日后按 R3 复评：数据齐且无工程异常 -> 出「模拟盘 30 日 vs 回测同口径对比报告」
   交诚哥拍板下一步（继续观察 / 加仓 / 停跑）。
3. **时机硬门禁**：2026-08-17（周一）的 P0（T-20260814-003 账户清理、T-20260814-007 双策略
   复跑核对）**完成且无遗留异常之前，禁止把 529 build 上载 QMT**。T1-T5 可先行并行。

## 一、部署口径（冻结，不再调参）

| 项 | 值 | 依据 |
|---|---|---|
| n_hold | 12 | 1/n 修正复跑最佳 |
| max_single_pct | 1/12 ≈ 8.33% | 同上 |
| stop_loss | -0.12（收盘口径） | v3 单变量归因 |
| max_holding_days | 60 交易日（入场日=0） | v2 证伪 20 天 |
| trailing_stop | 高点回撤 15%（收盘 ≤ 持有期最高收盘×0.85） | **待 T1 引擎验证，预注册规则见 §四** |
| market_gate | 0（关闭，不部署门控） | 门控与分散冲突 + 近段 gate 更差 |
| 虚拟子账户 | capital_base = 100,000 元 | 与 ATR/V2 同规格 |
| remark 标识 | `P13H529` | 共享账户订单隔离 |

执行时序对齐回测 next_open 口径：**决策全部基于 T-1 日收盘数据（信号 CSV / 止损 / trail / 到期
评估），执行统一在 T 日 09:35 开盘窗口**；盘中不动作（仅监控日志）。

## 二、任务拆分（opencode 执行清单）

### T1 · 引擎级 trail15 验证（研究环境，先行）
- `strategy/huang_529.py` 加 `trailing_stop` 参数（None=关；规则：`last_price <= 持有期最高收盘
  ×(1-trailing_stop)` 触发，与止损/到期同层卖出；最高价跟踪由引擎 positions 或策略自算，PIT 安全）。
- 跑 `research/scan_v3_1overN.py` 口径：n12_h60_s12 ± trail15，全样本 + 2019-2022 + 2023-2026 三段。
- **预注册决策规则（跑前生效，防挑参数）**：trail15 相对 base 全样本 CAGR 改善 ≥ +0.3pp，
  且 2023-2026 段 CAGR 不劣化 > 0.3pp、最大回撤不恶化 > 2pp -> 部署含 trail15；否则部署纯 base。
  不做 trail 参数扫描（离线已校准 15% 档；10% 档已证伪）。
- 验收：对比表落 `results/trail15引擎验证_YYYYMMDD.md`，明确「部署口径=blob」结论。

### T2 · 信号日更管道（本地预计算，QMT 只读 CSV）
- 新建 `projects/Project_13_529主升浪/research/gen_529_signal_daily.py`：
  从 `gen_529_signal_table.py` 抽增量逻辑，**口径逐字段一致**（突破60日高3日窗/small_chg_5d≥2/
  MA多头+MA60走平/angle5≥30/收阳/排除 pit&ji_la/筹码 (cost95-cost5)/cost5≤25%）。
- 输入 `E:/astock`（须当日已更新，未更新则用最新可得日期并在文件名体现）；输出
  `D:/QMT_POOL/529_signal_top16_YYYYMMDD.csv`（列：ts_code,atr_pct,rank；ATR 升序前 16）。
- 运行环境：`D:/Python311/python.exe`（需 pandas/pyarrow，QMT py3.6 不能算）。
- 注册 Windows 计划任务（工作日收盘后，参考 V2 的 `scripts/gen_qmt_csv.py` 调度；**教训
  T-20260816-003：V2 的 CSV 计划任务实际不存在**--本任务注册后必须 `schtasks /query` 验证在册，
  并手动跑一次全流程）。
- 一致性校验：抽 ≥3 个历史交易日，增量版输出与全量信号表 `signal_table_529_top16.json`
  当日切片**逐只一致**（顺序允许尾部并列差异，前 12 只必须一致）。
- 验收：校验脚本 PASS + 计划任务在册证据（查询输出贴报告）。

### T3 · QMT 部署 build（单文件，GBK / py3.6.8）
- 位置 `projects/Project_13_529主升浪/build/strategy_huang529.py`，经 `build.py` 流程产出
  （`# coding=gbk` 首行、GBK+CRLF、BUILD_TAG=YYYYmmdd-HHMMSS、init/handlebar/exit、
  `_DEFAULT_CONFIG` 完整 fallback、不依赖 `__file__`、时间用 `C.get_current_time()`、
  收盘任务 init 末尾+标志联动）。
- 骨架复用 `projects/Project_ATR_lowvol/build/strategy_atr_lowvol_equalweight.py` 的模式：
  ledger 持久化（`D:/QMT_POOL/p13_huang529_holdings.json`：code/entry_date/cost/peak_close/
  hold_days/volume）、`passorder` 防坑（异步返回非订单号、反查短轮询、五条 AND 硬过滤、
  `m_strOptName` 判方向、11 参统一）、reconcile 以账户 position 唯一真相 + 三态兜底、
  回滚先撤活单、跌停/停牌进暂缓队列、每日成交/持仓导出 CSV 到 `D:/QMT_POOL/`。
- 买入：读 T-1 信号 CSV，剔已持有，按 rank 补槽至 12 只，每只
  `min(total_asset/12, cash/当日新买数)`；涨停不追、T+1、整数手。
- 卖出：止损 -12% / trail15（若 §四判定启用）/ 到期 ≥60 交易日，任一触发即卖；
  **fail-open 语义**：当日信号 CSV 缺失/损坏 -> 当日不买入（现金闲置），日志高亮 WARN，
  持仓管理（卖出）不受影响照常执行。
- 验收：py_compile PASS、GBK/CRLF 核验、无 MOCK 残留、BUILD_TAG 在 init 与日终日志输出。

### T4 · MOCK 测试套件
- ≥14 用例覆盖：信号读取（正常/缺失/空）/ 槽位与 1/12 上限 / 现金不足分配 / 止损触发 /
  trail 触发（含高点更新）/ 到期 / T+1 / 涨停不买 / 跌停进暂缓 + 解封补卖 / 状态恢复 /
  reconcile 三态 / 回滚撤单 / 重复 bar 幂等 / CSV 导出。
- 验收：全绿，测试脚本与结果落 `research/tests/`。

### T5 · 资金分配登记
- `config/capital_allocation.yaml` 追加 `key: huang529_breakout, capital_base: 100000,
  max_hold: 12`；同步 `D:/QMT_STRATEGIES/资金分配总表与约束.md` 镜像（若流程要求）。
- 跑 `python scripts/check_capital_allocation.py`，**退出码 0**（预期 30万/1000万）。

### T6 · 本地 miniQMT 冒烟
- `local_validate.py`（复用 `broker/local_context.py`）：信号 CSV 读取通 / 候选数合理 /
  fail-open 生效 / 不实盘下单。用 miniqmt venv 或 QMT 自带解释器按 T-20260816-003 结论执行。

### T7 · 上载 QMT + 首跑核对（门禁：§〇 时机）
- 上载 `E:\国金QMT交易端模拟\python\STRATEGY_HUANG529.py`，首跑 1 交易日核对清单：
  ① BUILD_TAG 一致；② 持仓 ≤12 且单票 ≤1/12；③ remark=P13H529 无串账户单；
 ④ 无重复下单/无孤儿持仓；⑤ 信号缺失日 fail-open 日志；⑥ 成交/持仓 CSV 已导出；
  ⑦ 资金分配校验仍 0；⑧ 09:35 窗口外无交易动作。
- 核对模板落 `specs/模拟盘首跑核对清单_YYYYMMDD.md`。

### T8 · 收尾
- `全局控制台.md` T-20260817-001 转 DONE（含 T1 结论与部署口径）、`研究总览与路线图.md`
  P13 行更新、本 specs 补「实际部署口径」附录；commit + push（中文提交信息，含任务号）。

## 三、工程红线（违反=返工）

- AGENTS.md「QMT 红线」「实盘执行红线」「资金分配红线」全部适用（编码/py3.6 语法/passorder
  防坑/账号 67014907 硬编码/账本隔离/绝不纳管他人持仓）。
- **绝不改动**：ATR/V2 的 build 与运行态、共享模块 `broker/qmt_order.py`、引擎
  `backtest/`（T1 只加 `strategy/huang_529.py` 参数，属研究侧策略文件，允许）。
- 共享账户周一有清理/复跑 P0：在上载门禁解除前，任何 QMT 端操作只读不写。
- 研究回测产物必须走引擎（不得手填收益数字）；报告口径用 CAGR（线性年化括注）。

## 四、预注册决策规则与产物归档

- trail15 部署判定（T1 跑前生效，见 §T1）；判定结果无论启用与否都写入 T1 报告并回填本文件附录。
- 30 个交易日监控模板（T7 起每日/每周填）：
  `日期 | 信号数 | 买入数 | 卖出数(止损/trail/到期) | 单笔净收益% | 滚动到期均收% | 滑点bp | 异常`。
- 复评触发：30 个交易日 或 累计异常 ≥3 次，先到为准。

## 五、复用资产清单

| 资产 | 路径 | 用途 |
|---|---|---|
| ATR 部署骨架 | `projects/Project_ATR_lowvol/build/strategy_atr_lowvol_equalweight.py` | build 结构/防坑模式模板 |
| 下单防坑 | `broker/qmt_order.py` + `broker/QMT委托买卖防坑指南.md` | passorder 规范 |
| 本地适配器 | `broker/local_context.py` | T6 冒烟 |
| 全量信号生成 | `projects/Project_13_529主升浪/research/gen_529_signal_table.py` | T2 口径源 |
| 扫描口径 | `research/scan_v3_1overN.py` | T1 复跑对齐 |
| V2 计划任务教训 | 看板 T-20260816-003 | T2 注册后必须验证在册 |
