# P16（LightGBM股票大师）独立审计报告 · 2026-08-28

> 审计人：Qoder agent（应诚哥"审计一下p16"指令）
> 范围：T-20260827-004 / T-20260828-001~005 声称 vs 代码与数据产物实核；未提交改动；红线符合性
> 方法：git diff 逐文件核对 + 关键代码直读（file:line）+ 数据产物交叉验算 + verify_model_panel_sync.py 实测运行
> 结论先行：**核心声称全部属实，8/24 事故修复闭环质量高；发现 2 项 P1（共享数据层被未登记改造、账本无账号戳）、4 项 P2**

---

## 一、验证为真（声称 vs 证据）

| # | 声称 | 证据 | 判定 |
|---|---|---|---|
| 1 | 资金池硬校验 fail-loud | `rebalance_daily.py:361-392`：单笔≤SINGLE_CAP、累计≤POOL_BUY_CAP 双闸，超限拒绝+`POOL_BLOCK` | ✅ 真实存在 |
| 2 | 盯盘成本 FIFO 权威源 | `qmt_monitor.py:51` `fifo_positions_from_log`；`volume`（盯盘）/`can_use`（卖出）分离，open_price 仅兜底 | ✅ |
| 3 | 成交记录固化+异常检测 | `reconcile_trades.py:39` EXCLUDE_CODES；`data/qmt_trade_log.csv:89` RECON_BACKFILL 补记在案 | ✅ |
| 4 | 每日对账 PASS | `data/reconcile_20260828.md`：2 只持仓日志=QMT 全一致、无异常；策略资金池 100,024 元 | ✅ 实读确认 |
| 5 | 资金池 json 自洽 | `strategy_capital.json`：100000+599.21-1256.86=99,342.35 逐位验算一致 | ✅ |
| 6 | V1.2 promote + 绑定 | `data/model_panel_binding.json`（2837树/面板8-27）；`versions/models/` 备份链完整（pre_retrain 与 pre_promote 均为 3676 树旧正式，字节数一致）；正式模型只读（-r--r--r--） | ✅ |
| 7 | 同步校验可用 | 实测 `verify_model_panel_sync.py` exit=0（面板 8-27=绑定 8-27，树数 2837=2837）；run_scheduled.ps1 daily/retrain 均已接线（git diff 确认） | ✅ |
| 8 | 评分卡寻优未落盘纪律 | `review_full.py:52` / `deploy_predict.py:36` / `scorecard_real.py:23` SC_WEIGHTS 均为生产默认 0.25/0.20/0.20/0.15/0.10/0.10 | ✅ 未被寻优结论污染 |
| 9 | 前向管道修复 | `paper_forward_live.csv` 18 行=9 交易日×2、无重复、8/17-8/27 连续；`data/selections/g2/` 含 20260825-27；`data_live/update_meta.json` 增量至 20260828（29,130 行） | ✅ |
| 10 | 账号合规 | `qmt_config.py:17` ACCOUNT_ID="67014907"，符合双账号并存政策 | ✅ |
| 11 | 8/28 POOL_ADJ 回写 | trade_log 尾部：300684 卖200 @90.15 / 000737 卖1100 @15.31，与持仓 500/3100 闭合 | ✅ |

## 二、发现（按级）

### P1-1 共享数据层 `data/astock_reader.py` 被静默改造（未入板、未 commit）
未提交 diff 给全仓共享的 `AstockParquetReader` 加了**自动合并 `E:/astock/Updatedata/` 周增量**（当前只有 `8.17-8.21` 一个目录），并硬编码 `sys.path.insert` 导入 Project_16 的 `data_config`。四个问题：
1. **全仓回测行为静默变化**：Project_12/13/14/16_公开策略审计等 20+ 脚本 import 此 reader，改后任何回测自动混入增量数据，与历史报告口径不可比，且无人知晓。
2. **架构倒挂**：`data/` 共享层反向依赖 `projects/Project_16` 项目层，目录改名即断全仓 import。
3. **口径混装风险**：增量源是 miniQMT xtdata（`update_meta.json` source 字段），与主仓 tushare 口径未必逐字段一致，静默去重"增量优先"无对拍。
4. **未登记**：违反看板硬规则（>10分钟任务必须入板）。
**建议**：`astock_reader.py` 回退到 HEAD（共享层不该带此逻辑）；P16 自己的 `data_config.read_main_daily`（已实现同功能且只作用于本项目）保留。若确需全局增量，单独立项评审后再进共享层。**此为建议，回退动作待诚哥拍板，本审计未动任何文件。**

### P1-2 P16 账本/成交记录无 account_id 戳
AGENTS.md 红线（T-20260823-004）"持票账本必须内嵌 account_id 戳"落地时只覆盖 ATR/V2/P13，P16 的 `qmt_trade_log.csv`、`strategy_capital.json` 均无账号字段。当前 P16 独占 67014907、串账风险低，但 `specs/miniQMT迁移大QMT实施计划.md` 已把"账本 account_id 戳校验"列为迁移步骤——建议**迁移前必须补**（改动小：写档加字段+加载校验 fail-safe）。

### P2-1 V1.2 promote 审计链时间戳不一致
模型 mtime=13:08:15（copy2 保留训练完成时刻）；VERSIONS.md 上线记录=143151；binding promote_time=144515（文件 mtime 14:46）。`promote_model.py` 单次 run 用同一 stamp 写 VERSIONS+binding，两者不同说明**绑定记录非 promote 同一 run 产出**（疑为 14:31 promote 旧版代码后，14:45 手工/单独补写）。审计链应可复现：建议在 VERSIONS.md 补一行说明，或后续给 promote_model.py 加 `--write-binding-only` 模式重新生成对齐。

### P2-2 8/28 全部改动未 commit（9 文件，+598/-403）
含生产面 `promote_model.py` / `run_scheduled.ps1` / `data_config.py`。其中 run_scheduled 的 daily 已在真实调度中生效，**生产在跑的代码不在版本库里**。建议今日 commit（astock_reader.py 除外，见 P1-1）。

### P2-3 PROJECT_MEMORY.md「硬约束」段混有 trae 时代失效规则
如"株冶集团 600961 跌破 28.64 无条件清仓""策略持仓参与 9:45 评分对比，不在 Top 2 卖出"等，与现行 N=10 换仓（同文件 2026-08-27/28 节）矛盾。文件头虽声明"以最新代码为准"，但**硬约束标题会诱导未来 agent 执行死人之规则**。建议给 trae 时代条目加"【已失效 2026-08-28 审计标注】"前缀。

### P2-4 verify_model_panel_sync.py 的 mtime±60s 比较脆弱
成立依赖 promote 用 `copy2`（保留 mtime）；若将来有人改用 `copy` 会误报"模型被改动"。树数+绑定日期已足够，mtime 校验可降级为 warning。可不改。

## 三、观察（非发现）

- **8/24 事故闭环质量高**：代码修复真实在位 + 对账每日 PASS + 资金池双口径剥离——这是同类事故的正确收尾方式。
- **收益结构的诚实核算**：全账户已实现 467,996 元 vs 策略真实 599 元——99.87% 已实现利润来自超买标的运气。资金池口径已正确剥离，但这也意味着**策略真实 alpha 目前无证据**（真实已实现+599、浮盈-576），前向验证 N>30 是当前唯一裁判，纪律在守（9 天）。
- V1.2 promote 的 IC 优势（0.0394 vs 0.0380）本身很薄，promote 的第一理由（消除训练/推理分布不一致）成立——决策结构正确。
- 9 个 Quant_* 定时任务已切新路径（PROJECT_MEMORY 记载 8/28 完成），本次未逐一核 schtasks（无法从仓库验证），列为待人工确认项。

## 四、建议动作清单

| 优先 | 动作 | 负责 |
|---|---|---|
| P1-1 | 拍板：astock_reader.py 回退 vs 立项 | 诚哥 |
| P2-2 | commit 8/28 改动（除 astock_reader.py） | 可代做 |
| P1-2 | P16 账本加 account_id 戳（迁移前必须） | 排期 |
| P2-1/P2-3 | 文档修订（VERSIONS 说明+PROJECT_MEMORY 失效标注） | 可代做 |

*本审计未修改任何文件；只读核查 + 一次 verify_model_panel_sync.py 实测（exit=0）。*
