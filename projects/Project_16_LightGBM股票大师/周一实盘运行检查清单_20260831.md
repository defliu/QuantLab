# Project\_16 首日实盘运行检查清单（2026-08-31 周一）

> 依据：部署核对清单\_新机.md + WORKFLOW\_DEPLOY.md + MCP数据链路说明.md + MCP数据源配置说明书.md
> 用途：周一 08-31 首次实盘逐项核对；每项完成后打 `[x]` 并记录结果/异常。
> 账号：67014907（旧号，miniQMT 委托）；正式模型：**V1.2（2837 树，27 特征，面板 8/27）**。
>
> ⚠️ **2026-08-31 已回滚 V2.0→V1.2（用户确认）**：V1.2 保运行（miniQMT 67014907 沿用），V2.0（3017 树/33 特征）备份 `versions/models/lgb_model_v3_V20_live_20260831.txt`，准备大QMT 改造（70180771）。本清单全部按 V1.2 核对。

***

## 一、运行前环境核对（建议 08:50 前完成）

### 1.1 数据与模型就绪

* [ ] 主库行情 `D:\astock\daily\stock_daily.parquet` 到 2026-08-21，可读

* [ ] 增量目录 `D:\astock\Updatedata\8.24-8.28\` 存在；`read_main_daily()` 合并后到 **2026-08-28**

* [ ] 正式模型 `D:\QuantLab\models\lgb_model_v3.txt`（**2837 树 V1.2**）存在

* [ ] 面板 `data\feature_panel_v3.parquet`（**27 特征，8/27**）+ 绑定 `data\model_panel_binding.json` 存在

* [ ] 面板-模型同步：`python verify_model_panel_sync.py` 输出「校验结果: 一致」且 exit 0

* [ ] 股票池 `D:\QuantLab\data\universe_all_a.csv`、账本 `data\qmt_trade_log.csv` + `data\strategy_capital.json`（含 `account_id=67014907` 戳）就位

### 1.2 环境与服务

* [ ] QMT 客户端已启动并登录 **67014907**（实例 `D:\国金QMT交易端模拟`，2026-08-31 实测：行情 miniqmt 源 + 交易 connect=0，资产/持仓/委托查询全通；`xtdata` 能 `get_full_tick`）

* [ ] Python 环境：`python -c "import lightgbm, pyarrow, sklearn; print('env OK')"` 通过

* [ ] MCP 数据源实测可用：TDX（`mcp_plugin_TDX_tdx` tdx\_quotes 返回真实行情）/ iFind / 东财mx / 悟道 / 财汇 / 腾讯 qt.gtimg.cn

* [ ] TraeWork Schedule **6 个任务 Active**：09:15 候选预生成（2026-08-31 新增）/ 09:25 集合竞价 / 09:45 开盘复核 / 11:35 午休 / 15:40 盘后 / 15:05 模拟监控

* [ ] Windows 计划任务 9 个 Ready：daily(16:30) / weekly\_retrain(周一17:00) / monitor×6 / monthly\_factor

* [ ] 无遗留委托：QMT 账户委托列表为空（如有 09:15 前撤销）

***

## 二、盘前（09:15-09:25）

* [ ] 运行 `python is_trade_day.py` 确认 08-31 为交易日（周一，非节假日）

* [ ] 09:15 检查 QMT 连接、数据下载、账户查询正常

* [ ] 确认无遗留委托/持仓状态文件正常（`data/rebalance_*.md`、持仓一致）

***

## 三、09:25 集合竞价增强预判（TraeWork 任务）

**执行链路**：`is_trade_day` → `build_g2_daily.py`（G2 特征快照）→ `deploy_predict_g2.py --threshold 60 --top 10 --pool 100` → TDX 竞价+新闻+板块+资金（8 级链）→ 悟道四工具 → 《集合竞价预判》md → 飞书推送

* [ ] `build_g2_daily.py` 成功生成当日 G2 快照（无异常/无编造）

* [ ] `deploy_predict_g2.py` 产出 G2 候选（`data/selections/g2/`）

* [ ] TDX `tdx_get_call_auction`/`tdx_get_auction_0925` 取到竞价序列（或降级标注来源）

* [ ] 《集合竞价预判》摘要生成到 `data/real/`，含持仓预警等级 + 候选强弱 + 取数来源

* [ ] 飞书 bot 私聊推送成功（open\_id=ou\_bd13444d8ea53c28249c669f43f3eeff）

**核对点**：竞价预判候选与 09:45 换仓候选逻辑衔接（g2 预判是增强信号，非强制下单）。

***

## 四、09:45 开盘实时复核 + 每日换仓（TraeWork 任务，真实下单）

**执行链路**：`is_trade_day` → 确认选股清单（无则 `deploy_predict --model v3 --top-k 10`）→ 8 级链复核 F2/F3/F5 → `review_full.py` → 大盘风控定 T → `rebalance_daily.py --date 20260831 --top <T> --live` → 飞书推送

### 4.1 选股与复核

* [ ] 完整版清单 `data/selections/20260831_selection_full.csv` 存在（或已生成）

* [ ] 8 级链复核（v5 按职责分组，2026-08-31 优化）：基础行情腾讯兜底 + mcp\_tdx 覆盖增强；扩展指标 F2 资金/F3 催化/F5 板块 mcp\_tdx→iFind→东财mx→东财curl → 写 `data/tdx_review.json`；TDX 系不可达用腾讯+iFind 取数并**标注来源**

* [ ] `review_full.py` 生成完整版清单（模型分 + 评分卡双轨）

### 4.2 大盘风控定 T

* [ ] 沪深300 实时涨跌（优先 `tdx_get_index_quote("000300")`）：<=-1.5% → T=0 空仓；-1.5%\~-1.0% → T=1 半仓；>-1.0% → T=2 满仓

* [ ] 记录当日 T 值及依据

### 4.3 换仓执行（真实下单）

* [ ] `rebalance_daily.py --date 20260831 --top <T> --live` 执行（账号 67014907）

* [ ] 买入预算以 `data/strategy_capital.json` 资金池为准（**绝不用账户全量资金**）

* [ ] 先卖后买顺序正确；委托后**反查成交/撤单状态并回写**（短轮询等待 order\_id，不静默断链）

* [ ] 保存 `data/rebalance_20260831.md/.json`，每笔委托状态核对

* [ ] 飞书 bot 推送买入结果（大盘结论 + 买入/卖出清单 + 未买原因）

**红线**：若卖出回滚遇跌停/停牌进暂缓队列；pending 超时 5 分钟内重试 3 次不放弃；绝不抢他人持仓。

***

## 五、11:35 午休持仓报告（TraeWork 任务）

* [ ] QMT `query_stock_positions`（volume>0 为准）查当前持仓

* [ ] 8 级链采集行情/资金/板块 + 资金流 iFind→东财mx→东财curl + 悟道午间热榜/公告

* [ ] 《午休持仓报告》生成到 `data/real/`（逐股 + 操作建议 + 来源标注）

* [ ] 飞书 bot 推送

***

## 六、15:40 盘后持仓复盘（TraeWork 任务）

* [ ] 当日持仓与成交核对（QMT + `data/qmt_trade_log.csv`），浮盈亏/已实现盈亏

* [ ] `python strategy_capital.py` 更新资金池

* [ ] 8 级链采集收盘行情/板块/新闻/资金流 + 悟道盘后公告/龙虎榜/涨停梯队

* [ ] 《盘后持仓复盘》生成到 `data/real/`（当日表现 + 资金池 + 次日建议 + 来源标注）

* [ ] 飞书 bot 推送

***

## 七、盘后自动化（Windows 计划任务）

* [ ] **16:30 daily**：对账（账户 position 唯一真相）+ `verify_model_panel_sync.py`（面板 vs 模型绑定日一致，exit 0）+ 增量

* [ ] **17:00 weekly\_retrain（周一）**：`refresh_panel_v3.py` 重建面板（**27 特征 V1.2**，合并主仓+增量）→ `train_optuna.py` 产出候选 `lgb_model_v3_retrain_20260831.txt` → `verify_model_panel_sync.py`
  * 首跑注意：迁移后新版 retrain 链路首次实测，核对日志中面板重建行数、候选 IC/树数

* [ ] **6 档 monitor**（09:45\~14:30 每 30 分钟）：风控巡检正常，无止损/回撤误报

* [ ] 重训候选**不自动 promote**：如需提升，人工 `promote_model.py --model-file ... --suffix v3 --yes --note "..."`

***

## 八、异常应对速查

| 现象                      | 处置                                                      |
| ----------------------- | ------------------------------------------------------- |
| `is_trade_day.py` 判非交易日 | 08-31 为周一，正常应为交易日；若误判检查节假日表/日期                          |
| miniQMT 行情服务连不上         | 确认 QMT 客户端已启动登录；`data_sources.fetch_all` 会自动降级腾讯（已修复崩溃） |
| TDX 不可达                 | 降级腾讯/iFind/东财，**如实标注来源、不编造**；排查 `txmcp.tdx.com.cn:3001` |
| 大盘风控 T=0                | 空仓不买入，记录原因                                              |
| 委托失败/超时                 | 短轮询反查；pending 5 分钟重试 3 次；仍失败打印「pending放弃」               |
| 模型-面板同步告警               | 跑 `verify_model_panel_sync.py` 看差异；面板更新未 promote → 提升候选 |
| 飞书推送失败                  | 仅记录不阻塞主流程（已配置）                                          |

***

## 九、次日核对（09-01 周二盘前）

* [ ] 确认 08-31 持仓状态、成交记录、资金池（`data/strategy_capital.json`）正确

* [ ] 确认重训候选是否已 promote（未 promote 则正式模型仍是 V1.2 2837 树）

* [ ] 复盘 08-31 全天任务日志（TraeWork 执行记录 + `data/real/*.md` + Windows 计划任务历史）

***

## 十、V1.2 回滚确认 + V2.0 大QMT 改造状态（2026-08-31 夜）

### V1.2 回滚（已完成，复验通过）

* [x] 正式模型 `D:\QuantLab\models\lgb_model_v3.txt` = **2837 树 / 27 特征**（V1.2）

* [x] 面板 `data\feature_panel_v3.parquet`（8/27）+ 绑定 `data\model_panel_binding.json` = V1.2（`promote_time: 20260831_rollback`）

* [x] `features_v3.json` = 27 特征，与 V1.2 模型一致

* [x] `verify_model_panel_sync.py` **exit=0**（面板与模型同版）

* [x] 交易通道 connect=0，总资产 10,100,459.99 查询正常

* [x] V1.2 沿用 miniQMT（`D:\国金QMT交易端模拟`，账号 **67014907**）

* [x] V2.0 资产备份：`versions/models/lgb_model_v3_V20_live_20260831.txt`（3017 树/**33 特征**）——**已弃用，仅作备份**，大QMT 迁移**不使用**本模型（信号层用下方 43 特征 g2 模型，见第 188 行；33 特征版从未跑过可执行口径回测，审计 P0-1）

### V2.0 大QMT 改造（2026-08-31 夜启动，方案：文件桥）

> 依据 `specs/miniQMT迁移大QMT实施计划.md`：信号层（LightGBM/43特征）留外部 Python 3.10，交易执行搬大QMT 内置 Python 3.6，`D:/QMT_POOL/g2_bridge/{cmd,state}` 文件桥 + seq 幂等 + pending 状态机（5分钟/3次重试）+ 双账号隔离。新账号 **70180771**（大QMT）跑 V2.0。

* [x] 外部 `qmt_bridge_client.py`（信号层→写 cmd）

* [x] 内置 `build/strategy_p16_g2_bridge.py`（复用 ATR\_EW 脚手架）

* [x] `broker/qmt_builder.py` 构建 GBK 单文件 + py3.6 语法检查 + BUILD\_TAG（产物 `build/strategy_p16_g2_bridge.py`，BUILD\_TAG=20260831-122618，34KB）

* [x] 桥状态机 MOCK/本地测试（seq 幂等 / pending 重试 / 双账号 fail-safe，17 项全过）

* [x] **风控迁移（2026-08-31 拍板三条全迁内置）**：止损/止盈/追盈 `_check_risk_signals`（持仓纳管防孤儿 + get\_full\_tick 秒级判断 + 触发即卖走 pending + fills reason 透传）；成本锚外部每日写 `cmd/positions_cfg_<date>.json`（`qmt_bridge_client.py write-cfg`）；peak 持久化；MOCK 19/19 PASS；BUILD\_TAG=20260831-130648

* [ ] 模拟盘验证：部署大QMT 70180771，核对关键日志（connect/桥读取/委托/对账/**止损触发/防重复**）

* [ ] 小单实盘验证三件套（不易成交挂撤 / 正常成交 / 对账一致）

* [ ] 灰度切换（小资金并行观察 → 全量），旧 miniQMT 保留 ≥1 个月作回滚通道

### 迁移首个交易日（08-31 收盘后）验证结论

* [x] **反查代码格式 bug 已修复**：`_norm_code` 裸码归一化（`SH.600522`/`600522.SH`/`600522`→`600522`），`_lookup_order` 比较失配问题解决（T-20260831-001）

* [x] 修复后重新构建产物 `build/strategy_p16_g2_bridge.py`，**BUILD\_TAG=20260831-145550**，GBK 首行 + py\_compile 通过

* [x] 新产物已部署加载验证：15:00:56 reqid 136 启动，QMT 日志确认 `BUILD_TAG=20260831-145550`（替代旧 130648）

* [x] **幽灵单清理**：旧进程（130648）14:47 遗留的一笔 `P16_20260831_0001 600522 100股` 为"幽灵"pending（heartbeat `order_id/sysid` 空、retry=2；QMT 当日委托查无此单、账户市值 0、无 fills 回写）→ 已清空 heartbeat `pending_count=0`，状态恢复干净

* [ ] **明日（09-01 周二）开盘时段小单验证主链路**：读指令→下单→反查→成交→回写→对账；核对关键日志（BUILD 151459 / CMD / ORDER / FILL / 对账 / 防重复）

* [ ] 待定位疑点：14:47 `passorder ret=0` 成功但当日委托**查无此单** —— **已定位（T-20260831-002）**：代码格式错误。P16 桥 `_to_qmt_code` 把 `600522.SH` 翻转为 `SH.600522`，QMT 主日志实锤 `orderCode:600522SH 不合法!`（QMT 解析 `SH.600522` 为 `600522SH` 判不合法废单，委托从未进通道；ret=0 仅异步接口返回）。ATR（`601985.SH` 08-28 实盘成交）与 Project\_10（`600000.SH`）均为**数字在前**格式。

* [x] **代码格式修复（3 处，BUILD\_TAG=20260831-151459）**：①`_to_qmt_code` 原样返回 `600522.SH`（不再翻转）；②`_norm_code` 改取 6 位数字（原 `split(".")[-1]` 误取交易所后缀）；③风控纳管+持仓快照裸码补 `.SH`/`.SZ` 后缀（原补 `SH.` 前缀）。产物校验：GBK 首行 / py\_compile / 无 `SH.` 前缀残留 全通过。

* [ ] 明日以全新小单复测（应用 151459 产物），确认委托进通道（QMT 日志 `CTradeClient::order` 出现 + 委托界面可见）

* [ ] 小单实盘验证三件套（不易成交挂撤 / 正常成交 / 对账一致）

### 数据源排序优化 + 候选预生成（2026-08-31 落地）

* [x] **数据源 v5 按职责分组**（基础行情锚定腾讯最稳、扩展指标 mcp\_tdx→iFind→东财mx→东财curl、TDX 系不可达不阻塞）——9:25/9:45 任务指令已更新，`MCP数据源配置说明书.md` 第一章 v5 + 第十节同步

* [x] **候选预生成独立任务**（09:15，`候选预生成(项目16)`）：跑 `deploy_predict.py --model v3 --top-k 10`（确定性本地推理，不依赖行情/LLM），9:25 大模型卡住不影响候选

* [x] **9:45 任务检查点调整**：改为优先用预生成 `D_model_top10.csv`（D=面板最新日），缺失才现场 deploy 兜底（与历史行为一致）；实时复核（F2/F3/F5）与 review\_full 排序逻辑不变，价格波动由 9:45 实时复核兜住

- [x] **F3 催化缓存（2026-08-31 追加落地）**：9:25 任务新增 3.5 步——读 v3 候选 top10，采集 F3 催化（新闻/公告）写 `data/cache/review_<date>.json`；[review\_full.py](file:///D:/QuantLab/projects/Project_16_LightGBM股票大师/review_full.py#L114-L131) 加 `_read_catalyst_cache` 兜底分支（实时缺失才用缓存，显式 0 不覆盖，来源标 `+cache`）；**F2 主力资金 / F5 板块涨幅不缓存**（分档敏感、分钟级波动，保持 9:45 实时）。dry-run 验证通过（补 catalyst 1 只、排名与真实一致）

- [x] **F2 资金源修复（2026-08-31，T-20260831-003）**：实锤 300456 虚高 TOP1——TDX 503 降级 iFind 前一日口径打出 F2=10（主力净流入 +5.26 亿），东财实际 8/28 -2.79 亿 / 8/31 当日 -1.25 亿，方向完全相反。**修复**：①任务指令 v6——F2 主力资金当日口径 mcp_tdx→悟道 capital_flow（东财当日四档）→东财mx→东财curl，iFind 仅兜底且必须写 `main_net_inflow_date`；②[review_full.py](file:///D:/QuantLab/projects/Project_16_LightGBM股票大师/review_full.py#L73-L86) 加 `_valid_main_inflow` 时效校验（非当日数据 F2 按缺失 5 分，防旧数据虚高），dry-run 验证 10 只全拦截、300456 86→76 分。配置说明书同步 v6
- [x] **新浪智研 sina-finance 接入（2026-08-31，T-20260831-004）**：token 实测有效，75 工具；**A 股无个股主力资金流（不参与 F2）**，定位 F3 催化（stockNewsSearch）/F5 板块/估值/北向/融资融券**第二顺位**；已接入项目 `.mcp.json`（含 token，已 git rm --cached + gitignore 未 commit）；9:25/9:45 指令 + 配置说明书 v6 已同步
- [x] **盘前交叉验证（2026-08-31，T-20260831-005）**：9:25 任务 3.6 步双源对拍 F2 资金/基础行情/F5 板块写 `data/cache/crosscheck_<date>.json`；[review_full.py](file:///D:/QuantLab/projects/Project_16_LightGBM股票大师/review_full.py#L147-L168) `_read_crosscheck` 读它并对不一致候选打 `[交叉验证]` 预警 + selection_full 明细标注（只预警不阻塞），dry-run 验证 300456 触发 F2 资金预警正确；**单元测试 `research/tests/test_crosscheck.py` 16/16 PASS**（miniqmt venv 运行）
- [x] **全部交易相关定时任务已更新 v6（2026-08-31）**：候选预生成 09:15（deploy_predict --model v3 自动读 V1.3）/ 开盘实时复核 09:45（F2 当日口径+校验+新浪+腾讯优先+交叉验证）/ 集合竞价 09:25（v6+3.5 F3 缓存+3.6 交叉验证）/ **午休持仓报告 11:35 + 盘后持仓复盘 15:40 已同步 v6**（按职责分组 + F2 当日口径 + 新浪备选，修正 iFind 优先旧链）；仅模拟账号盘后监控(f43df15a) 保持 Paused 不参与
- [x] **每日刷新面板落地（2026-09-01，T-20260901-001）**：`run_scheduled.ps1` daily 模式 merge 后加 `refresh_panel_v3.py`（每天增量入库后刷新面板到最新交易日，次日 09:15 候选自动用最新数据）；`verify_model_panel_sync.py` 面板>绑定放宽 ≤7 自然日不告警（周更 promote 归零）；dtype bug 修复 `build_features_v2.py` 4 处日期键统一 ns；**面板已刷新至 8/31（27特征），verify exit=0**
- [x] **悟道改直连客户端（2026-09-03，T-20260903-001）**：排查 `悟道方案复盘_20260903.html` → `/api/openclaw` 404 不存在（报告推断错），真实端点为 `/api/mcp` JSON-RPC（实测 65 工具全通、key 盘中可用）；9-02/9-03 连挂真根因 = 项目 `.mcp.json` 无 mcp_wudao（定时任务环境加载不到全局插件）。修复：新增 `scripts/wudao_client.py` 直连 + `.mcp.json` 加 wudao + key 登记更新 + **9:25/9:45/11:35/15:40 四任务悟道调用全改脚本直连**；客户端实测 market_overview/intraday_main_flow/dragon_tiger 链路健康
- [x] **F3/F5/基础行情加悟道 + 熔断路由（2026-09-03，T-20260903-002）**：悟道加入 F3（official_announcements/research_reports/cls_news）、F5（theme_intraday_capital/sector_analysis）、基础行情（stock_rank/valuation_snapshot）、大盘指数（index_market）做备选源；新增 `scripts/data_source_router.py` 熔断路由（check/record，3 次失败熔断 300s，CLI 测试通过）；9:25/9:45/11:35/15:40 四任务取数规则升 **v8**（全链熔断 + F3/F5 悟道备选）；`MCP数据源配置说明书.md` 升 v8
- [x] **夜间检修任务（2026-09-03，T-20260903-003）**：用户拍板每日 01:00 检修，全模块 A-G。`scripts/nightly_check.py`（数据/面板/调度/数据源熔断恢复/QMT账户/环境/报告告警，--fix 自动修复）+ Schedule「夜间检修(项目16)」e05e526a 01:00 工作日（核对次日 6 任务 + 飞书告警 + 盘前晨报）。**首次 dry-run 抓到 B1 面板 9/2 落后 9/3**（16:30 refresh 未到位），待今晚 --fix 补刷
- [ ] 明日 09-01 验证：09:15 出 `model_top10.csv` → 09:25 写 F3 缓存 + crosscheck → 09:45 直接复核+下单（核对 F2 当日口径、F3 新浪备选、交叉验证标注、QMT 主日志 `CTradeClient::order`）

***

*仅供个人量化研究使用，不构成投资建议。市场有风险。*
