# Project\_16 股票大师 · 新机部署核对清单

> 适用：在新机器（已具备数据源 + QMT + TraeWork）上把「股票大师」策略恢复上线。
> 依据：`SERVER_DEPLOY.md` / `WORKFLOW_DEPLOY.md` / `PROJECT_MEMORY.md` / 2026-08-29 对话。
> 用法：逐项勾选，全部 \[x] 且冒烟验证通过视为部署完成。

***

## 一、代码与数据落位

* [x] 代码目录已迁至新机 `D:\QuantLab\projects\Project_16_LightGBM股票大师\`（含全部 .py 与 .md）

* [x] 主库行情 `D:\astock\daily\stock_daily.parquet` 存在可读

* [x] 财务 `D:\astock\finance\`、基础 `D:\astock\basic\` 已就位

* [x] 周增量目录 `D:\astock\Updatedata\` 已就位，含 `8.24-8.28/`（stock\_daily 33 列同构 + 分钟线 + stock\_basic\_data，2026-08-30 归位；`read_main_daily()` 增量合并验证到 2026-08-28，主仓未整合）

* [x] 股票池 `D:\QuantLab\data\universe_all_a.csv` 已就位

* [x] 模型 `D:\QuantLab\models\`（`lgb_model.txt` / `_v2` / `_v3` + 备份）已就位

* [x] 特征面板 `data\feature_panel_v3.parquet` + `data\features_v3.json` 已就位

* [x] 面板-模型绑定 `data\model_panel_binding.json` 已就位（缺失会导致盘后同步校验报警）

* [x] 账本 `data\qmt_trade_log.csv` + `data\strategy_capital.json` 已就位（含 `account_id=67014907` 戳，续跑需同账号）

## 二、两个配置文件（只改这几处）

* [x] `data_config.py`：`ASTOCK_DIR` = `D:/astock`（新机主库路径）

* [x] `data_config.py`：`UNIVERSE`（股票池路径 `D:/QuantLab/data/universe_all_a.csv`）

* [x] `data_config.py`：`MODEL_DIR` = `D:/QuantLab/models`

* [x] `qmt_config.py`：`QMT_PATH` = `D:\国金QMT交易端模拟`（2026-08-31 用户确认改用此实例；`USERDATA/XTPACK` 自动派生；另一套 `D:\QMT交易端模拟` 已停用关闭）

* [x] `qmt_config.py`：`ACCOUNT_ID` = `67014907`（Project\_16 专属旧号）

* [x] `qmt_config.py`：`LARK_CLI`（lark-cli 可执行文件路径）

* [x] `qmt_config.py`：`FEISHU_OPEN_ID` = `ou_bd13444d8ea53c28249c669f43f3eeff`（清单模板旧值 ou\_76dea 已确认废弃勿用）

* [x] `run_scheduled.ps1`：`FALLBACK_PY` 回退 Python 路径已改为新机实际路径（或 PATH 里的 python 带 numpy+lightgbm）

* [x] `run_scheduled.ps1`：重训备份目标 `D:/QuantLab/models/lgb_model_v3.txt` 与新机路径一致

## 三、Python 环境 + QMT 客户端

* [x] Python 3.10/3.11 已装：`lightgbm pyarrow scikit-learn scipy optuna optuna-integration`

* [x] `python -c "import lightgbm, pyarrow, pandas; print('env OK')"` 通过

* [x] QMT 客户端已登录 `67014907`（模拟/实盘账号）；实际安装 `D:\国金QMT交易端模拟`（2026-08-31 链路实测：行情 miniqmt 源 + 交易 connect=0，资产/持仓/委托查询全通）

* [x] `bin.x64\Lib\site-packages\xtquant` 存在且含匹配 Python 版本的 pyd（cp310/311）

* [x] QMT 客户端已设开机自启、常驻登录（注册表 HKCU\Run 指向 `D:\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe`，2026-08-31 从原 `D:\QMT交易端模拟` 改指）

## 四、TraeWork 内配置

* [x] TraeWork 已打开工作区 `D:\QuantLab`（加载 AGENTS.md 规则 + 知识中心）

* [x] MCP 数据源已授权/可调用（2026-08-31 逐项实测，缺失会导致复核/报告取数失败）：
  * [x] **mcp\_tdx**（通达信 TDX MCP）— 实测 `tdx_quotes` 可用，9:45 开盘复核 / `review_full.py` 必需

  * [x] **iFind**（同花顺 iFinD MCP）— 实测 `search_stocks` 可用，主力资金（日频）

  * [x] **悟道 mcp\_wudao** — 已配置可用（2026-08-31 实测 market\_overview 市场宽度 + dragon\_tiger 龙虎榜 50 条）；60+ 工具（公告/龙虎榜/涨停梯队/热榜/题材/盘中主力/指数）

  * [x] **TuShare**（tushareMcp）— 实测可用（研究用，220+ 接口）

  * [x] **盈米 yingmi** — 已装（基金/组合诊断）

  * [x] **东方财富 mx-ds-mcp** — 已装（财务/筛选/新闻）

  * [x] 腾讯财经 API（脚本直连 `qt.gtimg.cn`）— 实测连通

* [x] 飞书 lark 已授权/配置（推送前须移除 `LARKSUITE_CLI_APP_ID`/`LARKSUITE_CLI_USER_ACCESS_TOKEN` 并设 `LARKSUITE_CLI_STRICT_MODE=off`，bot 身份）

* [x] Schedule「集合竞价增强预判(项目16)」已建：cron `25 9 * * 1-5`，message 第一步跑 `is_trade_day.py`

* [x] Schedule「开盘实时复核(项目16)」已建：cron `45 9 * * 1-5`，`rebalance_daily.py --live`，第一步跑 `is_trade_day.py`

* [x] Schedule「午休持仓报告(项目16)」已建：cron `35 11 * * 1-5`，第一步跑 `is_trade_day.py`

* [x] Schedule「盘后持仓复盘(项目16)」已建：cron `40 15 * * 1-5`，第一步跑 `is_trade_day.py`

## 五、Windows 计划任务（9 个，全部指向新路径 `run_scheduled.ps1`）

* [x] `Quant_Monitor_0945 / 1030 / 1100 / 1330 / 1400 / 1430` → `-Mode monitor`

* [x] `Quant_Daily_Update` 交易日 16:30 → `-Mode daily`

* [x] `Quant_Weekly_Retrain` 周一 17:00 → `-Mode retrain`

* [x] `Quant_Monthly_Factor` 每月1日 20:00 → `-Mode factor`

* [x] 确认无任务残留指向旧路径 `D:\trae_workspace\...`（旧 13:00 任务已删）

## 六、冒烟验证（逐项通过）

* [x] 配置读取：`python -c "import data_config as D, qmt_config as C; print(D.MAIN_DAILY); print(D.model_file('_v3')); print(C.XTPACK); print(C.ACCOUNT_ID)"` 输出新机路径/账号

* [x] 主库可读：`python -c "import pandas as pd; df=pd.read_parquet('D:/astock/daily/stock_daily.parquet', columns=['close']); print(df.index.get_level_values('trade_date').max())"`（主仓至 2026-08-21，增量合并至 08-28）

* [x] 模型可加载：`python -c "import lightgbm as lgb; m=lgb.Booster(model_file='D:/QuantLab/models/lgb_model_v3.txt'); print(m.num_trees())"`（2837 树）

* [x] 交易/行情通道：`python qmt_link_test.py`（查资产/持仓，不下单，connect=0 通过）

* [x] 选股链路：`python deploy_predict.py --model v3 --top-k 5`

* [x] 面板-模型同步：`python verify_model_panel_sync.py` 退出码 0

* [x] 账本校验：成交记录/资金池 account\_id 与 `67014907` 一致（不一致会自动备份并空仓起步）

## 七、每年维护项

* [ ] 每年 1 月更新 `is_trade_day.py` 法定节假日休市表

* [ ] 主库周更重建面板后，必须 `promote_model.py` 提升候选模型（否则盘后同步校验报警）

***

## 八、2026-08-30 D:\tmp 采购数据归位记录

> 用户下载新一批采购数据到 `D:\tmp`，要求：非行情数据迁移进主库，行情数据增量不整合主仓、走 `Updatedata` 增量目录由 `read_main_daily()` 合并调用。

| 源（D:\tmp）                                            | 目标（D:\astock）                         | 说明                                               |
| ---------------------------------------------------- | ------------------------------------- | ------------------------------------------------ |
| `行情数据（增量数据8.24-8.28）\stock_daily.parquet`            | `Updatedata\8.24-8.28\`               | 33 列同构主仓，5 交易日 27741 行（日 5547±）；**不整合主仓**，增量合并生效 |
| 同上 `stock_{1,5,15,30,60}min_data.parquet`            | `Updatedata\8.24-8.28\`               | 各 1 份（`(1)` 副本 hash 相同已去重）                       |
| 同上 `stock_basic_data.parquet`                        | `Updatedata\8.24-8.28\`               | 完整股票基础信息（17682 行）                                |
| `特色数据更新至2026.8.28\report_rc_daily.parquet`           | `research\`                           | 932379 行，覆盖旧 8.21 版（已备份 `.bak_0821`）             |
| `特色数据更新至2026.8.28\stock_auction_{o,c}_daily.parquet` | `auction\`（新建）                        | 竞价开盘/收盘，2009 起\~8/28                             |
| `筹码分布数据\更新到20260821\`（5569 拆分）                       | `chip\` + 合并 `chip\cyq_daily.parquet` | 合并单文件 1854 万行 2018\~8/21（代码消费路径），保留拆分            |
| `指数权重数据\parquet格式20260807\index_weight_*.parquet`    | `index_weight\`                       | 2067 文件（剔除 \_progress\*.json）                    |
| `parquet格式指数数据\指数数据更新至2026.8.21\`（4 文件）              | `index\`（新建）                          | ci\_l1/ci\_l2/index\_basic/sw\_l1 完整快照           |
| `...\指数数据更新至2026.8.7\index_1min\`                    | `index\index_1min\`                   | 720 文件                                           |
| `...\指数数据更新至2026.8.7\index_daily\`                   | `index\index_daily\`                  | 321 文件（补迁）                                       |
| `...\指数数据（增量数据8.10-8.14 / 8.17-8.21 / 8.24-8.28）\`   | `index\increment\<周>\`                | 各 11 文件，增量形式存放                                   |
| `...\9.30 分钟线补充包\index_5min_0930_patch.parquet`      | `index\`                              | 1 份                                              |
| `parquet 20260807\ggt_top10_full.parquet`            | `northbound\`                         | 港股通十大成交 2014 起                                   |
| `parquet 20260807\hsgt_top10_full.parquet`           | `northbound\`                         | 沪深股通十大成交 2014 起                                  |
| `parquet 20260807\hk_hold_full.parquet`              | （跳过）                                  | 主库 northbound\ 已有同版本（5720422 行一致）                |
| `打板专题…`、`资金流向…`                                      | （跳过）                                  | 空目录                                              |
| `parquet行业概念板块…` + .zip                              | （跳过）                                  | 已归位 `board\source\`                              |

**验证**：`read_main_daily()` 合并后 2009-01-05 \~ 2026-08-28（8/24-8/28 27741 行）；主仓 `daily\stock_daily.parquet` 仍止 2026-08-21 未整合；research/auction/chip/northbound/index 全部可读。

***

## 九、2026-08-31 MCP 数据源实测核对（第四节「四、TraeWork 内配置」补充）

> 按清单第四节要求逐项实测（本机直接调用，非只看配置）：

| 清单要求数据源                                      | 状态      | 实测依据                                                                                       | <br /> |
| -------------------------------------------- | ------- | ------------------------------------------------------------------------------------------ | :----- |
| **mcp\_tdx**（通达信 TDX）                        | ✅ 可用    | `tdx_quotes(600519)` 返回实时行情/盘口/涨跌停/估值（20260828）                                            | <br /> |
| **iFind**（同花顺 iFinD）                         | ✅ 可用    | `search_stocks` 返回贵州茅台 600519.SH 收盘 1297.4（20260828）                                       | <br /> |
| **TuShare**（tushareMcp）                      | ✅ 可用    | `daily`/`adj_factor` 与主库逐字段 0 差异；token 有效（免费档限频，仅研究用）                                      | <br /> |
| **腾讯财经 API**（qt.gtimg.cn 脚本直连）               | ✅ 可用    | 实测返回浦发银行实时行情                                                                               | <br /> |
| **盈米 yingmi**                                | ✅ 已装    | 插件在位（基金/组合诊断）                                                                              | <br /> |
| **东方财富 mx-ds-mcp**                           | ✅ 已装    | 插件在位（财务/筛选/新闻）                                                                             | <br /> |
| **悟道 mcp\_wudao**（公告/龙虎榜/涨停梯队/热榜/题材/盘中主力/指数） | ✅ 已配置可用 | 2026-08-31 用户配置后实测：`market_overview`（涨3013/跌2390/强度68）+ `dragon_tiger`（08-28 龙虎榜50条，榜首嘉立创） | <br /> |

**说明**：

* 原清单模板残留两处**不应按清单改**：① `FEISHU_OPEN_ID=ou_76dea...` 是旧值（本机代码用 `ou_bd13444...`，注释明确 ou\_76dea 跨应用无效勿用）；② 主库路径 `E:\astock`（本机无 E 盘，实际 `D:\astock`）。

* **明日待办**：悟道 mcp\_wudao 安装（其余数据源已全部就绪）。

***

## 十、2026-08-31 财汇 MCP 接入 + V2.0 策略上线

### 财汇（大智慧企业预警通）接入

* key 已登记 `config/data_source_keys.json`（caihui）；**项目客户端** `scripts/caihui_client.py` 已实现（HTTP JSON-RPC + x-api-key），38 个一级工具三层架构全部验证：

  * 股票分红 `get_stock_dividend_details` ✅、财务 `get_company_financial_metrics/notes/主营构成` ✅、股权实控人 `get_company_actual_controller` ✅、公告 `search_company_announcements` ✅、新闻 `search_news` ✅、企业档案/信用/监管处罚/债券/基金/指数/行情/宏观/法规 ✅

  * **受限**：司法诉讼 `get_company_judicial_cases` → PERMISSION\_DENIED（需更高财汇订阅权限）

* 应用级 MCP（run\_mcp 直调）未接入（`.workbuddy/.mcp.json` 超出工作区可写范围，需 UI 添加 caihui\_mcp）

### V2.0 策略正式上线（替换 V1.2）

* 面板重建至 8/28（主仓+Updatedata 增量合并，33 特征 + N3 对齐标签）→ `feature_panel_v3.4_latest.parquet`

* 重训模型 promote → `lgb_model_v3.txt`（3017 树，验证IC 0.106 / testIC 0.070 / ICIR 0.50）

* `verify_model_panel_sync.py` exit 0，`deploy_predict --model v3` 正常

* V1.2 备份：`versions/models/lgb_model_v3_pre_promote_20260830_160209.txt` + `*_pre_V20_20260830_160142.*`

* 重建脚本保留：`scripts/_tmp_rebuild_v20_panel.py`（可复现）、`scripts/_tmp_predict_v20.py`（V2.0 选股）

> ⚠️ **2026-08-31 已回滚 V2.0→V1.2（用户确认）**：V1.2 保运行（miniQMT 67014907），V2.0 备份 `versions/models/lgb_model_v3_V20_live_20260831.txt` 准备大QMT 改造（70180771）。当前正式模型 `lgb_model_v3.txt` = **V1.2（2837 树/27 特征/面板 8/27）**；V2.0 上线记录保留作改造依据。

***

## 十一、面板-模型同步防呆（2026-08-30 更新）

> 事故背景：2026-08-14 起生产面板 `feature_panel_v3.parquet` 停更，模型输入特征冻结，选股文件同票 `model_prob` 跨日完全一致（如 000737 连续三天 0.6452）而毫无告警。根因：周更重训只重建 v2 面板，v3 靠手工切片命令不在自动化里。本防呆机制三件套必须随项目拷贝、换机后逐项核对：

* [ ] 三件套已就位：`data\feature_panel_v3.parquet`（生产面板）、`D:/QuantLab/models/lgb_model_v3.txt`（正式模型，只读）、`data\model_panel_binding.json`（绑定记录）

* [ ] 面板重建自动化：`refresh_panel_v3.py`（合并主库+增量→重建 v2→切片 v3）已接入 `run_scheduled.ps1 -Mode retrain`（周一 17:00）

* [ ] Promote 门禁：`promote_model.py` 为唯一写生产模型入口，提升时自动写绑定记录；重训只产出候选 `lgb_model_v3_retrain_<date>.txt`，不自动覆盖正式模型

* [ ] 每日校验：`run_scheduled.ps1 -Mode daily`（交易日 16:30）末尾跑 `verify_model_panel_sync.py`，面板最新日 vs 正式模型绑定日不一致 → exit 1 + 日志告警

* [ ] 部署后冒烟：`python verify_model_panel_sync.py` 输出「校验结果: 一致」且退出码 0

* [ ] 每周操作节奏：周一 retrain 日志提示「请 promote 今日候选」→ 人工确认后 `python promote_model.py --model-file D:/QuantLab/models/lgb_model_v3_retrain_<date>.txt --suffix v3 --yes --note "说明"`

**异常处置速查**：

| 现象                             | 处置                                                                                            |
| ------------------------------ | --------------------------------------------------------------------------------------------- |
| daily 日志 `!! [模型-面板同步] ...不一致` | 跑 `verify_model_panel_sync.py` 看差异；「面板更新未 promote」→ 按上面 Promote 命令提升今日候选；绑定缺失 → 重跑 Promote 补齐 |
| 「正式模型树数 != 绑定」/「mtime 不一致」     | 正式模型被绕过门禁直接改动 → 从 `versions/models/` 恢复受控版本重新 Promote                                         |
| 选股文件同票 `model_prob` 跨日完全一致     | 面板停更 → 跑校验、确认面板与绑定日是否脱节，脱节则重训+Promote                                                         |

> 完整配置手册：`PANEL_SYNC_部署配置说明.md`（项目根目录与 `D:\QuantLab\` 根目录均有副本）。

***

## 十二、数据链路 8 级降级配置 + 降级修复（2026-08-31 更新）

> 依据《MCP数据链路说明.md》：实时链路 8 级降级链（mcp\_tdx → 腾讯 → iFind → 东财mx → 东财curl → full-link → 独立TDX → miniQMT）+ 信息面悟道并行。本机已按该链配置并修复降级健壮性。

* [x] **策略脚本层**（`data_sources.py`，运行时自动取数）：优先级 `miniqmt → 腾讯 → tdx文件`（TDX 经外部采集写 `tdx_review.json` 供读取，非直连 MCP）

* [x] **Agent 任务层 8 级链已落入 TraeWork 任务**：`开盘实时复核`(09:45) 步骤1 与 `集合竞价增强预判`(09:25) 步骤3 均已写明 8 级降级链 + 信息面悟道并行；前一级失败即降级，TDX 不可达时改用腾讯/iFind/东财并**如实标注来源、不编造缺失字段**

* [x] **降级 BUG 已修复**：`fetch_minqmt` 的 `get_full_tick` 异常原未捕获（miniQMT 行情服务未启动/断连时 `fetch_all` 直接崩溃）→ 已包 try/except 降级到腾讯；实测 4 只测试股 miniQMT 失败后全部从腾讯取到真实行情（退出码 0）

* [x] **TDX 已恢复（2026-08-30 实测）**：远程 `mcp_plugin_TDX_tdx`（tdx-connector，`txmcp.tdx.com.cn:3001`）恢复可用，`tdx_lookup_stock`/`tdx_quotes`（茅台现价 1297.4 + 五档 + PE 18.22 + 涨停/跌停价）实测通过 → 8 级链第 1 级回归，9:25 竞价（`call_auction`）/大盘风控（`tdx_get_index_quote("000300")`）恢复。此前「降级运行」阶段已解除

* [x] **本地 tdx-mcp 已重装（独立 venv）**：`D:\QuantLab\mcp\tdx-mcp-repo`（GitHub Neoooo0909/tdx-mcp 源码，绕过失效 git 代理直连克隆）→ 重装到独立 venv `D:\QuantLab\mcp\tdx-mcp-venv`（Python 3.13 隔离，仅 tdx-mcp 0.1.0 + eltdx 0.5.1 + mcp 1.29.1，避免污染其他 Python）；`start.bat` 指向该 venv 的 `tdx-mcp.exe`；端到端验证 23 工具 + 茅台完整行情（现价/开高低/量额/五档）。备用通道未挂载会话（如需在 TraeWork MCP 管理添加 stdio `D:\QuantLab\mcp\tdx-mcp\start.bat`）

* [x] **Agent 任务层全部按文档配置实时取数链**：09:25（8级链 + `tdx_get_call_auction` 直读 + `tdx_get_auction_0925` + 悟道四工具）/ 09:45（8级链 + 大盘风控 `tdx_get_index_quote` + 真实下单）/ 11:35（8级链 + 资金流 iFind→东财mx→东财curl + 悟道午间）/ 15:40（8级链 + 资金流顺序 + 悟道盘后）/ 模拟账号盘后监控（TDX + 腾讯/iFind 降级）——全部按《MCP数据源配置说明书》第十节对齐

* [ ] **待办**：① 本地 tdx-mcp 如需作双保险，在 TraeWork MCP 管理添加 stdio `D:\QuantLab\mcp\tdx-mcp\start.bat`；② 将《MCP数据链路说明.md》中「独立 TDX 已恢复可用」与 `E:\astock` 两处过时信息修正为实测状态

***

*仅供个人量化研究使用，不构成投资建议。市场有风险。*
