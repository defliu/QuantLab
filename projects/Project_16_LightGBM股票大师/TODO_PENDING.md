# 待执行清单

> 本清单记录暂时搁置、需要在合适时机执行的任务。每项含背景与执行方式，任何会话接手均可直接执行。

---

## [已关闭] 核对模拟盘交易费用与真实费率是否一致

- 状态：**已关闭**（2026-08-22，用户决策）
- 关闭原因：模拟盘只是验证流程，**收益结算统一按实盘交易数据（真实费率）**，无需与模拟盘费率对齐。

### 结算口径（已落地）
统一按实盘费率结算（用户 2026 实测）：佣金 万2 双边最低5元 / 印花税 万5 仅卖出 / 过户费 万0.1 仅沪市双边。已配置在 `qmt_config.py`（COMM_RATE/COMM_MIN/STAMP_RATE/TRANS_RATE），并应用到：
- `strategy_capital.py`：已实现盈亏 = 卖出净回笼 − 买入含费成本（FIFO 扣费）
- `scan_rotate_cost.py`：回测成本模型引用 `qmt_config` 同口径

### 原执行方式（不再需要）
- ~~交易时段 xtquant 查成交明细反推模拟盘费率~~
- ~~请用户提供 GUI"交易费用"栏费率~~

---

## [待执行] 修正回测口径重跑，判定策略是否可实盘

- 状态：**待执行**（2026-08-22，用户决策换设备执行）
- 背景：第三方审计（`第三方审计报告_20260822.md`）与验证（`第三方审计验证报告_20260822.md`）已确认 **P0-1 回测口径与实盘系统性错位**，需重写可执行口径回测后重判。

### 已核实的证据（接手必读，勿重复发现）
- 官方 `scan_rotate_cost_report.md` 宣称日均超额 **+0.676%**（0滑点，close→close：T收盘买入、T+1收盘卖出）；
- 验证复现：可执行口径（open→open：T+1开盘买入、T+2开盘卖出 + 一字板过滤 + 真实成本）日均超额 **-0.109% / -0.193% / -0.278%**（滑点 0/0.1%/0.2%），**全部转负**；
- 成因：① 隔夜跳空（T收→T+1开）均值 +0.79%/笔，实盘 9:45 进场吃不到；② 5.76% 选股 T+1 一字涨停买不进（贡献约 64% 回测利润）；③ 每日换仓双边成本约 0.1%/天；
- 模型排序能力本身真实（滚动 IC 均值 0.052、ICIR 1.40），问题在执行口径，不是模型失效。

### 执行步骤（按序）
1. **写正式的可执行口径回测脚本**（放项目根目录，如 `backtest_executable.py`）：
   - 选股逻辑 1:1 复刻 `scan_rotate_cost.py`（前100池 → 前10池 → 红线 → top2 by total_new，评分卡 F1-F6 权重同 `deploy_predict.py`/`review_full.py`）；
   - 收益口径 **open→open**：T+1 开盘买入、T+2 开盘卖出（持有期 N 可调），买入价 = `daily.open`，一字板（T+1 开盘 ≥ `up_limit`×0.9999）跳过该候选、尝试下一个；
   - 成本：佣金万2双边最低5元 + 印花税万5卖出 + 过户费万0.1沪市 + 滑点 0/0.1%/0.2% 三档（常量引用 `qmt_config.py`）；
   - 基准：全市场 open→open 日收益均值；
   - 输出：日均超额 / 胜率 / 盈亏比 / 最大回撤 / 一字板跳过数，报告落盘 `data/backtest_executable_report.md`。
2. **参数重扫（必须用可执行口径）**：红线（50~65）、TOP（1~5）、持有期 N（1/3/5/10）全部在可执行口径下重扫，禁止再用 close→close 口径的历史最优参数（原 58/2/1 均已失效）。
3. **复权重建 + 重训后再测**（P1-2）：`build_features_v2.py` 的 `close` 改为 `close × adj_factor` 重建面板 v1/v2/v3 → `train_optuna.py --panel v3` 重训 → 重复步骤 1/2。
4. **判定门槛**：可执行口径（0.1% 滑点档）日均超额 **≥ +0.3%** → 策略可继续投入修复与实盘；**< +0.3% 或为负** → 停止当前方案，考虑改执行时点（如 14:50 尾盘买入缩小隔夜错位）或重构，不得以旧口径数字加仓。

### 参考文件
- 审计报告：`第三方审计报告_20260822.md`（P0-1/P1-2/P1-4 为改造依据）
- 验证报告：`第三方审计验证报告_20260822.md`（含已验证的复现逻辑与数字）
- 现有脚本：`scan_rotate_cost.py`（选股/成本逻辑参考）、`deploy_predict.py`/`review_full.py`（评分卡）、`qmt_config.py`（费率）、`data_config.py`（路径）
- 数据：面板 `data/feature_panel_v3.parquet`、模型 `D:/QuantLab/models/lgb_model_v3.txt`、日线 `E:/astock/daily/stock_daily.parquet`（含 `open/up_limit/adj_factor` 字段）
