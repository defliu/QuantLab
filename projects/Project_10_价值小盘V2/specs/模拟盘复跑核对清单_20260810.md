# P10 模拟盘复跑核对清单 — 2026-08-10（周一）

> 前置：2026-08-09 已部署修复版 build=20260809-005102（4处在途守卫+reconcile每日闸门），state 已备份重置（冷启动 10 万）。
> 8月为偶数月非正常调仓月，冷启动持仓 0% <60% 将走**补仓路径**全量建仓（拍板A保留 MIN_POSITION_PCT=0.6，属预期行为）。

## 一、复跑前检查（开 QMT 前）

- [x] `E:\国金QMT交易端模拟\python\STRATEGY_V2.py` = build 20260809-005102（旧版已备份 .bak）
- [x] `D:\QMT_POOL\v2_holdings_state.json` 已移除（旧污染态备份为 `v2_holdings_state_20260806_buggy.bak.json`）
- [ ] `D:\QMT_POOL\` 下 8 个 CSV 存在且为周五 18:30 计划任务产出（pb/circ_mv/total_mv/industry_map/bp_hist_pct/delist_info/pe_ttm/selected.txt）
- [ ] QMT 模拟端已登录账号 67014907，miniQMT 在线

## 二、启动后即刻核对（init）

- [ ] `[init] 策略初始化完成, 持仓=0, build=20260809-005102`（持仓=0 + 新 tag，两条缺一不可）

## 三、盘中核对重点（本日核心验证项）

### 1. 无重复下单（异常3回归验证）
- [ ] 同一 code 同方向 `[buy]`/`[sell]` 日志在 3 分钟内**最多出现 1 次**
- [ ] 若出现第 2 次，必须前有 `[pending] ... 重下` 行（即走的是撤单重下，旧单已撤）
- [ ] **判失败**：任何 code 出现连续多条 `[buy]`/`[sell]` 且无 `[pending]` 重下行 → 立即停止策略

### 2. buffer / 排雷日志（v2.3 行为验证）
- [ ] 建仓批次出现 `[buffer] 排雷剔除N只 | 卖出超界(>160)持仓M只 | 暂缓K只`（冷启动 M 应为 0，N 可能>0）
- [ ] 被排雷剔除的票不在买入列表中（可抽查 1-2 只剔除票核对）

### 3. reconcile 每日一次（Fix2 验证）
- [ ] 14:50 后 `[reconcile] 对账完成` 全日**仅出现 1 次**
- [ ] 对账后 `[hb]` 心跳中的持仓数/现金不再跳变

### 4. 挂单巡检（既有机制回归）
- [ ] 未成交单 3 分钟后出现 `[pending] ... 重下`，同一 code 重试 ≤3 次
- [ ] 重试耗尽出现 `[pending] ... 放弃` + 回滚日志（若有）

## 四、收盘后核对

- [ ] `D:\QMT_POOL\v2_holdings_state.json` 已生成，持仓数与日志最终持仓一致
- [ ] `strategy_log_v2.txt` 全日无 `[sell error]`/`[buy error]`/`[reconcile error]`（预期少量 `[pending]` 正常）
- [ ] 导出当日委托/成交 CSV 到 `D:/QMT_POOL/`（模拟端隔日查不到，刚需）

## 五、通过标准与失败处置

**通过**：三.1 无任何无标记重复单 + 三.3 reconcile 仅 1 次 + 四全部打勾 → 异常3修复宣布模拟盘验证通过，转入 P1（rollback 活单）修复排期。

**失败**：任一判失败项触发 → QMT 停止策略 → 回报日志关键行 → 回滚部署（`.bak` 恢复）→ 重新定位。

## 六、已知预期内现象（勿误报）

- 开盘后首批 ~80 只集中买入：补仓路径，预期行为
- 模拟端市价单可能不成交：`[pending] 重下` 是设计行为，看重下次数 ≤3
- 盘中若有个票触发止损（8%）卖出：设计行为（每 bar 止损检查），调试模式时间窗已解锁
