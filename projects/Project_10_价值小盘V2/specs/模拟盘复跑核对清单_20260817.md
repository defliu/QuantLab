# P10 模拟盘复跑核对清单 — 2026-08-17（周一）

> 背景：8/14 收盘对账 P0 修复（reconcile 三态兜底）+ 8/14 晚 P1 修复（回滚前撤活单 + 跌停/停牌进暂缓队列）已合入 build **20260814-210622**（MOCK 48/48），本周一为修复后首个完整交易日验证。
> 硬前提：账户 67014907 当前堆叠 69 只 V2 脏仓（约 28 万市值 > 10 万子账户），**必须先清仓再复跑**（T-20260814-003）。

## 执行顺序总览（6 步）
1. 盘前检查（09:00 前）→ 2. 撤残留挂单 + 部署新 build（09:15 后）→ 3. 清脏仓（09:30 后）→ 4. 启动复跑 → 5. 盘中核对（全天）→ 6. 收盘核对 + 可选 debug 验证 + 看板更新

---

## 一、盘前检查（开 QMT 前）
- [ ] `D:\QMT_POOL\` 下 8 个 CSV 存在且为周五 18:30 计划任务产出（pb/circ_mv/total_mv/industry_map/bp_hist_pct/delist_info/pe_ttm/selected.txt）
- [ ] `E:\国金QMT交易端模拟\python\STRATEGY_V2.py` 已替换为 build **20260814-210622**（旧版已备份 .bak）
- [ ] QMT 模拟端已登录账号 67014907，miniQMT 在线

## 二、开盘后第一步：撤残留挂单（09:15 后、策略启动前）
- [ ] 运行 `projects\Project_10_价值小盘V2\cancel_pending_cleanup.py`，撤掉 8/14 遗留的 7 笔 status=50 挂单（防修复版开盘意外成交）
- [ ] QMT 客户端委托页确认残留挂单清零

## 三、09:30 后清脏仓（T-20260814-003）
- [ ] `cleanup_account.py --dry-run` 复核：账户 69 只全为 V2 脏仓（ATR 空仓/Project_01 清零已确认）
- [ ] `pythonw.exe cleanup_account.py --execute` 卖光 69 只
- [ ] QMT 客户端确认持仓 = 0
- [ ] 重置 `D:\QMT_POOL\v2_holdings_state.json`（cash=100000/空仓，旧态备份 .bak）

## 四、启动复跑
- [ ] 启动 STRATEGY_V2.py，init 日志：`[init] 策略初始化完成, 持仓=0, build=20260814-210622`（持仓=0 + 新 tag 缺一不可）
- [ ] 冷启动持仓 0% <60% → 走**补仓路径**全量建仓（预期行为，与 8-10 轮相同）

## 五、盘中核对（核心验证项）

### 1. 2026-08-14 两轮修复的功能验证
- [ ] **reconcile 三态兜底（P0，build 151539）**：14:50 对账时，当日已成交买入**不得**被误判「未成交」→ 撤销 → 尾盘重复建仓；`[reconcile] 对账完成` 全日仅 1 次；对账后 `[hb]` 心跳持仓数/现金不跳变
- [ ] **回滚前撤活单（P1，build 210622）**：若某单重试 3 次耗尽回滚，日志必须**先** `[cancel] 撤单 xxx order_id=xxx` **再**出现回滚恢复持仓（当日无超时场景则标 N/A，MOCK Test12 覆盖）
- [ ] **跌停/停牌进暂缓（P1，build 210622）**：若卖出回滚后仍跌停/停牌 → 日志 `[pending] xxx 卖出放弃但跌停/停牌，进暂缓队列`，解封后自动补卖（当日无则 N/A，MOCK Test13/15 覆盖）

### 2. 历史机制回归（20260809-005102 已通过项）
- [ ] 无重复下单：同一 code 同方向 `[buy]`/`[sell]` 3 分钟内最多 1 次；第 2 次必须前有 `[pending] 重下` 行
- [ ] **判失败**：任何 code 连续多条 `[buy]`/`[sell]` 且无 `[pending]` 重下行 → 立即停止策略
- [ ] 挂单巡检：未成交单 3 分钟 `[pending] 重下`，同 code 重试 ≤3 次；耗尽 `[pending] 放弃`
- [ ] buffer/排雷：建仓批次 `[buffer] 排雷剔除N只 | 卖出超界(>160)持仓M只 | 暂缓K只`

## 六、收盘核对（15:00 后）
- [ ] `D:\QMT_POOL\v2_holdings_state.json` 已生成，持仓数与日志最终一致
- [ ] 全日无 `[sell error]`/`[buy error]`/`[reconcile error]`（预期少量 `[pending]` 正常）
- [ ] 导出当日委托/成交 CSV 到 `D:/QMT_POOL/`（模拟端隔日查不到，刚需）
- [ ] 收盘对账：账户 position vs 记账一致——无孤儿持仓、无重复成交、总市值 ≤ 10 万

## 七、可选：debug_pending.py 委托链路验证（约 5 分钟，非必须）
- [ ] **buy 模式**（默认不改）：加载 `projects\Project_10_价值小盘V2\build\debug_pending.py` → 期望 `[结果] 全部成交! 100股`（600016 100 股），验证 下单→反查→成交识别 链路
- [ ] **sell 模式**（可选）：源文件改 `TEST_SIDE = "sell"` + `TEST_CODE` = 当日 V2 新持仓中一只 → 期望 `[sell] 账户持仓校验通过` → `[结果] 全部成交`；**红线：只能卖 V2 自己的持仓，ATR 的票绝不能动**
- [ ] 改动源文件后必须重建 GBK 产物：`python projects\Project_10_价值小盘V2\debug_pending_build.py`（产物 `build\debug_pending.py`，首行 `# coding=gbk`）
- [ ] 日志看 `D:\QMT_POOL\debug_pending_log.txt`
- [ ] 测试买入的 100 股 600016 会留持仓，次日记得处理（或纳入对账）

## 八、通过标准与失败处置
**通过**：三清零 + 五.1 无异常（当日无场景的项标 N/A 注明 MOCK 覆盖）+ 五.2 全过 + 六全勾 → 2026-08-14 两轮修复（reconcile 三态 / 回滚撤活单 / 跌停暂缓）模拟盘验证**通过**。
**失败**：任一判失败项触发 → QMT 停止策略 → 回报日志关键行 → `.bak` 回滚 → 重新定位。

## 九、顺带任务（ATR 项目，时间允许再做）
- [ ] T-20260814-005：部署 ATR 8只+max_price=50（`projects\Project_ATR_lowvol\build\strategy_atr_lowvol_equalweight.py`）到模拟端，跑 1 交易日：`筛选完成...(高价排除N)` + 持仓≈8只
- [ ] T-20260814-006：双策略共存核对——ATR≤8 / V2≤80 / 无孤儿 / 无重复下单
- [ ] 过完 8 项 checklist 后更新看板 T-20260814-005/006

## 十、完成后收尾（硬规则：不更新看板=工作没做完）
- [ ] 更新 `D:\QuantLab\全局控制台.md`：T-20260814-003 → DONE（附验证结果）；T-20260809-002 待验证 → 验证结论
- [ ] 更新 `AGENTS.md` 策略项目状态表（P10 行）
- [ ] **决策点**：模拟盘验证通过后是否部署真钱（10 万额度）——需诚哥拍板

## 十一、已知预期内现象（勿误报）
- 开盘后首批 ~80 只集中买入：补仓路径，预期行为
- 模拟端市价单可能不成交：`[pending] 重下` 是设计行为，看重下次数 ≤3
- 盘中若有个票触发止损（8%）卖出：设计行为
- 8/14 的 62 笔废单（17:09 收盘后手动下单）不会再现：策略交易窗口已收窄至 14:56
