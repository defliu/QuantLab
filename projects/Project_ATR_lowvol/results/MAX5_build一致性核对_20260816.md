# MAX5 build 落地 · 参数一致性核对（T-20260816-001 T4）

> 日期：2026-08-16。归属：审计修复任务书 T4（P1）。本轮只改 build 源文件与仓库 config，**不上传 QMT**；
> QMT 上传并入周一 2026-08-17 同跑核对（T-20260814-007）。

## 一、改了什么

| 文件 | 改动 |
|---|---|
| `projects/Project_ATR_lowvol/build/strategy_atr_lowvol_equalweight.py` | 新增 `max_exclude_pct`（默认 0.20，实现口径与 `strategy/atr_lowvol.py` L216-224 一致：当日收盘取近 20 日最大单日涨幅 MAX5，剔除最高 `max_exclude_pct` 分位、次日生效）；同步加 `BUILD_TAG = 20260816-141355`（部署核对用，init 日志输出） |
| `config/atr_lowvol_equalweight_config.yaml` | `screening` 下显式声明 `max_exclude_pct: 0.20` |

**未触碰**：`D:\QMT_STRATEGIES`（任务红线）、`capital_allocation.yaml`、任何实盘/模拟盘指令。

## 二、三方一致性核对

| 参数 | ①build 默认（内置 CONFIG） | ②config/atr_lowvol_equalweight_config.yaml | ③研究基线 atr_10w_price50_a_max.yaml | 一致 |
|---|---|---|---|---|
| n_hold | 8 | 8 | 8 | ✅ |
| max_price | 50.0 | 50.0 | 50.0 | ✅ |
| max_exclude_pct | 0.20 | 0.20 | 0.20 | ✅ |

> **说明**：实盘运行时 build 读 `D:/QMT_STRATEGIES/config/atr_lowvol_equalweight_config.yaml`（该副本本轮按任务红线不动，无 `max_exclude_pct` 键）→ `_load_config` 未命中该键时回落到 build 内置默认 **0.20**，生效值仍与三方一致。周一同步时把该键补上即为显式一致。

## 三、校验结果

- **编码**：GBK 编码 ✓（首行 `# coding=gbk`，全文件 CRLF，无裸 LF）
- **语法**：`ast.parse(feature_version=(3,6))` 通过 ✓（Python 3.6.8 QMT 兼容）
- **原子写入**：temp + `os.replace` ✓；原文件备份 `strategy_atr_lowvol_equalweight.py.bak_20260816-141355` ✓
- **配置解析冒烟**：`_load_config` 读含 `max_exclude_pct` 的 config → 生效 `_MAX_EXCLUDE_PCT=0.20` ✓（日志行含 max_exclude_pct）
- **MAX5 过滤逻辑等价**：build 侧 `pct_change().tail(20).max()` + 分位阈值 与 研究侧 L216-224 输出逐配置一致（pct=0.15/0.20/0.30 全同），彩票股被正确剔除 ✓
- **无 MOCK 残留**：build 内无 mock/test 代码 ✓

## 四、验证口径复述（审计已复核）

- 基线 `atr_10w_price50`：总 +208.8% / CAGR 16.95%（线性 28.99%）/ 回撤 -24.80% / 卡玛(CAGR) 0.68
- MAX5 `atr_10w_price50_a_max`：总 +237.6% / CAGR 18.40%（线性 32.99%）/ 回撤 -20.05% / 卡玛(CAGR) 0.92（+1.45pp，零成本尾部保险）