# 任务书：ATR 等权 build P0 修复（编码腐蚀重放）— OP 执行

> 委派：诚哥 ｜ 出具：GLM（审计方）2026-08-19 ｜ 执行者：OP（opencode）
> 性质：P0，目标是赶在下一交易日 09:15 前完成构建与审计，部署需远程访问方式（等诚哥）

## 背景一句话

Fix4（build 20260819-133000）逻辑正确但编码被腐蚀：全文 4168 个字面 `?`，含 3 处功能性致命串（`沪深A股`/`风险警示板`/`买入`/`卖出`），且从未真正部署到远程。需以 044755（git 849e08d，最后已知良好版）为基底重放 Fix4。

## 必读（按序，读完再动手）

1. `results/ATR零买入修复审计_GLM_20260819.md` — 事实源（腐蚀证据/部署拓扑/数据实证）
2. `results/GLM审计_8项修复方案_20260819.md` — 你自己的方案，**执行主体，但必须先看下面三条修正**
3. `results/ATR全天0买入根因与修复_20260818.md` — 原始根因链（背景）

## 对你方案的三条修正（强制）

1. **3b 无回归，不要"修"**：133000 与 044755 的消费处都已是 `if _g_turnover_available is True:`（已字节级核验）。你方案第 1 项是无操作，跳过，防止把好代码改坏。
2. **build.py 不存在**：项目根目录没有此文件（build 一直是手工维护）。你方案里的"build.py 强化"改为**新建** `validate_build.py`（见步骤 5）。
3. **N_HOLD 口径先于部署**：诚哥未拍板前按 n_hold=8 准备（本地配置已是 8）。远程配置同步在部署环节一并做，不单独先行。

## 执行步骤

1. **取基底**：`git show 849e08d:projects/Project_ATR_lowvol/build/strategy_atr_lowvol_equalweight.py` 导出为工作基底（044755，中文完好）。
2. **脚本化重放 Fix4**（禁止用 edit 工具直改 GBK 文件）：
   - 流程必须是 `raw.decode('gbk')` -> 在内存中做字符串手术 -> `encode('gbk')` 写回，全程一个脚本完成；
   - 移植三块逻辑，**中文一律取自 044755 基底或重新手写**，禁止从 133000/e4830b7 复制任何含中文的旧行（Fix4 新增块如 `仓位缩减`/`_get_account_cash` 的中文是完好的，可整块移植）：
     - **4a** 仓位缩减块（`_execute_buys_equalweight` 内，133000 L623-647）——按原样重放，**不顺手重设计**（"买不起就跳过"的重设计留待 N_HOLD 拍板后另做）；
     - **4b** 死循环修复（`_main_loop` 内：`if selected: _g_last_rebalance_key = key` + `_g_last_attempt_date = today_y`）；
     - **4c** `_get_account_cash` 三级 fallback 整个函数；
   - `BUILD_TAG` 递增为构建时刻（格式 YYYYmmdd-HHMMSS）。
3. **四道验证闸（全过才算完成）**：
   - R9 单测：`C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe research/tests/test_roe_r9_failopen.py` 必须 2/2 PASS；
   - 关键字符串断言：`沪深A股`/`风险警示板`/`买入`/`卖出`/`ATR_EW` 全部 in；
   - `txt.count('?') < 10`；
   - **diff 白名单**：与 044755 基底的差异只允许出现在上述三块逻辑 + BUILD_TAG 行 + 对应注释，其余任何差异都算失败。
4. **常规闸**：py3.6 语法（`ast.parse(feature_version=(3,6))`）、GBK 可解码、首行 `# coding=gbk`、无 f-string/walrus/`dict[str`/MOCK。
5. **新建 `validate_build.py`**（项目根目录）：固化第 3 步四道闸，今后每次构建必跑。
6. **commit**（信息注明 build tag 与"重放 Fix4"），然后**通知 GLM 复审**（我会重跑全部闸 + 抽查字节）。
7. **部署（GLM 放行后）**：经远程 QMT 客户端上传到远程机 `D:\国金QMT交易端模拟`（访问方式等诚哥提供）；同步远程 `D:\QMT_STRATEGIES/config/atr_lowvol_equalweight_config.yaml` 的 n_hold 口径（按诚哥拍板值）；本机 `E:\国金QMT交易端\python\` 的暂存副本可顺手更新但**不得**在日志里称为"已部署"。
8. **次日开盘核对**：F:\tmp\QMT\log（远程镜像）出现新 `build=`；`筛选完成: 候选N>0 -> 入选N只`；有真实买入；无每 3 秒刷屏；无对账异常。

## 禁止事项

- 禁用 edit 工具直改 GBK build 文件（本次事故根因）。
- 禁止把"复制到本机 E:"表述为"已部署"。
- 禁止动 src、broker、其他项目等无关模块；禁止顺手重构。
- 禁止覆盖/修改 results/ 下带 `_GLM_` 后缀的文档及本任务书。
- 生产产物不得混入 mock/测试代码。

## 待诚哥输入（阻塞项）

- N_HOLD 实盘口径拍板（建议 8）。
- 远程机 192.168.31.131 的访问方式（RDP / QMT 客户端在哪台操作）。

## 完成标准

四道验证闸 + 常规闸全绿，GLM 复审签字，远程日志出现新 build tag 且次日核对清单通过。缺任何一项即未完成。
