# Project_01 多因子IC小盘Alpha — 项目入口或共享任务本

## 当前任务

### T1: 验证 QMT passorder 委托通路
- **优先级**: P0
- **状态**: 待执行（明天开盘测试）
- **脚本**: `E:\QuantLab\scripts\daily_order_test.py`

### T2: 扩展因子库 — alpha101/gtja191 全量 IC 测试
- **优先级**: P1
- **状态**: 待启动

## 关键代码用法

### filter_func 市值过滤
```python
# 正确用法：filter_func 传入 scorer.score() 内部
mv_func = lambda p, f, d: (p.loc[d]['circ_mv'] > 0) & (p.loc[d]['circ_mv'] < 300000)

equity_df, trades_df, metrics = backtest_stop_loss(
    panel, fin_ffill,
    top_n=80,
    freq='2M',
    stop_loss=-0.12,
    filter_func=mv_func  # 关键参数
)

# 错误用法：外部过滤 codes 列表（会丢失评分信息，年化只有 4.6%）
filtered_codes = [c for c in codes if mv_check(c)]  # 不要这样做！
```

### 回测调用
```python
from research.backtest import backtest, backtest_stop_loss

# 无止损
equity_df, trades_df, metrics = backtest(panel, fin_ffill, top_n=80, freq='2M')

# 有止损
equity_df, trades_df, sl_df, metrics = backtest_stop_loss(
    panel, fin_ffill, top_n=80, freq='2M', stop_loss=-0.12, filter_func=mv_func
)
```

## 源码位置

| 文件 | 路径 |
|---|---|
| 评分器 | research/scoring.py |
| 回测引擎 | research/backtest.py |
| 数据加载 | research/data_loader.py |
| 因子计算 | research/factors.py |
