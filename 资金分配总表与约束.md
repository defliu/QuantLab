> ⚠️ 本文件是 `D:\QMT_STRATEGIES\资金分配总表与约束.md` 的**镜像副本**，置于 QuantLab 开发中枢（接手者入口），便于一眼看到资金硬规则。
> 单一事实源（机器可校验）：`D:\QMT_STRATEGIES\config\capital_allocation.yaml`；以事实源与本文件为准，两副本保持一致。

# 资金分配总表与账户总额约束（硬规则）

> **这是本项目资金管理的唯一权威文档。** 任何人在国金模拟账号 `67014907` 上新增 / 调整策略本金，必须先读此文件。
> 机器可校验事实源：`config/capital_allocation.yaml` ｜ 校验器：`scripts/check_capital_allocation.py`
> 红线位置：`D:\QuantLab\AGENTS.md`（资金分配红线）｜ 看板：`D:\QuantLab\全局控制台.md`

---

## 〇、三条硬规则（违反 = 事故）

1. **每个策略锁定独立「虚拟子账户」本金**（`capital_base`），只能动自己 ledger 里的票和自己的额度，**绝不抢占他人资金、绝不纳管/卖出他人持仓**。
2. **账户总额约束（最重要）**：`Σ 各策略 capital_base ≤ 账户实际总资产`。超额 = 多策略会互相抢现金、买入普遍失败。
3. **改了分配必须跑校验**：改 `config/capital_allocation.yaml` 或任一策略 `config` 的 `capital_base` 后，必须跑 `python scripts/check_capital_allocation.py`，通过（退出码 0）才能部署。

---

## 一、资金分配总表（当前）

| 策略 key | 名称 | 锁定本金(元) | 最大持仓 | 独立 config | 说明 |
|---|---|---:|---:|---|---|
| `atr_lowvol_equalweight` | ATR低波等权不杠杆 | 100,000 | 100 | `config/atr_lowvol_equalweight_config.yaml` | 满仓等权100只；NAV含浮盈滚动 |
| `value_smallcap_v2` | 价值小盘V2(退市排雷+buffer) | 100,000 | 80 | `config/value_smallcap_v2_config.yaml` | 纯行业中性BP+质量排雷+退市排雷+buffer160；双月调仓80只 |
| `huang529_breakout` | 黄氏529主升浪(ATR低波top16) | 100,000 | 12 | `D:/QMT_POOL/p13_huang529_config.yaml` | 529信号日更CSV+09:35执行；1/12等权、止损-12%/到期60交易日/trail15默认关 |
| **合计** | — | **300,000** | — | — | ≤ 账户总资产 10,000,000（剩余 9,700,000 / 97%） |

- **账户总额 `account.total_capital` = 10,000,000（1000万）**，2026-08-05 由大诚填报；校验器据此做硬约束。
- 新增策略：在 `config/capital_allocation.yaml` 的 `strategies:` 下追加一项（参考文件内注释模板），并建同名独立 config（含 `capital_base` 供校验器交叉核对）。

---

## 二、虚拟子账户模型（为什么能多策略共存）

同一共享账号下，各策略**不是物理隔离**，而是靠约定 + 代码纪律实现逻辑隔离：

- **各策略只认自己的钱**：买入规模 = `本策略 NAV / n_target`，`NAV = 本策略持仓当前市值（含浮盈）`，与账户里别人占了多少现金**无关**。
- **账户现金只是结算介质**：买时若账户现金不足（被别的策略用了），订单失败 → 下根 K 线重试，**不抢占别人的钱**。
- **各策略只动自己的票**：再平衡只买目标里自己没有/不足量的票、只卖自己 ledger 里不再在目标中的票（且只卖自己记录的量）；重叠股各调各的 delta，QMT 同质持仓净额结算后各留各的，**互不误卖**。

### ⚠️ 唯一无法靠代码根治的限制
虚拟子账户能防止 ATR-EW 去抢别人的钱，但**同一共享账户无法物理阻止别的策略花掉「你的10万」**。要真物理隔离，必须开**子账户 / 多模拟账号**。当前靠本总表的「Σ本金 ≤ 账户总额」约定保证不超额。

---

## 三、运维动作（必做）

```bash
# 1) 改完分配后校验（退出码 0 = 通过）
cd D:\QMT_STRATEGIES
python scripts/check_capital_allocation.py

# 2) 查账户总额（非交易时间也能查）
#    国金QMT客户端 -> 查询 -> 资金股份 -> 记录「总资产」-> 填进 config/capital_allocation.yaml 的 account.total_capital
```

- 校验器会同时核对每个策略独立 `config` 里的 `capital_base` 是否与总表一致，不一致直接报 `[FAIL]`。
- `total_capital` 未填时只警告不报错；**生产环境务必填真实值**，否则超额风险无人知晓。

---

## 四、关联文档

- 机器事实源：`config/capital_allocation.yaml`
- 校验器：`scripts/check_capital_allocation.py`
- 策略实现约束（10万虚拟子账户 + 收益滚动 + 持仓隔离）：`deploy/strategy_atr_lowvol_equalweight.py`
- 红线：`D:\QuantLab\AGENTS.md` → 资金分配红线
- 看板：`D:\QuantLab\全局控制台.md` → 资金分配总表与账户总额约束
- 知识库总入口：`F:\天翼云盘同步盘\Obsidian\量化知识库\60_工程知识库\量化开发全景说明.md`
