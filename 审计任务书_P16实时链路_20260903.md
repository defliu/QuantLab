# 审计任务书 · Project_16 实时链路修复验收

> **审计性质**：独立审计（只审计，不改代码、不动账本）
> **审计时点**：2026-09-03 02:28–02:40
> **审计对象**：Project_16 LightGBM 股票大师 G2（账号 70180771，大QMT 文件桥）
> **审计范围**：①前次审计提出的 4 项实时链路修复；②G2 首日（09-02）事故修复的落地闭环
> **审计方法**：代码 diff 核查 + parquet 实测 + 桥心跳/状态文件核验 + 对账报告复核
> **结论**：**4 项修复仅完成 1 项；另发现 3 项 P0 阻塞 09-03 开盘**

---

## 一、结论摘要

| ID | 事项 | 优先级 | 责任方 | 当前状态 |
|---|---|---|---|---|
| **T-01** | 桥策略修复版**未部署上线**（线上仍跑旧版） | **P0** | 用户（QMT 界面） | ❌ 未完成 |
| **T-02** | 持仓总额 **16.08 万 > 锁定本金 10 万**（+60.8%）违反资金红线 | **P0** | 用户拍板 + Agent 执行 | ❌ 未完成 |
| **T-03** | 账本与账户脱节（5 只孤儿 4100 股，对账仅预警零处置） | **P0** | Agent | ❌ 未完成 |
| T-04 | F6 估值字段缺失，估值类特征滞后 11 天 | P1 | Agent | ❌ 未开始 |
| T-05 | F3 催化无真实数据源，`scorecard_real.py` 未改动 | P1 | Agent | ❌ 未开始 |
| T-06 | 集合竞价字段单点依赖 mcp_tdx | P2 | Agent | ❌ 未开始 |
| T-07 | vol 字段名修复**已完成**，待验收归档 + 注释笔误 | P2 | Agent | ✅ 待归档 |
| T-08 | `全局控制台.md:16` 仍写 `E:/astock`（实际 `D:/astock`） | P2 | Agent | ❌ 未开始 |

---

## 二、P0 任务（阻塞 09-03 开盘，必须开盘前处理）

### T-01 · 桥策略修复版未部署上线

**现象**：status=55 双倍建仓的修复只落地到代码，生产环境仍在跑旧版。

**证据链**（三重独立印证）：

| 检查项 | 结果 |
|---|---|
| 源文件 `strategy/strategy_p16_g2_bridge_src.py:871` | `if status == 57` ✅ 已修 |
| 产物 `build/strategy_p16_g2_bridge.py:24` | `BUILD_TAG = "20260902-161814"` ✅ 已构建 |
| QMT 端 `D:/QMT交易端模拟/python/STRATEGY_P16_G2_BRIDGE.py` | `MiFBOec` 加密头，mtime **2026-09-01 14:25**，grep `20260902-161814` 命中 **0 次** |
| 心跳 `D:/QMT_POOL/g2_bridge/state/heart_20260902.json`（09-02 20:57） | `build_tag = "20260901-142420"` ❌ 旧版 |

> 心跳 `20260901-142420` 与 QMT 端文件 mtime（09-01 14:25）完全吻合，双向印证修复版从未上线。

**影响**：`_check_pending_orders` 1b 分支仍把 status=55（部成·活跃态）判为废单，`_handle_rejected_retry` 不撤原单直接重报剩余量 → **今日再遇部成仍会双倍建仓**。

**处置动作**（必须人工，QMT 端为加密密文，脚本无法覆盖）：
1. QMT 界面重新加载 `projects/Project_16_LightGBM股票大师/build/strategy_p16_g2_bridge.py`
2. 确认策略运行、心跳刷新

**验收标准**：
- `heart_<date>.json` 的 `build_tag == "20260902-161814"`
- QMT 主日志出现 `BUILD_TAG=20260902-161814`
- 产物校验（已由构建脚本覆盖，可复跑 `build_p16_g2.py` 确认）：`status==55` 残留 = 0、`status==57` 生效 = 1、GBK 头、无 f-string/walrus/MOCK

---

### T-02 · 持仓总额超资金池 60.8%，违反资金分配红线

**现象**：G2 实际持仓总市值远超锁定本金。

**证据**（`positions_20260902.json`，09-02 15:00:02 账户唯一真相）：

| 代码 | 股数 | 市值(元) | 可用 |
|---|---:|---:|---:|
| 600262.SH | 6400 | 100,544.00 | 0（T+1 锁定） |
| 300964.SZ | 1000 | 57,020.00 | 0（T+1 锁定） |
| 601988.SH | 200 | 1,332.00 | 200 |
| 600028.SH | 200 | 1,088.00 | 200 |
| 601398.SH | 100 | 820.00 | 100 |
| **合计** | | **160,804.00** | |

- G2 锁定本金（`g2_strategy_capital.json`）：**100,000 元**
- **超限：+60,804 元（+60.8%）**

**影响**：
1. 违反 AGENTS.md 资金分配红线「每个策略只能动自己 ledger 的票和自己的额度，**绝不抢占他人资金**」——账号 70180771 下还并行 ATR低波（10 万）与价值小盘V2（10 万），超额部分可能挤占他人额度
2. 600262 单只占 10.05 万，已超过全额本金，策略层面无分散可言

**处置动作**（需用户先拍板处置口径，再由 Agent 执行）：
- 选项 A：超额部分（600262 超 3400 股 / 300964 超 200 股）在 T+1 解锁后立即减持至资金池以内
- 选项 B：按 N=10 持有期逻辑到期（约 9/16）统一减持——**但此期间持续占用超额资金**
- 选项 C：上调 G2 `capital_base` 并同步 `config/capital_allocation.yaml` 重跑 `check_capital_allocation.py`（须满足 Σ capital_base ≤ 账户总资产）

**验收标准**：
- 持仓总市值 ≤ 锁定本金（或已按选项 C 合规上调并校验通过）
- `check_capital_allocation.py` 退出码 0

---

### T-03 · 账本与账户脱节，对账仅预警零处置

**现象**：G2 账本记录的持仓远少于账户实际持仓，5 只标的存在孤儿/超额。

**证据**（`data/reconcile_g2/reconcile_g2_20260902.md`，09-02 15:47）：

| 代码 | G2 账本 | 账户实际 | 差额 |
|---|---:|---:|---:|
| 600262.SH | 3000 | 6400 | **+3400** |
| 300964.SZ | 800 | 1000 | +200 |
| 600028.SH | 0 | 200 | +200 |
| 601398.SH | 0 | 100 | +100 |
| 601988.SH | 0 | 200 | +200 |

- 报告原文：「G2 不主动处理，**仅预警**」——5 只全部零处置
- `fills` 仅 2 条（retry 复用同 `strategy_order_id` 被覆盖）

**影响**：
1. 今日 09:50 换仓按 G2 账本算目标差额 → 账本 3000 vs 实际 6400，**差额计算错误**
2. 孤儿持仓无成本锚 → 桥内风控（止损/止盈/追盈）无法正确评估
3. 违反 AGENTS.md「账户 position 唯一真相」红线

**处置动作**（Agent）：
1. 以 `positions_20260902.json` 为唯一真相重写 G2 `positions_cfg`，补齐 5 只实际持仓与成本价
2. 与 T-02 的处置口径联动执行（减持超额 / 清孤儿）
3. 补齐每日 `positions_cfg` 自动化写入（当前无脚本，桥无成本锚——此项为控制台 T-20260902-001 待办③，建议一并纳入）

**验收标准**：
- `reconcile_g2.py` 对账报告**无持仓差额告警**（账本 == 账户 positions）
- 孤儿持仓已纳管且成本锚可读，桥内风控可正常触发

---

## 三、P1 任务（本周内）

### T-04 · F6 估值字段缺失，估值类特征滞后 11 天

**现象**：前次审计提出「给增量库补 PE/PB/换手率/流通市值」，未执行。

**证据**：
- `data_live/incremental_daily.parquet`（46,608 行，08-24 → 09-02）**仍仅 10 列**：
  `trade_date / ts_code / open / high / low / close / volume / amount / preClose / suspendFlag`
  —— **无** `pe_ttm / turnover_rate / volume_ratio / pb / dv_ttm / circ_mv / is_st / pct_chg`
- `build_g2_daily.py:127-130` 的 `fill_cols` 未改动，仍从 `main_last`（主库，实测到 **08-21**）map 给增量所有行
- `deploy_predict_g2.py:84` 仍从 `DC.MAIN_DAILY` 取 `pe_ttm`/`turnover_rate` 作 F6

**影响**：F6 估值（权重 0.10）+ `turn_ma5`/`log_mv`/`volume_ratio` 均为 11 天前陈旧值。

> 数据管道本身是通的（增量已到 09-02，8 个交易日），只是字段不全。

**处置动作**：
1. 每日盘后批量抓全市场估值指标补进增量库
2. **可复用现成代码**：`data_sources.py:95` 已有 `fetch_tencent`（qt.gtimg.cn，可拿 PE/PB/换手率/流通市值，项目内已实现且实测可用）
3. 补充后移除 `fill_cols` 的陈旧值 map 逻辑（或改为仅兜底）
4. 注意 AGENTS.md 红线：`C.get_market_data_ex` 无财务字段，不能指望 xtdata

**验收标准**：
- 增量库含 `pe_ttm / turnover_rate / volume_ratio / pb / circ_mv / is_st / pct_chg`，且值为 T 日真实值
- 快照 `pe_ttm` 缺失率与训练面板同量级（当前 30.3%，需确认非陈旧导致）
- `deploy_predict_g2.py` 的 F6 输入日期 == 目标交易日

---

### T-05 · F3 催化无真实数据源

**现象**：前次审计提出「F3 走悟道 MCP 或新浪智研」，未执行。

**证据**：
- `scorecard_real.py` **git diff 为空**（未改动），第 8 行仍为「F1/F3/F4 沿用 DP.compute_scorecard 代理分（**无真实列**，保持不变）」
- F3 权重 **0.20**，与 F2 并列第二高

**处置动作**（二选一，建议先 A）：
- **A（零成本，建议）**：悟道 MCP（`mcp_wudao`，已有新闻/公告/题材热度）当日文件落地，走 `fetch_lhb_from_mcp_file` 同款文件桥模式
- **B（约 24 元/月）**：新浪智研 API——但 Key 余额不足且**接口 code 至今未确认**（资金趋势/公司资讯 code 均待定），需先调通

**验收标准**：F3 有真实催化分输入，非代理分；`selection_full.csv` 的 F3 列有真实数据来源标注

---

## 四、P2 任务（择机）

### T-06 · 集合竞价字段单点

`call_auction_amount/rate` 仅 mcp_tdx 提供（`MCP数据链路说明.md` 高危点 2），该源故障则 9:25 竞价预判无替代。`g2_realtime.py` 本轮无相关改动。

**动作**：接入悟道 `intraday_main_flow.auctionMainNetAmount` 作备份。
**验收**：mcp_tdx 不可用时，9:25 任务仍能产出竞价预判（标注降级来源）。

---

### T-07 · vol 字段名修复 —— 已完成，待验收归档

**本轮唯一完成项，修复质量合格。**

**证据**：
- `build_g2_daily.py:123-126` 新增 `rename(columns={"volume": "vol"})`
- 快照 `g2_latest_features.parquet`（09-02 16:47 重生成，目标日 **2026-09-02**，T+0）

| 特征 | 修复前（08-31 快照） | 修复后（09-02 快照） |
|---|---:|---:|
| `vol_ratio_5_20` | **100% 缺失**（0/5826） | **0.2% 缺失**（5815/5826） |
| `amount_ma5` | 0.0% | 0.0% |
| `rsi6` / `macd_hist` | 0.0% | 0.0% |

**待办**：
1. 验收后归档（建议登记到全局控制台 DONE）
2. 修注释笔误：`build_g2_daily.py:122` 写「vol_ratio_5_20/**vol_ratio_5_20**」重复，应改为 `vol_ratio_5_20 / vol_ma5 / vol_ma20` 之类实际受影响特征

---

### T-08 · 文档路径残留

`全局控制台.md:16` 仍写 `E:/astock/` 为第一数据源；实际 `data_config.py:12` 已于 2026-08-28 迁至 `D:/astock`（注释注明本机无 E 盘）。`g2_realtime.py` 内注释本轮已修正，控制台未同步。

**动作**：修正控制台第 16 行及全文残留的 `E:/astock` 表述。

---

## 五、开盘前必做清单（09-03 09:15 前）

- [ ] **T-01-a** 用户：QMT 界面重载 `build/strategy_p16_g2_bridge.py`（BUILD_TAG 20260902-161814）
- [ ] **T-01-b** 验证：心跳 `build_tag == "20260902-161814"`
- [ ] **T-02** 用户拍板：超额 60,804 元的处置口径（A 立即减持 / B 到期减持 / C 合规上调本金）
- [ ] **T-03** Agent：按账户实况重写 `positions_cfg`（依 T-02 口径执行）
- [ ] **T-03** 验证：`reconcile_g2.py` 对账无差额告警
- [ ] **T-02** 验证：`check_capital_allocation.py` 退出码 0

> **若 T-01 未完成，建议暂停 09-03 自动换仓**（`30344e79` 09:50）——旧版在部成场景会重复下单，且账本错误会导致差额计算失真。

---

## 六、证据索引

| 编号 | 证据 | 路径/位置 |
|---|---|---|
| E-1 | 修复版产物 BUILD_TAG | `projects/Project_16_LightGBM股票大师/build/strategy_p16_g2_bridge.py:24` |
| E-2 | 源文件 status 分支已修 | `.../strategy/strategy_p16_g2_bridge_src.py:871`（`status == 57`） |
| E-3 | QMT 端部署文件（加密，旧版） | `D:/QMT交易端模拟/python/STRATEGY_P16_G2_BRIDGE.py`（mtime 2026-09-01 14:25，`MiFBOec` 头） |
| E-4 | 桥心跳（线上版本判据） | `D:/QMT_POOL/g2_bridge/state/heart_20260902.json` → `build_tag: "20260901-142420"` |
| E-5 | 账户持仓（唯一真相） | `D:/QMT_POOL/g2_bridge/state/positions_20260902.json`（09-02 15:00:02） |
| E-6 | 资金池本金 | `D:/QMT_POOL/g2_bridge/g2_strategy_capital.json` → `capital: 100000` |
| E-7 | 对账报告（5 只差额） | `.../data/reconcile_g2/reconcile_g2_20260902.md`（09-02 15:47） |
| E-8 | vol 修复验证 | `.../build_g2_daily.py:123-126` + `data_live/g2_latest_features.parquet`（09-02 16:47） |
| E-9 | 增量库字段（仍 10 列） | `.../data_live/incremental_daily.parquet`（46,608 行 / 08-24→09-02） |
| E-10 | F3 未改动 | `git diff -- .../scorecard_real.py` 为空 |
| E-11 | 前次审计记录 | `.workbuddy/memory/2026-09-02.md` |

---

## 七、审计声明

1. 本任务书为**独立审计产出**，审计过程未修改任何代码、配置或账本。
2. T-01 因 QMT 端为加密密文，**只能人工操作**，无脚本替代方案。
3. T-02/T-03 涉及实盘持仓与资金处置，需用户先拍板口径再执行。
4. 本任务书**不代替** `全局控制台.md` 看板登记；按项目硬规则，执行方在任务新建/完成/阻塞/拍板时应同步更新控制台（T-20260902-001 已有相关待办记录，与本任务书 T-01/T-03 一致，可合并跟踪）。
5. 审计中所有数字均为 2026-09-03 02:28–02:40 实测所得，可复现。

---

*审计人：WorkBuddy（独立审计）｜ 2026-09-03 02:40*
